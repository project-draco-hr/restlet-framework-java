def test_ints(self):
    import sys
    n = sys.maxint
    while n:
        for expected in ((- n), n):
            s = self.dumps(expected)
            n2 = self.loads(s)
            self.assertEqual(expected, n2)
        n = (n >> 1)
