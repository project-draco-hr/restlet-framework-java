{
  WadlComponent comp=new WadlComponent("clap://class/org/restlet/test/YahooSearch.wadl");
  WadlApplication app=(WadlApplication)comp.getHosts().get(0).getRoutes().get(0).getNext();
  assertNotNull(app);
  assertEquals(app.getRoot(),app.getRouter());
  assertNotNull(app.getRoot());
}
