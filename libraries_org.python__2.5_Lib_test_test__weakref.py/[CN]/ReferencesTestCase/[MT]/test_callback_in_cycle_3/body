def test_callback_in_cycle_3(self):
    import gc


    class C:

        def cb(self, ignore):
            self.me
            self.c1
            self.wr
    (c1, c2) = (C(), C())
    c2.me = c2
    c2.c1 = c1
    c2.wr = weakref.ref(c1, c2.cb)
    del c1, c2
    gc.collect()
