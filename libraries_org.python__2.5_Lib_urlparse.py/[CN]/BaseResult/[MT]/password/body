@property
def password(self):
    netloc = self.netloc
    if ('@' in netloc):
        userinfo = netloc.split('@', 1)[0]
        if (':' in userinfo):
            return userinfo.split(':', 1)[1]
    return None
