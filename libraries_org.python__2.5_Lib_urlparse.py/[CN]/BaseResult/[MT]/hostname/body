@property
def hostname(self):
    netloc = self.netloc
    if ('@' in netloc):
        netloc = netloc.split('@', 1)[1]
    if (':' in netloc):
        netloc = netloc.split(':', 1)[0]
    return (netloc.lower() or None)
