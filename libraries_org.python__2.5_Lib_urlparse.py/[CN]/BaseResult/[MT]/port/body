@property
def port(self):
    netloc = self.netloc
    if ('@' in netloc):
        netloc = netloc.split('@', 1)[1]
    if (':' in netloc):
        port = netloc.split(':', 1)[1]
        return int(port, 10)
    return None
