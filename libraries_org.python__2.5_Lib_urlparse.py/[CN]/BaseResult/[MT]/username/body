@property
def username(self):
    netloc = self.netloc
    if ('@' in netloc):
        userinfo = netloc.split('@', 1)[0]
        if (':' in userinfo):
            userinfo = userinfo.split(':', 1)[0]
        return userinfo
    return None
