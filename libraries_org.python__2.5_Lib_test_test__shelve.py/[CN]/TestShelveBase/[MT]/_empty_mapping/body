def _empty_mapping(self):
    if self._in_mem:
        x = shelve.Shelf({}, **self._args)
    else:
        self.counter += 1
        x = shelve.open((self.fn + str(self.counter)), **self._args)
    self._db.append(x)
    return x
