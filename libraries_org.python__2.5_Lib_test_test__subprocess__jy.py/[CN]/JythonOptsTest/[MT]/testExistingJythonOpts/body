def testExistingJythonOpts(self):
    options = '-Qold -Qwarn'
    os.environ['JYTHON_OPTS'] = options
    p1 = Popen([sys.executable, '-c', 'import os, sys; sys.stdout.write(os.environ["JYTHON_OPTS"])'], stdout=PIPE)
    self.assertEquals(options, p1.stdout.read())
