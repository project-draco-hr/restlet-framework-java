def testDefaultEnvIsInherited(self):
    os.environ['foo'] = 'something'
    p1 = Popen([sys.executable, '-c', 'import os, sys; sys.stdout.write(os.environ["foo"])'], stdout=PIPE)
    self.assertEquals('something', p1.stdout.read())
