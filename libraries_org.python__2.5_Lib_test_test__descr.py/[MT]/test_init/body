def test_init():


    class Foo(object):

        def __init__(self):
            return 10
    try:
        Foo()
    except TypeError:
        pass
    else:
        raise TestFailed, 'did not test __init__() for None return'
