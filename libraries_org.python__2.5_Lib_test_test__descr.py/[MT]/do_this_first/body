def do_this_first():
    if verbose:
        print 'Testing SF bug 551412 ...'


    class UserLong(object):

        def __pow__(self, *args):
            pass
    try:
        pow(0L, UserLong(), 0L)
    except:
        pass
    if verbose:
        print 'Testing SF bug 570483...'
    type.mro(tuple)
