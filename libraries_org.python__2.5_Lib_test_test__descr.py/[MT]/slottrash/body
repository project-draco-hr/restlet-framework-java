def slottrash():
    if verbose:
        print 'Testing slot trash...'


    class trash(object):
        __slots__ = ['x']

        def __init__(self, x):
            self.x = x
    o = None
    for i in xrange(50000):
        o = trash(o)
    del o
