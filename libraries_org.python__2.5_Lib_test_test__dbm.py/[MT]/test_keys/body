def test_keys():
    d = dbm.open(filename, 'c')
    verify((d.keys() == []))
    d['a'] = 'b'
    d['12345678910'] = '019237410982340912840198242'
    d.keys()
    if d.has_key('a'):
        if verbose:
            print 'Test dbm keys: ', d.keys()
    d.close()
