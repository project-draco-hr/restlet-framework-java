{
  if (Edition.CURRENT != Edition.GWT) {
    return createSeries(getDelegate().subList(fromIndex,toIndex));
  }
 else {
    return createSeries(org.restlet.engine.util.ListUtils.copySubList(getDelegate(),fromIndex,toIndex));
  }
}
