{
  if (Edition.CURRENT != Edition.GWT) {
    return new Series(series.entryClass,java.util.Collections.unmodifiableList(series.getDelegate()));
  }
  return new Series(series.entryClass,(List<NamedValue>)series.getDelegate());
}
