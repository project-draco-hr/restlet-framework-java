def assertEquals(self, a, b, ignore=None):
    ' succeed iff a == b or a in ignore or b in ignore '
    if ((ignore == None) or (a in ignore) or (b in ignore)):
        return
    unittest.TestCase.assertEquals(self, a, b)
