def start_python(self, cmd_line):
    (outfp, infp) = popen2.popen4(('"%s" %s' % (sys.executable, cmd_line)))
    infp.close()
    data = outfp.read()
    outfp.close()
    popen2._cleanup()
    return data
