def test_run_module(self):
    self.assertNotEqual(self.exit_code('-m'), 0)
    self.assertNotEqual(self.exit_code('-m', 'fnord43520xyz'), 0)
    self.assertNotEqual(self.exit_code('-m', 'runpy', 'fnord43520xyz'), 0)
    self.assertEqual(self.exit_code('-m', 'timeit', '-n', '1'), 0)
