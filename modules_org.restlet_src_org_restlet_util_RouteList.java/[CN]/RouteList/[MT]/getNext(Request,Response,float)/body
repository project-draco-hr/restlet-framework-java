{
  Route[] routes=toArray();
  for (int initialIndex=lastIndex++; initialIndex != lastIndex; lastIndex++) {
    if (lastIndex >= routes.length) {
      lastIndex=0;
    }
    if (routes[lastIndex].score(request,response) >= requiredScore)     return routes[lastIndex];
  }
  return null;
}
