def load_dict(self):
    k = self.marker()
    d = {}
    items = self.stack[(k + 1):]
    for i in range(0, len(items), 2):
        key = items[i]
        value = items[(i + 1)]
        d[key] = value
    self.stack[k:] = [d]
