def load_int(self):
    data = self.readline()
    if (data == FALSE[1:]):
        val = False
    elif (data == TRUE[1:]):
        val = True
    else:
        try:
            val = int(data)
        except ValueError:
            val = long(data)
    self.append(val)
