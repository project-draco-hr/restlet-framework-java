def put(self, i, pack=struct.pack):
    if self.bin:
        if (i < 256):
            return (BINPUT + chr(i))
        else:
            return (LONG_BINPUT + pack('<i', i))
    return ((PUT + repr(i)) + '\n')
