def get(self, i, pack=struct.pack):
    if self.bin:
        if (i < 256):
            return (BINGET + chr(i))
        else:
            return (LONG_BINGET + pack('<i', i))
    return ((GET + repr(i)) + '\n')
