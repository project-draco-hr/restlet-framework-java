{
  if ((getState() != ConnectionState.CLOSING) && (getState() != ConnectionState.CLOSED)) {
    getInboundWay().updateState();
    getOutboundWay().updateState();
    getSocketRegistration().setInterestOperations(getSocketInterestOps());
  }
}
