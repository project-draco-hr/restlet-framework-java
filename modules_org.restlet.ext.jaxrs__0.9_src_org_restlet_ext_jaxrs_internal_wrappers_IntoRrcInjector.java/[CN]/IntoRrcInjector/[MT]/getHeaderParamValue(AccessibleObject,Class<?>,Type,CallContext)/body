{
  HeaderParam headerParam=fieldOrBeanSetter.getAnnotation(HeaderParam.class);
  DefaultValue defaultValue=fieldOrBeanSetter.getAnnotation(DefaultValue.class);
  Object value=WrapperUtil.getHeaderParamValue(convTo,paramGenericType,headerParam,defaultValue,callContext);
  return value;
}
