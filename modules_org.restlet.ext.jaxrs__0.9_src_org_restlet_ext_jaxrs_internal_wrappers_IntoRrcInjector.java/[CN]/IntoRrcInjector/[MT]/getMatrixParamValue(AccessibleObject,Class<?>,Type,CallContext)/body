{
  MatrixParam headerParam=fieldOrBeanSetter.getAnnotation(MatrixParam.class);
  DefaultValue defaultValue=fieldOrBeanSetter.getAnnotation(DefaultValue.class);
  Object value=WrapperUtil.getMatrixParamValue(convTo,paramGenericType,headerParam,leaveEncoded,defaultValue,callContext);
  return value;
}
