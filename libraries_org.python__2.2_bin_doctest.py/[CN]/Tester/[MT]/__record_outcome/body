def __record_outcome(self, name, f, t):
    if self.name2ft.has_key(name):
        print (("*** Warning: '" + name) + "' was tested before;"), 'summing outcomes.'
        (f2, t2) = self.name2ft[name]
        f = (f + f2)
        t = (t + t2)
    self.name2ft[name] = (f, t)
