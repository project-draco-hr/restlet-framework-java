{
  if (mmap == null)   return null;
  Form form=new Form();
  for (  Map.Entry<String,List<String>> entry : mmap.entrySet()) {
    String name=entry.getKey();
    for (    String value : entry.getValue())     form.add(name,value);
  }
  return form;
}
