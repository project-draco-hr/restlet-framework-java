{
  if (requestEntityStream == null) {
    if (isRequestChunked()) {
      requestEntityStream=new ChunkedInputStream(getRequestStream());
    }
 else {
      requestEntityStream=new InputEntityStream(getRequestStream(),size);
    }
  }
  return requestEntityStream;
}
