{
  for (  Parameter header : getRequestHeaders()) {
    if (header.getName().equalsIgnoreCase(HttpConstants.HEADER_TRANSFER_ENCODING)) {
      if (header.getValue().equalsIgnoreCase("chunked")) {
        return true;
      }
    }
  }
  return false;
}
