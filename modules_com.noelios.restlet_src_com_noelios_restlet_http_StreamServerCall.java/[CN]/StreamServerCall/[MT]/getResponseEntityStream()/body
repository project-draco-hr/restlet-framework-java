{
  if (this.responseEntityStream == null) {
    if (isResponseChunked()) {
      this.responseEntityStream=new ChunkedOutputStream(getResponseStream());
    }
 else {
      this.responseEntityStream=new KeepAliveOutputStream(getResponseStream());
    }
  }
  return this.responseEntityStream;
}
