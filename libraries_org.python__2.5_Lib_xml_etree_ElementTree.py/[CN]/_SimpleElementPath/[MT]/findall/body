def findall(self, element, tag):
    if (tag[:3] == './/'):
        return element.getiterator(tag[3:])
    result = []
    for elem in element:
        if (elem.tag == tag):
            result.append(elem)
    return result
