def _fixname(self, key):
    try:
        name = self._names[key]
    except KeyError:
        name = key
        if ('}' in name):
            name = ('{' + name)
        self._names[key] = name = self._fixtext(name)
    return name
