def test_mutatingiteration(self):
    d = {}
    d[1] = 1
    try:
        for i in d:
            d[(i + 1)] = 1
    except RuntimeError:
        pass
    else:
        self.fail("changing dict size during iteration doesn't raise Error")
