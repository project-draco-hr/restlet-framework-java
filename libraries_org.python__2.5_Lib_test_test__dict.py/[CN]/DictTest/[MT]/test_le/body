def test_le(self):
    self.assert_((not ({} < {})))
    self.assert_((not ({1: 2, } < {1L: 2L, })))


    class Exc(Exception):
        pass


    class BadCmp(object):

        def __eq__(self, other):
            raise Exc()
    d1 = {BadCmp(): 1, }
    d2 = {1: 1, }
    try:
        (d1 < d2)
    except Exc:
        pass
    else:
        self.fail("< didn't raise Exc")
