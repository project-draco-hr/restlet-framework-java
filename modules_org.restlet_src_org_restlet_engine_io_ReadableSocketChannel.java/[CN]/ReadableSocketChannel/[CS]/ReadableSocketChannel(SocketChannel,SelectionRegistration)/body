{
  super(wrappedChannel,registration);
  if (Context.getCurrentLogger().isLoggable(Level.FINER)) {
    Context.getCurrentLogger().log(Level.FINER,"ReadableSocketChannel created from: " + wrappedChannel + ","+ registration+ ". Registration: "+ getRegistration());
  }
}
