{
  try {
    return ContentType.parseContentType(contentType);
  }
 catch (  final Exception e) {
    throw new IllegalArgumentException("The content type string \"" + contentType + "\" can not be parsed: "+ e.getMessage());
  }
}
