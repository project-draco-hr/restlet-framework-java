{
  int c;
  do {
    c=getContentHandler().step();
  }
 while (c != RdfN3ParsingContentHandler.EOF && !RdfN3ParsingContentHandler.isDelimiter(c));
  setValue(getContentHandler().getCurrentToken());
}
