def test_badentry(self):


    class Exc(Exception):
        pass


    class Bad:

        def __eq__(self, other):
            raise Exc
    x = [Bad()]
    y = [Bad()]
    for op in opmap['eq']:
        self.assertRaises(Exc, op, x, y)
