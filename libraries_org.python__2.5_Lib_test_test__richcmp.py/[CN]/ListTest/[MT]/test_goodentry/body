def test_goodentry(self):


    class Good:

        def __lt__(self, other):
            return True
    x = [Good()]
    y = [Good()]
    for op in opmap['lt']:
        self.assertIs(op(x, y), True)
