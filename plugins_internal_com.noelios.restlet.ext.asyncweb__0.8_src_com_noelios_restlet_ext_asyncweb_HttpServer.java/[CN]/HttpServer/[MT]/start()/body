{
  if (!isStarted()) {
    NIOTransport nio=new NIOTransport();
    nio.setPort(getPort());
    nio.setServiceContainer(this);
    nio.setIoWorkerCount(getIoWorkerCount());
    setTransport(nio);
    super.start();
  }
}
