{
  List<Field> icf=new ArrayList<Field>();
  for (  Field field : this.jaxRsClass.getFields()) {
    if (field.isAnnotationPresent(Context.class))     icf.add(field);
  }
  this.injectContextFields=icf.toArray(EMPTY_FIELD_ARRAY);
}
