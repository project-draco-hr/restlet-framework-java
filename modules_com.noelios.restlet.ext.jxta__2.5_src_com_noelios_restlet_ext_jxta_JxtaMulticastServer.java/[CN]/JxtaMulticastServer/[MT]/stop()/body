{
  if (socket == null) {
    return;
  }
  socket.disconnect();
  socket.close();
  executor.shutdown();
  while (!executor.isTerminated()) {
    try {
      Thread.sleep(100);
    }
 catch (    InterruptedException ie) {
    }
  }
  socket=null;
}
