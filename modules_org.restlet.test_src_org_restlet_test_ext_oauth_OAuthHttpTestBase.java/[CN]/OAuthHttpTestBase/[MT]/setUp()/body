{
  super.setUp();
  setupConnectors(cc,sc);
  if (https)   setupHttps();
  List<AuthenticatorHelper> authenticators=Engine.getInstance().getRegisteredAuthenticators();
  authenticators.add(new OAuthHelper());
  if (multiple)   setupMultiple();
 else   setupSingle();
}
