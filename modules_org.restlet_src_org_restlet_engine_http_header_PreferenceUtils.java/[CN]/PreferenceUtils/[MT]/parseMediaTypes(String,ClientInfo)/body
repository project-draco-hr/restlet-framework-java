{
  if (acceptMediaTypeHeader != null) {
    AcceptReader pr=new AcceptReader(AcceptReader.TYPE_MEDIA_TYPE,acceptMediaTypeHeader);
    pr.addValues(preference.getAcceptedMediaTypes());
  }
 else {
    preference.getAcceptedMediaTypes().add(new Preference(MediaType.ALL));
  }
}
