def test_range(self):
    req = urllib2.Request('http://www.python.org', headers={'Range': 'bytes=20-39', })
    result = urllib2.urlopen(req)
    data = result.read()
    self.assertEqual(len(data), 20)
