{
  if (!isEmpty()) {
    for (int initialIndex=lastIndex++; initialIndex != lastIndex; lastIndex++) {
      if (lastIndex >= size()) {
        lastIndex=0;
      }
      Route route=get(lastIndex);
      if (route.score(request,response) >= requiredScore)       return route;
    }
  }
  return null;
}
