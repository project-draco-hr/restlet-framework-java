{
  if (this.adapter == null) {
    final String adapterClass=getHelpedParameters().getFirstValue("adapter","org.restlet.engine.http.HttpClientAdapter");
    this.adapter=(HttpClientAdapter)Class.forName(adapterClass).getConstructor(Context.class).newInstance(getContext());
  }
  return this.adapter;
}
