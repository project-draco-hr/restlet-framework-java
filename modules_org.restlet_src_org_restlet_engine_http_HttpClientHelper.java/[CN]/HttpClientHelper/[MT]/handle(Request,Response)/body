{
  try {
    final HttpClientCall httpCall=getAdapter().toSpecific(this,request);
    getAdapter().commit(httpCall,request,response);
  }
 catch (  Exception e) {
    getLogger().log(Level.INFO,"Error while handling an HTTP client call",e);
    response.setStatus(Status.CONNECTOR_ERROR_INTERNAL,e);
  }
}
