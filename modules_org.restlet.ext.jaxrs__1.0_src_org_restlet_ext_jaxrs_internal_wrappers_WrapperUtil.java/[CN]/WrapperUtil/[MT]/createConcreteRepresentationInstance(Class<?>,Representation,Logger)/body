{
  if (representationType.equals(Representation.class))   return entity;
  Constructor<?> constr;
  try {
    constr=representationType.getConstructor(Representation.class);
  }
 catch (  SecurityException e) {
    logger.warning("The constructor " + representationType + "(Representation) is not accessable.");
    return null;
  }
catch (  NoSuchMethodException e) {
    return null;
  }
  try {
    return constr.newInstance(entity);
  }
 catch (  Exception e) {
    throw ConvertRepresentationException.object(representationType,"the message body",e);
  }
}
