def test_tmpfile(self):
    if (not hasattr(os, 'tmpfile')):
        return
    fp = os.tmpfile()
    fp.write('foobar')
    fp.seek(0, 0)
    s = fp.read()
    fp.close()
    self.assert_((s == 'foobar'))
