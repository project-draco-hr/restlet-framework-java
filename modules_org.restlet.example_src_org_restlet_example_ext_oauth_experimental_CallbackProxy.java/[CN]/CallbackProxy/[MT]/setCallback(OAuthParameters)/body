{
  String clientId=callback.getClientId();
  String clientSecret=callback.getClientSecret();
  Client client=clients.findById(clientId);
  if (client == null)   client=clients.createClient(clientId,clientSecret,null);
  getLogger().info("Registered callback client - " + clientId + " : "+ clientSecret);
}
