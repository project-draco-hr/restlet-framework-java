def check(self, value, expected):
    self.assertEqual(type(value), type(expected), ('%r is a %s, not a %s' % (value, type(value), type(expected))))
    self.assertEqual(value, expected, ('%r (%s) != %r (%s)' % (value, type(value), expected, type(expected))))
