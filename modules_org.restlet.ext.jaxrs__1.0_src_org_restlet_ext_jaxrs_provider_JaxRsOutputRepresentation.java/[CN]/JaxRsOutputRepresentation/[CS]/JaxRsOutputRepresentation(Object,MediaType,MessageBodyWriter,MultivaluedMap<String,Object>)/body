{
  super(mediaType,mbw.getSize(object));
  if (!Util.isConcrete(mediaType))   throw new IllegalArgumentException(mediaType + " is not concrete");
  this.mbw=mbw;
  this.httpHeaders=httpHeaders;
  this.object=object;
}
