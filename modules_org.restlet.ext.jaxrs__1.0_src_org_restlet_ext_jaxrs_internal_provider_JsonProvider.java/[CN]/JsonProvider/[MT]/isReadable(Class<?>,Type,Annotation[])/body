{
  if (JSONObject.class.isAssignableFrom(type))   return true;
  if (JSONArray.class.isAssignableFrom(type))   return true;
  if (JSONString.class.isAssignableFrom(type))   return true;
  return false;
}
