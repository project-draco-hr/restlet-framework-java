def setUp(self):
    try:
        os.mkdir(test_support.TESTFN)
    except OSError:
        pass
    for name in self.files:
        f = open(name, 'w')
        f.write((name + '\n').encode('utf-8'))
        f.close()
        os.stat(name)
