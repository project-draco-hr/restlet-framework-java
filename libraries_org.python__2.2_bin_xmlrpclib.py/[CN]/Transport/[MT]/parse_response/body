def parse_response(self, f):
    (p, u) = self.getparser()
    while 1:
        response = f.read(1024)
        if (not response):
            break
        if self.verbose:
            print 'body:', repr(response)
        p.feed(response)
    f.close()
    p.close()
    return u.close()
