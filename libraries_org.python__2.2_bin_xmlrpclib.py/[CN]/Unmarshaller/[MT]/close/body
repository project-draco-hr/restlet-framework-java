def close(self):
    if ((self._type is None) or self._marks):
        raise ResponseError()
    if (self._type == 'fault'):
        raise apply(Fault, (), self._stack[0])
    return tuple(self._stack)
