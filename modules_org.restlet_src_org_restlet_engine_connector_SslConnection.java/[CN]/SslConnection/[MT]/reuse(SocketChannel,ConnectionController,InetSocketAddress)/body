{
  super.reuse(socketChannel,controller,socketAddress);
  if (getSslContext() != null) {
    this.sslEngine=getSslContext().createSSLEngine(socketAddress.getHostName(),socketAddress.getPort());
  }
}
