{
  do {
    consumeWhiteSpaces();
switch (getChar()) {
case '@':
      parseDirective(context);
    break;
case '#':
  parseComment();
break;
case '.':
break;
default :
parseStatement(context);
break;
}
}
 while (getChar() != RdfN3ContentHandler.EOF);
}
