def _search_end(self):
    while 1:
        pos = self.fp.tell()
        line = self.fp.readline()
        if (not line):
            return
        if (line == '\x01\x01\x01\x01\n'):
            self.fp.seek(pos)
            return
