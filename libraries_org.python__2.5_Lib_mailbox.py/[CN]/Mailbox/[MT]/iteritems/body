def iteritems(self):
    'Return an iterator over (key, message) tuples.'
    for key in self.iterkeys():
        try:
            value = self[key]
        except KeyError:
            continue
        yield (key, value)
