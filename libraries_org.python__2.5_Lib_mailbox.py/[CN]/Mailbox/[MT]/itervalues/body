def itervalues(self):
    'Return an iterator over all messages.'
    for key in self.iterkeys():
        try:
            value = self[key]
        except KeyError:
            continue
        yield value
