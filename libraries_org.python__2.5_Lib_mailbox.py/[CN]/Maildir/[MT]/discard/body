def discard(self, key):
    'If the keyed message exists, remove it.'
    try:
        self.remove(key)
    except KeyError:
        pass
    except OSError as e:
        if (e.errno != errno.ENOENT):
            raise
