def iterkeys(self):
    'Return an iterator over keys.'
    self._refresh()
    for key in self._toc:
        try:
            self._lookup(key)
        except KeyError:
            continue
        yield key
