def unlock(self):
    'Unlock the mailbox if it is locked.'
    if self._locked:
        _unlock_file(self._file)
        _sync_close(self._file)
        self._file.close()
        del self._file
        self._locked = False
