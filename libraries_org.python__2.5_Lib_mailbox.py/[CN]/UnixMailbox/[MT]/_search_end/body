def _search_end(self):
    self.fp.readline()
    while 1:
        pos = self.fp.tell()
        line = self.fp.readline()
        if (not line):
            return
        if ((line[:5] == 'From ') and self._isrealfromline(line)):
            self.fp.seek(pos)
            return
