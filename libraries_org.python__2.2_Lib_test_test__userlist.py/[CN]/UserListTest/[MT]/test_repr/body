def test_repr(self):
    l0 = []
    l2 = [0, 1, 2]
    u0 = UserList(l0)
    u2 = UserList(l2)
    self.assertEqual(str(u0), str(l0))
    self.assertEqual(repr(u0), repr(l0))
    self.assertEqual(`u2`, `l2`)
