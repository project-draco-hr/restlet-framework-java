def test_extend(self):
    u1 = UserList((0,))
    u2 = UserList((0, 1))
    u = u1[:]
    u.extend(u2)
    self.assertEqual(u, (u1 + u2))
    u = UserList('spam')
    u.extend('eggs')
    self.assertEqual(u, list('spameggs'))
