def test_getslice(self):
    l = [0, 1]
    u = UserList(l)
    for i in xrange((-3), 4):
        self.assertEqual(u[:i], l[:i])
        self.assertEqual(u[i:], l[i:])
        for j in xrange((-3), 4):
            self.assertEqual(u[i:j], l[i:j])
