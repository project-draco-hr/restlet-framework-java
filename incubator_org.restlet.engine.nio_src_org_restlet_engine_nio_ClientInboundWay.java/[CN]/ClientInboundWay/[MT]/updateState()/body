{
  if (getIoState() == IoState.IDLE) {
    if (getConnection().isPipelining()) {
      setIoState(IoState.INTEREST);
    }
 else     if (getMessages().isEmpty() && (getConnection().getOutboundWay().getMessages().isEmpty())) {
      setIoState(IoState.INTEREST);
    }
  }
}
