def __getitem__(self, i):
    if (not (0 <= i < self.max)):
        raise IndexError
    n = len(self.sofar)
    while (n <= i):
        self.sofar.append(str((n * n)))
        n = (n + 1)
    return self.sofar[i]
