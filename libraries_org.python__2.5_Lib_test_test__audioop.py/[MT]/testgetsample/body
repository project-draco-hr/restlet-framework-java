def testgetsample(data):
    if verbose:
        print 'getsample'
    for i in range(3):
        if ((audioop.getsample(data[0], 1, i) != i) or (audioop.getsample(data[1], 2, i) != i) or (audioop.getsample(data[2], 4, i) != i)):
            return 0
    return 1
