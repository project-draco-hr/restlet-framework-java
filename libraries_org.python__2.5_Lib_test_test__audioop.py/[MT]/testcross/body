def testcross(data):
    if verbose:
        print 'cross'
    if ((audioop.cross(data[0], 1) != 0) or (audioop.cross(data[1], 2) != 0) or (audioop.cross(data[2], 4) != 0)):
        return 0
    return 1
