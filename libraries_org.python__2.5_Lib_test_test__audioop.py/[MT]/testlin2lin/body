def testlin2lin(data):
    if verbose:
        print 'lin2lin'
    for d1 in data:
        for d2 in data:
            got = (len(d1) // 3)
            wtd = (len(d2) // 3)
            if (len(audioop.lin2lin(d1, got, wtd)) != len(d2)):
                return 0
    return 1
