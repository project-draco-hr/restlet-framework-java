def test_int_conversion_support(self):


    class Foo(object):

        def __init__(self, x):
            self.x = x

        def __int__(self):
            return self.x
    self.assertEqual('1', ('%d' % Foo(1)))
    self.assertEqual('1', ('%d' % Foo(1L)))
