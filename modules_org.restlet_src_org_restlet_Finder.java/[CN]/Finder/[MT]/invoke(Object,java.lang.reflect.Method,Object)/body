{
  Object result=null;
  if (method != null) {
    try {
      result=method.invoke(target,args);
    }
 catch (    final Exception e) {
      getLogger().log(Level.WARNING,"Couldn't invoke the handle method for \"" + method + "\"",e);
    }
  }
  return result;
}
