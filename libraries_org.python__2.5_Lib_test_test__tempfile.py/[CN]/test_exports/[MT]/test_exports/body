def test_exports(self):
    dict = tempfile.__dict__
    expected = {'NamedTemporaryFile': 1, 'TemporaryFile': 1, 'mkstemp': 1, 'mkdtemp': 1, 'mktemp': 1, 'TMP_MAX': 1, 'gettempprefix': 1, 'gettempdir': 1, 'tempdir': 1, 'template': 1, }
    unexp = []
    for key in dict:
        if ((key[0] != '_') and (key not in expected)):
            unexp.append(key)
    self.failUnless((len(unexp) == 0), ('unexpected keys: %s' % unexp))
