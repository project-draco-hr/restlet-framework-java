def test_directory_exists(self):
    dir = tempfile.gettempdir()
    self.assert_((os.path.isabs(dir) or (dir == os.curdir)), ('%s is not an absolute path' % dir))
    self.assert_(os.path.isdir(dir), ('%s is not a directory' % dir))
