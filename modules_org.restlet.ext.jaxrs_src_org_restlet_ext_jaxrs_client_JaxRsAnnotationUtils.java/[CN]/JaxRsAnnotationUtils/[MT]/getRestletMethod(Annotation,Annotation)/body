{
  Method restletMethod=Method.valueOf(annotation.annotationType().getSimpleName());
  if (restletMethod == null) {
    super.getRestletMethod(annotation,methodAnnotation);
  }
  return restletMethod;
}
