def load_long(self):
    size = self.r_long()
    sign = 1
    if (size < 0):
        sign = (-1)
        size = (- size)
    x = 0L
    for i in range(size):
        d = self.r_short()
        x = (x | (d << (i * 15L)))
    return (x * sign)
