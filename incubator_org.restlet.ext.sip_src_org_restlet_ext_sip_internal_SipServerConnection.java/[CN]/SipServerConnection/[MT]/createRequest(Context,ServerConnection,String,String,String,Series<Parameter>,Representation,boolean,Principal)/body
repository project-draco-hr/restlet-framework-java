{
  SipRequest request=new SipRequest(getHelper().getContext(),this,SipMethod.valueOf(methodName),resourceUri,version,headers,createInboundEntity(headers),false,null);
  request.setRecipientsInfo(new ArrayList<RecipientInfo>());
  return request;
}
