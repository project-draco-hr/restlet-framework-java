def test_big_queue_popleft(self):
    pass
    d = deque()
    (append, pop) = (d.append, d.popleft)
    for i in xrange(BIG):
        append(i)
    for i in xrange(BIG):
        x = pop()
        if (x != i):
            self.assertEqual(x, i)
