def testBasic(self):
    x = 2
    x += 1
    x *= 2
    x **= 2
    x -= 8
    x //= 5
    x %= 3
    x &= 2
    x |= 5
    x ^= 1
    x /= 2
    if ((1 / 2) == 0):
        self.assertEquals(x, 3)
    else:
        self.assertEquals(x, 3.0)
