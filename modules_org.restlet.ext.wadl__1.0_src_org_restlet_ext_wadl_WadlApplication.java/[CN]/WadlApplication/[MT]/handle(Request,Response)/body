{
  if (isAutoDescribed() && Method.OPTIONS.equals(request.getMethod()) && request.getResourceRef().getIdentifier().endsWith("*")) {
    response.setEntity(wadlRepresent(request));
  }
 else {
    super.handle(request,response);
  }
}
