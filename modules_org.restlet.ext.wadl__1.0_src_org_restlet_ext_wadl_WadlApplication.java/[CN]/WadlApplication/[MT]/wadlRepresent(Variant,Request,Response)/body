{
  Representation result=null;
  if (MediaType.APPLICATION_WADL_XML.equals(variant.getMediaType())) {
    result=new WadlRepresentation(getApplicationInfo(request,response));
  }
 else   if (MediaType.TEXT_HTML.equals(variant.getMediaType())) {
    result=new WadlRepresentation(getApplicationInfo(request,response)).getHtmlRepresentation();
  }
  return result;
}
