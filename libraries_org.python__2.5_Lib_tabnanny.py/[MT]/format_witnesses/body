def format_witnesses(w):
    firsts = map((lambda tup: str(tup[0])), w)
    prefix = 'at tab size'
    if (len(w) > 1):
        prefix = (prefix + 's')
    return ((prefix + ' ') + ', '.join(firsts))
