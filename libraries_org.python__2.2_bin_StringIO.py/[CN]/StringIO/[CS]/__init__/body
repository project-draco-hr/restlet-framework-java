def __init__(self, buf=''):
    if (type(buf) not in types.StringTypes):
        buf = str(buf)
    self.buf = buf
    self.len = len(buf)
    self.buflist = []
    self.pos = 0
    self.closed = 0
    self.softspace = 0
