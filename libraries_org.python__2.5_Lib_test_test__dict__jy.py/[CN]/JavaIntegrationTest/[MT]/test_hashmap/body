def test_hashmap(self):
    x = java.util.HashMap()
    x.put('a', 1)
    x.put('b', 2)
    x.put('c', 3)
    x.put((1, 2), 'xyz')
    y = dict(x)
    self.assertEqual(set(y.items()), set([('a', 1), ('b', 2), ('c', 3), ((1, 2), 'xyz')]))
