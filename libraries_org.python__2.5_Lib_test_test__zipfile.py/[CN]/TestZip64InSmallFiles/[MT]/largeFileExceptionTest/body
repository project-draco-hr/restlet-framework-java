def largeFileExceptionTest(self, f, compression):
    zipfp = zipfile.ZipFile(f, 'w', compression)
    self.assertRaises(zipfile.LargeZipFile, zipfp.write, TESTFN, (('another' + os.extsep) + 'name'))
    zipfp.close()
