def largeFileExceptionTest2(self, f, compression):
    zipfp = zipfile.ZipFile(f, 'w', compression)
    self.assertRaises(zipfile.LargeZipFile, zipfp.writestr, (('another' + os.extsep) + 'name'), self.data)
    zipfp.close()
