def check_ok(x, x_str):
    assert (x > 0.0)
    x2 = eval(x_str)
    assert (x2 > 0.0)
    diff = abs((x - x2))
    if ((x2 + (diff / 8.0)) != x2):
        raise TestFailed(('Manifest const %s lost too much precision ' % x_str))
