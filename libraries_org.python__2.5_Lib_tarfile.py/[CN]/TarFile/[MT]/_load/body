def _load(self):
    'Read through the entire archive file and look for readable\n           members.\n        '
    while True:
        tarinfo = self.next()
        if (tarinfo is None):
            break
    self._loaded = True
