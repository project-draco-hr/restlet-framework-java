def find(self, offset):
    idx = self.idx
    while True:
        item = self[idx]
        if (offset in item):
            break
        idx += 1
        if (idx == len(self)):
            idx = 0
        if (idx == self.idx):
            return None
    self.idx = idx
    return item
