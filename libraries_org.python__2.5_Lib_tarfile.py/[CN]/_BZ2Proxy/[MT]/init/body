def init(self):
    import bz2
    self.pos = 0
    if (self.mode == 'r'):
        self.bz2obj = bz2.BZ2Decompressor()
        self.fileobj.seek(0)
        self.buf = ''
    else:
        self.bz2obj = bz2.BZ2Compressor()
