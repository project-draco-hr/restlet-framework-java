def readlines(self):
    'Return a list with all remaining lines.\n        '
    result = []
    while True:
        line = self.readline()
        if (not line):
            break
        result.append(line)
    return result
