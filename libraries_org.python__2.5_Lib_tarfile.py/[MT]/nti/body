def nti(s):
    'Convert a number field to a python number.\n    '
    if (s[0] != chr(128)):
        n = int((nts(s) or '0'), 8)
    else:
        n = 0L
        for i in xrange((len(s) - 1)):
            n <<= 8
            n += ord(s[(i + 1)])
    return n
