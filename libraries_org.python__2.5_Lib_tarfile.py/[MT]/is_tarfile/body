def is_tarfile(name):
    'Return True if name points to a tar archive that we\n       are able to handle, else return False.\n    '
    try:
        t = open(name)
        t.close()
        return True
    except TarError:
        return False
