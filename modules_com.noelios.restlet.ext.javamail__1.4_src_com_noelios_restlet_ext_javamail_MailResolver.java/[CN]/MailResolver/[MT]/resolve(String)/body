{
  String result=null;
  if ("mailId".equals(name)) {
    result=identifier;
  }
 else {
    Object value=this.map.get(name);
    result=(value == null) ? null : value.toString();
  }
  return result;
}
