def versionedfiles(dir):
    (out, _) = popen2.popen2(('%s status -v %s' % (CMD, dir.replace(' ', '\\ '))))
    items = [line[3:].split(None, 3)[(-1)][:(-1)] for line in out if (not line.startswith('?'))]
    return [i for i in items if os.path.isfile(i)]
