def setprop(prop, value, files):
    cmd = ([CMD, 'propset', prop, value] + files)
    st = os.spawnvp(os.P_WAIT, CMD, cmd)
    if st:
        print ('Command "%s" failed with exit status %s' % (' '.join(cmd), st))
        sys.exit(1)
