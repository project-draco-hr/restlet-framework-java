def test_raise_reraise(self):

    def f(p):
        try:
            (1 / 0)
        except:
            raise
    f_ident = ident(f)
    self.check_events(f, [(1, 'call', f_ident), (1, 'return', f_ident)])
