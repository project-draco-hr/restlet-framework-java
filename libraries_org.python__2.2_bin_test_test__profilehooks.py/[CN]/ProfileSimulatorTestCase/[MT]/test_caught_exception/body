def test_caught_exception(self):

    def f(p):
        try:
            (1 / 0)
        except:
            pass
    f_ident = ident(f)
    self.check_events(f, [(1, 'call', f_ident), (1, 'return', f_ident)])
