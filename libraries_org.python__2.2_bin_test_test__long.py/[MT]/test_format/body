def test_format(maxdigits=MAXDIGITS):
    if verbose:
        print 'long str/hex/oct/atol'
    for x in special:
        test_format_1(x)
    for i in range(10):
        for lenx in range(1, (maxdigits + 1)):
            x = getran(lenx)
            test_format_1(x)
