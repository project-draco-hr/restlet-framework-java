def testRaisedStopIteration3(self):

    @contextmanager
    def cm():
        yield

    def shouldThrow():
        with cm():
            raise iter([]).next()
    self.assertRaises(StopIteration, shouldThrow)
