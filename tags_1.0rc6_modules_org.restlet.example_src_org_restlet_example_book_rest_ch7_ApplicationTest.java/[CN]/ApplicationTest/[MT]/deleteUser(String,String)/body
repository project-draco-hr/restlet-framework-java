{
  Request request=getAuthenticatedRequest(Method.DELETE,getUserUri(userName),userName,password);
  Response resp=new Client(Protocol.HTTP).handle(request);
  System.out.println(resp.getStatus() + " : " + resp.getRedirectRef());
}
