{
  if (!AuthenticatorUtils.anyNull(challengeResponse.getIdentifier(),password,challengeResponse.getRealm())) {
    return DigestUtils.toHttpDigest(challengeResponse.getIdentifier(),password.toCharArray(),challengeResponse.getRealm()).toCharArray();
  }
  return null;
}
