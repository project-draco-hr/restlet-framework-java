{
  if (!AuthenticatorUtils.anyNull(challengeResponse.getIdentifier(),challengeResponse.getPassword(),challengeResponse.getRealm())) {
    return DigestUtils.toHttpDigest(challengeResponse.getIdentifier(),challengeResponse.getPassword().toCharArray(),challengeResponse.getRealm());
  }
  return null;
}
