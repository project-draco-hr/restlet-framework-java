{
  if (apiErrorAnnotation == null)   return null;
  DocumentationError error=new DocumentationError();
  error.setCode(apiErrorAnnotation.code());
  error.setReason(apiErrorAnnotation.reason());
  return error;
}
