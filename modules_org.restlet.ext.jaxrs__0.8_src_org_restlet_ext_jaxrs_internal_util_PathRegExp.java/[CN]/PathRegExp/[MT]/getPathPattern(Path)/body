{
  if (path == null)   throw new IllegalArgumentException("The path must not be null");
  String pathPattern=Util.getPathTemplate(path);
  if (pathPattern.startsWith("/"))   return pathPattern;
  return "/" + pathPattern;
}
