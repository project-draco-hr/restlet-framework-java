{
  try {
    response.setStatus(httpCall.sendRequest(request.isEntityAvailable() ? request.getEntity() : null));
    response.getServerInfo().setAddress(httpCall.getServerAddress());
    readResponseHeaders(httpCall,response);
    response.setEntity(httpCall.getResponseOutput());
  }
 catch (  Exception e) {
    logger.log(Level.INFO,"Exception intercepted",e);
  }
}
