@expat('ExternalEntityRefHandler', guard='not_in_dtd', returns=empty_source)
def resolveEntity(self, name, publicId, baseURI, systemId):
    context = name
    base = self.parser.GetBase()
    return (context, base, systemId, publicId)
