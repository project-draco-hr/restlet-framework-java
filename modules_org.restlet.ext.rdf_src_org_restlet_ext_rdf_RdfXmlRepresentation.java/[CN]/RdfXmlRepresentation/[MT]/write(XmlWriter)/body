{
  if (getGraph() != null) {
    try {
      new RdfXmlWritingContentHandler(getGraph(),xmlWriter);
      xmlWriter.flush();
    }
 catch (    SAXException e) {
      e.printStackTrace();
    }
  }
}
