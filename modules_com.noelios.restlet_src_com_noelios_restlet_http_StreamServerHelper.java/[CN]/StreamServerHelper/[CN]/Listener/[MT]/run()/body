{
  latch.countDown();
  for (; ; ) {
    try {
      SocketChannel client=(SocketChannel)serverSocket.accept();
      if (!handlerService.isShutdown()) {
        handlerService.submit(new ConnectionHandler(helper,client.socket()));
      }
    }
 catch (    ClosedByInterruptException ex) {
      break;
    }
catch (    IOException ex) {
      helper.getLogger().log(Level.WARNING,"Unexpected error while accepting new connection",ex);
    }
  }
}
