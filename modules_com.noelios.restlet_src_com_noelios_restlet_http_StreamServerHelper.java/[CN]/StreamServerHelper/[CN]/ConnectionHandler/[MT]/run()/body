{
  try {
    helper.handle(new StreamServerCall(helper.getServer(),socket.getInputStream(),socket.getOutputStream(),socket));
  }
 catch (  IOException ex) {
    helper.getLogger().log(Level.WARNING,"Unexpected error while handling a call",ex);
  }
}
