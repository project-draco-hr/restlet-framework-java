{
  if (getServer().getAddress() == null) {
    return new InetSocketAddress(getServer().getPort());
  }
 else {
    return new InetSocketAddress(getServer().getAddress(),getServer().getPort());
  }
}
