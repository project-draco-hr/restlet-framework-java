def adaptgetter(name, klass, getter):
    (kind, des) = getter
    if (kind == 1):
        if (des == ''):
            des = ('_%s__%s' % (klass.__name__, name))
        return (lambda obj: getattr(obj, des))
