{
  final Form parameters=(Form)message.getAttributes().get("org.restlet.http.headers");
  final Parameter p=parameters.getFirst(HeaderConstants.HEADER_TRANSFER_ENCODING);
  assertFalse(p == null);
  assertEquals("chunked",p.getValue());
}
