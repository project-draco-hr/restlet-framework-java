{
  if (path.startsWith("/"))   path=path.substring(1);
  if (path.length() > 0) {
    List<JaxRsPathSegment> pathSegments=convertToJaxRsPathSegments(path.split("/"));
    this.pathSegments.addAll(pathSegments);
  }
}
