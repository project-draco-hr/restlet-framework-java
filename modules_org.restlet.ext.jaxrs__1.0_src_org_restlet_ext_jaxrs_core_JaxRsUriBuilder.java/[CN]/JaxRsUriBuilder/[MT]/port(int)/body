{
  if (port > 65535)   throw new IllegalArgumentException("The port must between zero and 65535 or -1 to unset the explizit port");
  if (port < 0)   this.port=null;
 else   this.port=String.valueOf(port);
  return this;
}
