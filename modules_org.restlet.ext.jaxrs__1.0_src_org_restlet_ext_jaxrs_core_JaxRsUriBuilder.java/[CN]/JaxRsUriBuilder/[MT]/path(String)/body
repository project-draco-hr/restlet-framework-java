{
  if (segments == null)   throw new IllegalArgumentException("The segments must not be null");
  for (int i=0; i < segments.length; i++)   segments[i]=Util.encode(segments[i],i,"segment of the path (index started with 0)",this.encode);
  for (  String segment : segments)   addPathSegment(segment);
  return this;
}
