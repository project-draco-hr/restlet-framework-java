{
  if (port == null) {
    this.port=null;
  }
 else   if (port.startsWith("{") && port.endsWith("}")) {
    this.port=port;
  }
 else {
    Integer.parseInt(port);
    this.port=port;
  }
  return this;
}
