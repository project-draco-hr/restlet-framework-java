{
  if (startIndex < 0)   throw new IndexOutOfBoundsException("The startIndex must be greater or equal zero");
  if (startIndex > array.length)   throw new IndexOutOfBoundsException("The startIndex must not be greater than the array length");
  this.array=array;
  this.counter=startIndex;
}
