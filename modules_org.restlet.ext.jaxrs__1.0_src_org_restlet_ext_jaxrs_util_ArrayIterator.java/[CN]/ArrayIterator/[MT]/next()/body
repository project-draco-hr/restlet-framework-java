{
  if (counter >= array.length)   throw new NoSuchElementException("There are only " + array.length + " elements");
  this.lastReturnedIndex=counter;
  E obj=array[counter];
  counter++;
  return obj;
}
