{
  ChannelBuffer buf=dynamicBuffer();
  this.response.setContent(buf);
  OutputStream stream=new ChannelBufferOutputStream(response.getContent());
  if (isResponseChunked()) {
    return new ChunkedOutputStream(stream);
  }
 else {
    return stream;
  }
}
