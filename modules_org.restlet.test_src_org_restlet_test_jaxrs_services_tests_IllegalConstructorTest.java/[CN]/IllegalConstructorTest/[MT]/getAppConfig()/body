{
  final Application appConfig=new Application(){
    @Override @SuppressWarnings("unchecked") public Set<Class<?>> getClasses(){
      return Util.createSet(ParamConstructorProvider.class,EntityConstructorProvider.class,getRootResourceClass());
    }
  }
;
  return appConfig;
}
