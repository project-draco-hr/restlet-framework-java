{
  if (Util.isNotEmpty(this.encodings) || Util.isNotEmpty(this.languages) || Util.isNotEmpty(this.mediaTypes))   buildVariants();
  List<Variant> variants=this.variants;
  this.variants=null;
  return variants;
}
