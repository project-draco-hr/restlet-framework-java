{
  if (schemeIndex != -1) {
    this.uri=scheme + this.uri.substring(schemeIndex);
  }
 else {
    if (this.uri == null) {
      this.uri=scheme + ':';
    }
 else {
      this.uri=scheme + ':' + this.uri;
    }
  }
  updateIndexes();
}
