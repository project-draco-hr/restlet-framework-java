{
  if (queryIndex != -1) {
    if (fragmentIndex != -1) {
      return this.uri.substring(queryIndex + 1,fragmentIndex);
    }
 else {
      return this.uri.substring(queryIndex + 1);
    }
  }
 else {
    return null;
  }
}
