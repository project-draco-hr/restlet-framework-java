def _get_containing_entref(node):
    c = node.parentNode
    while (c is not None):
        if (c.nodeType == Node.ENTITY_REFERENCE_NODE):
            return c
        c = c.parentNode
    return None
