def interface_test_driver(self, results):
    for (test_name, (given, expected)) in zip(self.interface_tests, results):
        self.failUnlessEqual(given, expected, ('%s: %s != %s' % (test_name, given, expected)))
