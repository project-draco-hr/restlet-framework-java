def test_interface_no_arg(self):
    exc = Exception()
    results = ([len(exc.args), 0], [exc.args, tuple()], [exc.message, ''], [str(exc), ''], [unicode(exc), u''], [repr(exc), (exc.__class__.__name__ + '()')], [True, True])
    self.interface_test_driver(results)
