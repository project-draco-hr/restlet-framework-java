{
  if (template == null)   return new UriTemplateRegExp("",true);
  String pathTemplate=template.value();
  if (ensureStartSlash)   pathTemplate=Util.ensureStartSlash(pathTemplate);
  return new UriTemplateRegExp(pathTemplate,template.limited());
}
