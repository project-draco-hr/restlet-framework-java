{
  String pathTemplate=path.value();
  if (path.encode())   return Util.encodeNotBraces(pathTemplate,false);
  Util.checkForInvalidUriChars(pathTemplate,-1,"path template");
  return pathTemplate;
}
