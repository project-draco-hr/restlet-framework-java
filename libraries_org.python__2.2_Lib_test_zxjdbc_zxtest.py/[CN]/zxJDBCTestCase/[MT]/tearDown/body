def tearDown(self):
    c = self.cursor()
    try:
        try:
            c.execute('drop table zxtesting')
        except:
            self.db.rollback()
    finally:
        c.close()
    zxCoreTestCase.tearDown(self)
