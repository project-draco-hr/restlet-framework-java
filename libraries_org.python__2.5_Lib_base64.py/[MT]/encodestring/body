def encodestring(s):
    'Encode a string.'
    pieces = []
    for i in range(0, len(s), MAXBINSIZE):
        chunk = s[i:(i + MAXBINSIZE)]
        pieces.append(binascii.b2a_base64(chunk))
    return ''.join(pieces)
