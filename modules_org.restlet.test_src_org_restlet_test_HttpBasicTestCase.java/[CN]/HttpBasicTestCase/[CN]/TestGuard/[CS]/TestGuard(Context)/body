{
  super(context,ChallengeScheme.HTTP_BASIC,HttpBasicTestCase.class.getSimpleName());
  getSecrets().put(SHORT_USERNAME,SHORT_PASSWORD.toCharArray());
  getSecrets().put(LONG_USERNAME,LONG_PASSWORD.toCharArray());
}
