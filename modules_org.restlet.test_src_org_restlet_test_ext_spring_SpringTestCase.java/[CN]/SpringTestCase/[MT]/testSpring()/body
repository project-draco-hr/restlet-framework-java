{
  final ClassPathResource resource=new ClassPathResource("org/restlet/test/ext/spring/SpringTestCase.xml");
  final BeanFactory factory=new XmlBeanFactory(resource);
  final Component component=(Component)factory.getBean("component");
  component.start();
  Thread.sleep(500);
  component.stop();
}
