def testReplaceChildFragment():
    (dom, orig, c1, c2, c3, frag) = _create_fragment_test_nodes()
    dom.documentElement.replaceChild(frag, orig)
    orig.unlink()
    confirm((tuple(dom.documentElement.childNodes) == (c1, c2, c3)), 'replaceChild(<fragment>)')
    frag.unlink()
    dom.unlink()
