{
  if (producedMimes == null) {
    ProduceMime pm=writer.getClass().getAnnotation(ProduceMime.class);
    if (pm != null)     this.producedMimes=ResourceMethod.convertToMediaTypes(pm.value());
 else     this.producedMimes=Collections.singletonList(MediaType.ALL);
  }
  return producedMimes;
}
