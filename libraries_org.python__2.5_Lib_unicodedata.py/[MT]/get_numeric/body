def get_numeric(col):
    try:
        return float(col)
    except ValueError:
        try:
            (a, b) = col.split('/')
            return (float(a) / float(b))
        except:
            return None
