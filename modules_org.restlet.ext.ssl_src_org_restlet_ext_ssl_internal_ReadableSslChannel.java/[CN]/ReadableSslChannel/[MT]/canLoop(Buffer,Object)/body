{
  return getConnection().getInboundWay().canLoop(buffer,args) && (getConnection().getSslState() != SslState.END) && ((getConnection().getSslEngineStatus() == Status.OK) || (getConnection().getSslEngineStatus() == Status.BUFFER_UNDERFLOW));
}
