{
  return ((lastRead > 0) || ((getManager().getState() == SslState.HANDSHAKING) && (getManager().getEngineStatus() == Status.OK) && (getManager().getHandshakeStatus() == HandshakeStatus.NEED_UNWRAP))) && targetBuffer.hasRemaining();
}
