{
  return ((lastRead > 0) || ((getConnection().getSslState() == SslState.HANDSHAKING) && (getConnection().getSslEngineStatus() == Status.OK) && (getConnection().getSslHandshakeStatus() == HandshakeStatus.NEED_UNWRAP))) && targetBuffer.hasRemaining();
}
