{
  return ((lastRead > 0) || ((getConnection().getSslState() == SslState.HANDSHAKE) && (getConnection().getSslEngineStatus() == Status.OK) && (getConnection().getSslHandshakeStatus() == HandshakeStatus.NEED_UNWRAP))) && targetBuffer.hasRemaining();
}
