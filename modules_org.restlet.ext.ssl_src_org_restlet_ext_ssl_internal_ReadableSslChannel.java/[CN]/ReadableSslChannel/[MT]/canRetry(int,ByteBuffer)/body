{
  return ((lastRead > 0) || (getConnection().isSslHandshaking() && (getConnection().getSslEngineStatus() == Status.OK) && (getConnection().getSslHandshakeStatus() == HandshakeStatus.NEED_UNWRAP))) && targetBuffer.hasRemaining();
}
