def __repr__(self):
    L = []
    items = self.items()
    items.sort()
    for (K, V) in items:
        L.append(('%s=%s' % (K, repr(V.value))))
    return ('<%s: %s>' % (self.__class__.__name__, string.join(L)))
