def _quote(str, LegalChars=_LegalChars, join=string.join, idmap=string._idmap, translate=string.translate):
    if ('' == translate(str, idmap, LegalChars)):
        return str
    else:
        return (('"' + join(map(_Translator.get, str, str), '')) + '"')
