{
  if (properties == null)   properties=new LinkedHashMap<String,Property>();
  if (required != null) {
    for (    String ek : required) {
      if (key.equals(ek))       property.setRequired(true);
    }
  }
  properties.put(key,property);
}
