{
  float result=-1.0F;
  if (source instanceof SuperCsvRepresentation<?>) {
    result=1.0F;
  }
 else   if ((target != null) && SuperCsvRepresentation.class.isAssignableFrom(target)) {
    result=1.0F;
  }
 else   if (VARIANT_CSV.isCompatible(source)) {
    result=0.8F;
  }
  return result;
}
