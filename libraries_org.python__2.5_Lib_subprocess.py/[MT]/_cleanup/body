def _cleanup():
    for inst in _active[:]:
        if (inst.poll(_deadstate=sys.maxint) >= 0):
            try:
                _active.remove(inst)
            except ValueError:
                pass
