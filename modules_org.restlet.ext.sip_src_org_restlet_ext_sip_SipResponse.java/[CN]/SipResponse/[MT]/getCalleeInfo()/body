{
  List<Address> ci=this.calleeInfo;
  if (ci == null) {
synchronized (this) {
      ci=this.calleeInfo;
      if (ci == null) {
        this.calleeInfo=ci=new CopyOnWriteArrayList<Address>();
      }
    }
  }
  return ci;
}
