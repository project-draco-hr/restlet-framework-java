{
  getLogger().info("Starting the Restlet NIO " + getProtocols() + " client");
  SslContextFactory factory=SslUtils.getSslContextFactory(this);
  setSslContext(factory.createSslContext());
  super.start();
}
