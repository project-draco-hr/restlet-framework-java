def test_load_closed_file(self):
    import os
    f = open(TESTFN, 'w')
    try:
        f.close()
        self.assertRaises(ValueError, self.module.dump, 123, f)
    finally:
        os.remove(TESTFN)
