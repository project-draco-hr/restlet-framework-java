def time(self):
    'Return Unixish timestamp, as a float (assuming UTC).'
    days = (self.toordinal() - _ORD1970)
    seconds = (((((days * 24.0) + self.hour) * 60.0) + self.minute) * 60.0)
    return ((seconds + self.second) + (self.microsecond / 1000000.0))
