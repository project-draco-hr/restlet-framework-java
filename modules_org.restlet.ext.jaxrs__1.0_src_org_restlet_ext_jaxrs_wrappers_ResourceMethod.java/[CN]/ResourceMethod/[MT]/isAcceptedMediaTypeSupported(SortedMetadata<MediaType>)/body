{
  if (accMediaTypess == null || accMediaTypess.isEmpty())   return true;
  List<MediaType> prodMimes=getProducedMimes();
  if (prodMimes.isEmpty())   return true;
  if (true)   throw new NotYetImplementedException("sorry, check in to early");
  for (  MediaType producedMediaType : prodMimes) {
    for (    MediaType accMediaType : accMediaTypess)     return true;
  }
  return false;
}
