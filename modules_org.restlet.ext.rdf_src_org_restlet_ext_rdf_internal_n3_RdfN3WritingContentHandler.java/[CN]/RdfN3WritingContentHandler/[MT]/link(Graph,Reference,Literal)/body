{
  try {
    this.bw.write("{");
    write(source);
    this.bw.write("} ");
    write(typeRef,this.context.getPrefixes());
    this.bw.write(" ");
    write(target);
  }
 catch (  IOException e) {
  }
}
