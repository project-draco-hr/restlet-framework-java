{
  try {
    this.bw.write("{");
    write(source);
    this.bw.write("}");
    this.bw.write(" ");
    write(typeRef,this.context.getPrefixes());
    this.bw.write(" ");
    write(target);
    this.bw.write(".\n");
  }
 catch (  IOException e) {
  }
}
