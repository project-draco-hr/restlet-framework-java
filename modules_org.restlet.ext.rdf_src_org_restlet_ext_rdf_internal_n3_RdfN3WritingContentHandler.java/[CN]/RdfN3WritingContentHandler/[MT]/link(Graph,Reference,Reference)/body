{
  try {
    this.bw.write("{");
    write(source);
    this.bw.write("}");
    this.bw.write(" ");
    write(typeRef,this.context.getPrefixes());
    this.bw.write(" ");
    write(target,this.context.getPrefixes());
    this.bw.write(".\n");
  }
 catch (  IOException e) {
  }
}
