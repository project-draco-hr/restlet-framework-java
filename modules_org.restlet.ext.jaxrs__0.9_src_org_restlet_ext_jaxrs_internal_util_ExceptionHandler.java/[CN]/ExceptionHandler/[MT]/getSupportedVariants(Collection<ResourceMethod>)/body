{
  Set<Variant> supportedVariants=new HashSet<Variant>();
  for (  ResourceMethod resourceMethod : supporting)   supportedVariants.addAll(resourceMethod.getSupportedVariants());
  return supportedVariants;
}
