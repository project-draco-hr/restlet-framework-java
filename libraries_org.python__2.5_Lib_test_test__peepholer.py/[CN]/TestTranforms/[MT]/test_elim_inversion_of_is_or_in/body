def test_elim_inversion_of_is_or_in(self):
    for (line, elem) in (('not a is b', '(is not)'), ('not a in b', '(not in)'), ('not a is not b', '(is)'), ('not a not in b', '(in)')):
        asm = dis_single(line)
        self.assert_((elem in asm))
