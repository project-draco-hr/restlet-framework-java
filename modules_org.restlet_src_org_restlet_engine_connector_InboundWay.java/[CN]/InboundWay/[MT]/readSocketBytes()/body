{
  int result=getConnection().getReadableSelectionChannel().read(getByteBuffer());
  getLogger().fine("Number of bytes read: " + result);
  if (result > 0) {
    getByteBuffer().flip();
    setByteBufferState(BufferState.DRAINING);
  }
  return result;
}
