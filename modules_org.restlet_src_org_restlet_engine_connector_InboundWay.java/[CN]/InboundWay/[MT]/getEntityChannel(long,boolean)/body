{
  ReadableSelectionChannel result=null;
  if (chunked) {
    result=new ReadableChunkedChannel(this,getByteBuffer(),getConnection().getReadableSelectionChannel());
  }
 else {
    result=new ReadableSizedChannel(this,getByteBuffer(),getConnection().getReadableSelectionChannel(),size);
  }
  return result;
}
