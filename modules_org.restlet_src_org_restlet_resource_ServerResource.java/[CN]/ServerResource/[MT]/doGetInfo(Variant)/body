{
  RepresentationInfo result=null;
  if (variant instanceof AnnotatedVariant) {
    result=doHandle(((AnnotatedVariant)variant).getAnnotationInfo(),variant);
  }
 else {
    result=getInfo(variant);
  }
  return result;
}
