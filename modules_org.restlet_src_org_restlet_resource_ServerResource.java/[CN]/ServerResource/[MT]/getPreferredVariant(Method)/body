{
  Variant result=null;
  List<Variant> variants=getAvailableVariants(method);
  if ((variants != null) && (!variants.isEmpty())) {
    result=getClientInfo().getPreferredVariant(variants,(getApplication() == null) ? null : getApplication().getMetadataService());
  }
  return result;
}
