{
  super(getReferrer(connection),getUserAgentName(connection),getMediaPrefs(connection),getCharacterSetPrefs(connection),getLanguagePrefs(connection),getMethod(connection),getResource(connection),getCookies(connection),getInput(connection));
  setClientAddress(connection.getRequest().getRemoteAddr());
}
