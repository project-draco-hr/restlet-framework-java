{
  Swagger swagger=new Swagger();
  swagger.setSwagger(SWAGGER_2_0_VERSION);
  fillMainAttributes(definition,swagger);
  fillInfo(definition,swagger);
  fillPaths(definition,swagger);
  fillDefinitions(definition,swagger);
  return swagger;
}
