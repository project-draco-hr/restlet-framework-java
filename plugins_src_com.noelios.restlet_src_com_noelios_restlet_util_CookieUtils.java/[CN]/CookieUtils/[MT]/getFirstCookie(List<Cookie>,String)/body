{
  Cookie result=null;
  Cookie cookie;
  for (Iterator<Cookie> iter=source.iterator(); (result == null) && iter.hasNext(); ) {
    cookie=iter.next();
    if (name.equals(cookie.getName())) {
      result=cookie;
    }
  }
  return result;
}
