def next(self):
    while self.readline():
        pass
    if ((self.level > 1) or self.last):
        return 0
    self.level = 0
    self.last = 0
    if self.seekable:
        self.start = self.fp.tell()
    return 1
