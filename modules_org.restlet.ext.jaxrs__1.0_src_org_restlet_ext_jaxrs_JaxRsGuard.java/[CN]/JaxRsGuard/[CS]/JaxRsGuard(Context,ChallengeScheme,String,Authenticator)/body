{
  super(context,scheme,realm);
  if (authenticator == null)   throw new IllegalArgumentException("The Authenticator must not be null. You can use the " + AllowAllAuthenticator.class.getName() + " or the "+ ForbidAllAuthenticator.class.getName());
  this.authenticator=authenticator;
}
