{
  super(context,scheme,realm);
  if (authorizator == null)   throw new IllegalArgumentException("The Authorizator must not be null. You can use the " + AllowAllAuthorizator.class.getName() + " or the "+ ForbidAllAuthorizator.class.getName());
  this.authorizator=authorizator;
}
