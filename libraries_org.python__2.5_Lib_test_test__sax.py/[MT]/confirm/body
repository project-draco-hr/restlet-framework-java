def confirm(outcome, name):
    global tests
    tests = (tests + 1)
    if outcome:
        if verbose:
            print 'Passed', name
    else:
        print 'Failed', name
        failures.append(name)
