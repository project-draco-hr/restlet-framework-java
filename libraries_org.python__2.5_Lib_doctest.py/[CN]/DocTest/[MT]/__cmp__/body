def __cmp__(self, other):
    if (not isinstance(other, DocTest)):
        return (-1)
    return cmp((self.name, self.filename, self.lineno, id(self)), (other.name, other.filename, other.lineno, id(other)))
