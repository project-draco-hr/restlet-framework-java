def check_basic_ref(self, factory):
    o = factory()
    ref = weakref.ref(o)
    self.assert_((ref() is not None), 'weak reference to live object should be live')
    o2 = ref()
    self.assert_((o is o2), '<ref>() should return original object if live')
