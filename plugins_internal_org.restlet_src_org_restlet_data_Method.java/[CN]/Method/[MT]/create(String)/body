{
  Method result=null;
  if (methodName != null) {
    if (methodName.equalsIgnoreCase(GET.getName()))     result=GET;
 else     if (methodName.equalsIgnoreCase(POST.getName()))     result=POST;
 else     if (methodName.equalsIgnoreCase(HEAD.getName()))     result=HEAD;
 else     if (methodName.equalsIgnoreCase(OPTIONS.getName()))     result=OPTIONS;
 else     if (methodName.equalsIgnoreCase(PUT.getName()))     result=PUT;
 else     if (methodName.equalsIgnoreCase(DELETE.getName()))     result=DELETE;
 else     if (methodName.equalsIgnoreCase(CONNECT.getName()))     result=CONNECT;
 else     if (methodName.equalsIgnoreCase(COPY.getName()))     result=COPY;
 else     if (methodName.equalsIgnoreCase(LOCK.getName()))     result=LOCK;
 else     if (methodName.equalsIgnoreCase(MKCOL.getName()))     result=MKCOL;
 else     if (methodName.equalsIgnoreCase(MOVE.getName()))     result=MOVE;
 else     if (methodName.equalsIgnoreCase(PROPFIND.getName()))     result=PROPFIND;
 else     if (methodName.equalsIgnoreCase(PROPPATCH.getName()))     result=PROPPATCH;
 else     if (methodName.equalsIgnoreCase(TRACE.getName()))     result=TRACE;
 else     if (methodName.equalsIgnoreCase(UNLOCK.getName()))     result=UNLOCK;
 else     result=new Method(methodName);
  }
  return result;
}
