def test_raise_no_arg(self):
    r = None
    try:
        try:
            raise RuntimeError('dummy')
        except RuntimeError:
            raise
    except RuntimeError as e:
        r = str(e)
    self.assertEquals(r, 'dummy')
