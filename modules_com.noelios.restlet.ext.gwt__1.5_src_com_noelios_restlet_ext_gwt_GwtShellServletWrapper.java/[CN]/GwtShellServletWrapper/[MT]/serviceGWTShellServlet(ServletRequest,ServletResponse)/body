{
  try {
    this.gwtShellServletServiceMethod.invoke(this.gwtShellServlet,request,response);
  }
 catch (  final IllegalAccessException x) {
    log("[Noelios Restlet Engine] - Unable to call service(request,response) in GWTShellServlet",x);
  }
catch (  final InvocationTargetException x) {
    log("[Noelios Restlet Engine] - Unable to call service(request,response) in GWTShellServlet",x);
  }
}
