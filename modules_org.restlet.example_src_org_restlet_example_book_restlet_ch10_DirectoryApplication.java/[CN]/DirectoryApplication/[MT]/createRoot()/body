{
  Directory directory=new Directory(getContext(),LocalReference.createFileReference("d:\\temp"));
  directory.setNegotiateContent(true);
  return new TweakingClientFilter(getContext(),directory);
}
