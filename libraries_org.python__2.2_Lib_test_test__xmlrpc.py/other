import sys
import test_support
import unittest
import xmlrpclib
alist = [{'astring': 'foo@bar.baz.spam', 'afloat': 7283.43, 'anint': (2 ** 20), 'ashortlong': 2L, 'anotherlist': ['.zyx.41'], 'abase64': xmlrpclib.Binary('my dog has fleas'), 'boolean': xmlrpclib.False, }]
if (__name__ == '__main__'):
    test_main()
