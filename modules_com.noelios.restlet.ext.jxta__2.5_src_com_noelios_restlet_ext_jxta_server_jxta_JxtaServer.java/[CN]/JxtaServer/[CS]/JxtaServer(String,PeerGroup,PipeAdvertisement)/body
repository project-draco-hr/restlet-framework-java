{
  this.name=name;
  this.group=group;
  this.pipe=pipe;
  if (logger.isLoggable(Level.FINE)) {
    logger.log(Level.FINE,"instantiated[name][group][pipe]: [" + name + "]["+ group+ "]["+ pipe+ "]");
  }
}
