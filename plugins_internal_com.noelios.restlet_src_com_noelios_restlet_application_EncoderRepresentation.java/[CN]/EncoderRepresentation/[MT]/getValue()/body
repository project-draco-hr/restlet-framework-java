{
  String result=null;
  if (canEncode()) {
    try {
      result=ByteUtils.toString(getStream());
    }
 catch (    IOException ioe) {
    }
  }
 else {
    result=getWrappedRepresentation().getValue();
  }
  return result;
}
