{
  if (decode) {
    if (this.pathSegmentsDecoded == null)     this.pathSegmentsDecoded=createPathSegments(decode);
    return pathSegmentsDecoded;
  }
 else {
    if (this.pathSegmentsEncoded == null)     this.pathSegmentsEncoded=createPathSegments(decode);
    return pathSegmentsEncoded;
  }
}
