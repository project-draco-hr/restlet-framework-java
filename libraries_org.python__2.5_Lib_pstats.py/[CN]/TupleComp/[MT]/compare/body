def compare(self, left, right):
    for (index, direction) in self.comp_select_list:
        l = left[index]
        r = right[index]
        if (l < r):
            return (- direction)
        if (l > r):
            return direction
    return 0
