def func_std_string(func_name):
    if (func_name[:2] == ('~', 0)):
        name = func_name[2]
        if (name.startswith('<') and name.endswith('>')):
            return ('{%s}' % name[1:(-1)])
        else:
            return name
    else:
        return ('%s:%d(%s)' % func_name)
