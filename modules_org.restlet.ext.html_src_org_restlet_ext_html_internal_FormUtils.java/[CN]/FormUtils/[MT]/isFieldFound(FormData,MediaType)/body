{
  boolean result=false;
  for (Iterator<? extends NamedValue> iter=mediaRange.getParameters().iterator(); !result && iter.hasNext(); ) {
    result=searchedField.equals(iter.next());
  }
  return result;
}
