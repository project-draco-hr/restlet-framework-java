{
  boolean result=other != null;
  if (result) {
    result=(getCharacterSet() == null) || getCharacterSet().equals(CharacterSet.ALL) || getCharacterSet().equals(other.getCharacterSet());
  }
  if (result) {
    result=(getMediaType() == null) || getMediaType().isCompatible(other.getMediaType());
  }
  if (result) {
    result=(getLanguages().isEmpty()) || getLanguages().contains(Language.ALL) || getLanguages().containsAll(other.getLanguages());
  }
  return result;
}
