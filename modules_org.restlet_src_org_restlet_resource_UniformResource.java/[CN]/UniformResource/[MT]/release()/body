{
  try {
    doRelease();
  }
 catch (  ResourceException e) {
    if (getResponse() != null) {
      getResponse().setStatus(e.getStatus());
    }
  }
  getRequest().release();
  getResponse().release();
}
