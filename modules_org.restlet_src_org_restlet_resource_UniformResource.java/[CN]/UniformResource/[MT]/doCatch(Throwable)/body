{
  getLogger().log(Level.INFO,"Exception or error caught it resource",throwable);
  if (getResponse() != null && getApplication() != null) {
    getResponse().setStatus(getApplication().getStatusService().getStatus(throwable,this));
  }
}
