{
  try {
    Field field=getDeclaredField(clazz,fieldName);
    if (field != null) {
      return getFieldType(field);
    }
  }
 catch (  Exception ex) {
    logger.log(Level.INFO,"Error when getting type of field " + fieldName + " for class "+ clazz.getCanonicalName(),ex);
  }
  return null;
}
