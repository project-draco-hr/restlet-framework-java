{
  Representation result=null;
  if ((object instanceof Feed) && ((targetVariant == null) || targetVariant.isCompatible(VARIANT_ATOM))) {
    Feed feed=(Feed)object;
    result=feed;
  }
  return result;
}
