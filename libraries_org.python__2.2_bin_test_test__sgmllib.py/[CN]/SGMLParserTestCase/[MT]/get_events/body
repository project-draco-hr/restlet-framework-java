def get_events(self, source):
    parser = self.collector()
    try:
        for s in source:
            parser.feed(s)
        parser.close()
    except:
        raise
    return parser.get_events()
