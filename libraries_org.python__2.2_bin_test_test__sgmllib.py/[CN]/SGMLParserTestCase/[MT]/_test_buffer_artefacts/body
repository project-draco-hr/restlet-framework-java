def _test_buffer_artefacts(self):
    output = [('starttag', 'a', [('b', '<')])]
    self.check_events(["<a b='<'>"], output)
    self.check_events(['<a ', "b='<'>"], output)
    self.check_events(['<a b', "='<'>"], output)
    self.check_events(['<a b=', "'<'>"], output)
    self.check_events(["<a b='<", "'>"], output)
    self.check_events(["<a b='<'", '>'], output)
    output = [('starttag', 'a', [('b', '>')])]
    self.check_events(["<a b='>'>"], output)
    self.check_events(['<a ', "b='>'>"], output)
    self.check_events(['<a b', "='>'>"], output)
    self.check_events(['<a b=', "'>'>"], output)
    self.check_events(["<a b='>", "'>"], output)
    self.check_events(["<a b='>'", '>'], output)
