{
  Series<FormData> result=new Series<FormData>(FormData.class);
  FormData field=readNextField();
  while (field != null) {
    result.add(field);
    field=readNextField();
  }
  this.stream.close();
  return result;
}
