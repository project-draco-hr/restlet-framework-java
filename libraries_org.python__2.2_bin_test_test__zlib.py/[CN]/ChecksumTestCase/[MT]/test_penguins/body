def test_penguins(self):
    self.assertEqual32(zlib.crc32('penguin', 1), 1136044692)
    self.assertEqual32(zlib.adler32('penguin', 1), 198574839)
    self.assertEqual(zlib.adler32('penguin'), zlib.adler32('penguin', 1))
