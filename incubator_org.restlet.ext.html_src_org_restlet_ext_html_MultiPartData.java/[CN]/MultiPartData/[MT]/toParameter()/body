{
  try {
    return new Parameter(getName(),getValue().getText());
  }
 catch (  IOException e) {
    Context.getCurrentLogger().log(Level.WARNING,"Unable to convert to a parameter",e);
    return null;
  }
}
