def __init__(self, module, name, super, file, lineno):
    self.module = module
    self.name = name
    if (super is None):
        super = []
    self.super = super
    self.methods = {}
    self.file = file
    self.lineno = lineno
