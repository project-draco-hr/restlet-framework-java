{
  ApplicationConfig appConfig=new ApplicationConfig(){
    @Override public Set<Class<?>> getResourceClasses(){
      return (Set)Util.createSet(SimpleTrain.class);
    }
  }
;
  JaxRsRouter router=new JaxRsRouter(null,appConfig,AllowAllAuthenticator.getInstance());
  try {
    router.attach(IllegalPathService1.class);
    fail("must not pass");
  }
 catch (  AssertionFailedError e) {
  }
  router.attach(IllegalPathService2.class);
}
