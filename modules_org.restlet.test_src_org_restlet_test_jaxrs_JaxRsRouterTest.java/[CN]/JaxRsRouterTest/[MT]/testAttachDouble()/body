{
  ApplicationConfig appConfig=new ApplicationConfig(){
    @Override public Set<Class<?>> getResourceClasses(){
      return (Set)Util.createSet(DoublePath1.class,DoublePath1.class);
    }
  }
;
  new JaxRsRouter(null,appConfig);
}
