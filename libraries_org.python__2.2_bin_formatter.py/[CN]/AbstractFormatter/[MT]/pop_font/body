def pop_font(self):
    if self.font_stack:
        del self.font_stack[(-1)]
    if self.font_stack:
        font = self.font_stack[(-1)]
    else:
        font = None
    self.writer.new_font(font)
