def format_letter(self, case, counter):
    label = ''
    while (counter > 0):
        (counter, x) = divmod((counter - 1), 26)
        s = chr((ord(case) + x))
        label = (s + label)
    return label
