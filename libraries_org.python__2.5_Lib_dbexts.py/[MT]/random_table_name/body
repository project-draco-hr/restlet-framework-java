def random_table_name(prefix, num_chars):
    import random
    d = [prefix, '_']
    i = 0
    while (i < num_chars):
        d.append(chr(((int((100 * random.random())) % 26) + ord('A'))))
        i += 1
    return ''.join(d)
