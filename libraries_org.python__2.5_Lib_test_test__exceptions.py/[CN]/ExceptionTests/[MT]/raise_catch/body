def raise_catch(self, exc, excname):
    try:
        raise exc, 'spam'
    except exc as err:
        buf1 = str(err)
    try:
        raise exc('spam')
    except exc as err:
        buf2 = str(err)
    self.assertEquals(buf1, buf2)
    self.assertEquals(exc.__name__, excname)
