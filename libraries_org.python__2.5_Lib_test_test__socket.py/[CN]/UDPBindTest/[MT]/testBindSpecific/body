def testBindSpecific(self):
    self.sock.bind((self.HOST, self.PORT))
    actual_port = self.sock.getsockname()[1]
    self.failUnless((actual_port == self.PORT), ('Binding to specific port number should have returned same number: %d != %d' % (actual_port, self.PORT)))
