{
  super(Container.this.mediaType);
  setCharacterSet(Container.this.characterSet);
  if (Container.this.language != null) {
    setLanguages(Arrays.asList(new Language[]{Container.this.language}));
  }
  this.script=script;
}
