{
  setAnnotated(false);
  List<MediaType> mediaTypes=Arrays.asList(new MediaType[]{MediaType.TEXT_HTML,MediaType.TEXT_PLAIN});
  Map<Method,Object> variants=getVariants();
  variants.put(Method.GET,mediaTypes);
  variants.put(Method.POST,mediaTypes);
}
