{
  boolean result=true;
  if (call.getMethod().equals(Method.GET) || call.getMethod().equals(Method.HEAD) || call.getMethod().equals(Method.DELETE)) {
    result=false;
  }
 else {
    result=(call.getInput() != null) && call.getInput().isContentAvailable();
  }
  return result;
}
