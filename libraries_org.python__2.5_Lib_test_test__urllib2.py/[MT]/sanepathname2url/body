def sanepathname2url(path):
    import urllib
    urlpath = urllib.pathname2url(path)
    if (((os._name if test_support.is_jython else os.name) == 'nt') and urlpath.startswith('///')):
        urlpath = urlpath[2:]
    return urlpath
