def test_read_chunks(self):
    CHUNK = 1024
    f = open(test_support.findfile('sgml_input.html'))
    fp = sgmllib.SGMLParser()
    while 1:
        data = f.read(CHUNK)
        fp.feed(data)
        if (len(data) != CHUNK):
            break
