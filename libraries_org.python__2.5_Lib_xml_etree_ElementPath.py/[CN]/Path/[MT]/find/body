def find(self, element):
    tag = self.tag
    if (tag is None):
        nodeset = self.findall(element)
        if (not nodeset):
            return None
        return nodeset[0]
    for elem in element:
        if (elem.tag == tag):
            return elem
    return None
