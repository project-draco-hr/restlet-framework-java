def findtext(self, element, default=None):
    tag = self.tag
    if (tag is None):
        nodeset = self.findall(element)
        if (not nodeset):
            return default
        return (nodeset[0].text or '')
    for elem in element:
        if (elem.tag == tag):
            return (elem.text or '')
    return default
