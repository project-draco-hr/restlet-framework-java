def test_nested_break(self):

    @contextmanager
    def a():
        yield
    state = 0
    while True:
        state += 1
        with nested(a(), a()):
            break
        state += 10
    self.assertEqual(state, 1)
