{
  Form form=new Form();
  if (parameters == null)   return form;
  for (  Map.Entry<String,String> parameter : parameters.entrySet())   form.add(parameter.getKey(),parameter.getValue());
  return form;
}
