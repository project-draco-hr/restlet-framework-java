def msg(self, level, str, *args):
    if (level <= self.debug):
        for i in range(self.indent):
            print '   ',
        print str,
        for arg in args:
            print repr(arg),
        print 
