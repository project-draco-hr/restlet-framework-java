def testIntStrCompares(self):
    assert (not ((-1) > 'a'))
    assert ((-1) < 'a')
    assert (not (4 > 'a'))
    assert (4 < 'a')
    assert (not ((-2) > 'a'))
    assert ((-2) < 'a')
    assert (not ((-1) == 'a'))
