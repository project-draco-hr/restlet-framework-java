def close(self):
    try:
        if self.istream:
            self.istream.close()
        if self.ostream:
            self.ostream.close()
        if self.sock_impl:
            self.sock_impl.close()
    except java.lang.Exception as jlx:
        raise _map_exception(jlx)
