{
  if (logger != null && logLevel == null)   throw new IllegalArgumentException("The logLevel must not be null if a Logger is given");
  this.logger=logger;
  this.logLevel=logLevel;
  this.throwOnExc=throwOnExc;
}
