{
  if (!(key instanceof String))   return null;
  Series<Parameter> subForm=form.subList((String)key,true);
  List<String> list=new ArrayList<String>();
  for (  Parameter parameter : subForm)   list.add(parameter.getName());
  return list;
}
