{
  if (isAvailable()) {
    MessageDigest md=MessageDigest.getInstance(algorithm);
    DigestInputStream dis=new DigestInputStream(getStream(),md);
    ByteUtils.exhaust(dis);
    return new Digest(algorithm,Engine.getInstance().toBase64(md.digest()));
  }
 else {
    return null;
  }
}
