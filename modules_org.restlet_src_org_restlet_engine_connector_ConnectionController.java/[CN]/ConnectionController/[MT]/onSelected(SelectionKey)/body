{
  System.out.println("onSelected: " + key.interestOps() + " | "+ key.readyOps()+ " | "+ key.attachment());
  if (key.attachment() != null) {
    ((SelectionRegistration)key.attachment()).onSelected(key.readyOps());
  }
}
