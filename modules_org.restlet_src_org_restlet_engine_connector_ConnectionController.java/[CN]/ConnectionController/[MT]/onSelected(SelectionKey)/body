{
  if (getHelper().getLogger().isLoggable(Level.INFO)) {
    getHelper().getLogger().log(Level.INFO,"NIO selection: " + key.interestOps() + " | "+ key.readyOps()+ " | "+ key.attachment());
  }
  if (key.attachment() != null) {
    ((SelectionRegistration)key.attachment()).onSelected(key.readyOps());
  }
}
