{
  if (getHelper().getLogger().isLoggable(Level.FINE)) {
    getHelper().getLogger().log(Level.FINE,"NIO selection: " + key.interestOps() + " | "+ key.readyOps()+ " | "+ key.attachment());
  }
  if (key.attachment() != null) {
    ((SelectionRegistration)key.attachment()).onSelected(key.readyOps());
  }
}
