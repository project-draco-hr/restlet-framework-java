{
  for (  SelectionKey key : getSelector().keys()) {
    if ((key.attachment() instanceof SelectionRegistration) && ((SelectionRegistration)key.attachment()).isCanceled()) {
      key.cancel();
    }
  }
}
