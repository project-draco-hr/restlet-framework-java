{
  Series<CookieSetting> c=this.cookieSettings;
  if (c == null) {
synchronized (this) {
      c=this.cookieSettings;
      if (c == null)       this.cookieSettings=c=new CookieSettingSeries();
    }
  }
  return c;
}
