def test_boom():
    a = Boom()
    b = Boom()
    a.attr = b
    b.attr = a
    gc.collect()
    garbagelen = len(gc.garbage)
    del a, b
    expect(gc.collect(), 4, 'boom')
    expect(len(gc.garbage), garbagelen, 'boom')
