def test_boom_new():
    a = Boom_New()
    b = Boom_New()
    a.attr = b
    b.attr = a
    gc.collect()
    garbagelen = len(gc.garbage)
    del a, b
    expect(gc.collect(), 4, 'boom_new')
    expect(len(gc.garbage), garbagelen, 'boom_new')
