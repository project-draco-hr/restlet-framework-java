{
  Handler result=null;
  if ((getTargetClass() != null) && Handler.class.isAssignableFrom(getTargetClass())) {
    result=createTarget((Class<? extends Handler>)getTargetClass(),request,response);
  }
  return result;
}
