{
  String segment=null;
  if (path != null) {
    segment="/";
    for (    String part : path.split("/")) {
      if (part != null && !part.isEmpty()) {
        segment+=part;
        break;
      }
    }
  }
  return segment;
}
