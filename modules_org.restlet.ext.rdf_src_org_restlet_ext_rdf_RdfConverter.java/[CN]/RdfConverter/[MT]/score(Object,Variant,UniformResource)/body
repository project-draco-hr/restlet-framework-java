{
  if (source instanceof Graph) {
    if (VARIANT_RDF_N3.isCompatible(target) || VARIANT_RDF_NTRIPLES.isCompatible(target) || VARIANT_RDF_TURTLE.isCompatible(target)|| VARIANT_RDF_XML.isCompatible(target)) {
      return 1.0f;
    }
  }
  return -1.0f;
}
