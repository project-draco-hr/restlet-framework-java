{
  List<Class<?>> result=null;
  if (VARIANT_RDF_N3.isCompatible(source) || VARIANT_RDF_NTRIPLES.isCompatible(source) || VARIANT_RDF_TURTLE.isCompatible(source)|| VARIANT_RDF_XML.isCompatible(source)) {
    result=addObjectClass(result,Graph.class);
  }
  return result;
}
