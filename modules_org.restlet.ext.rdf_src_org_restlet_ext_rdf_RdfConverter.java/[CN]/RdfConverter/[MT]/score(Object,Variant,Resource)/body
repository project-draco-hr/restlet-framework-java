{
  float result=-1.0F;
  if (source instanceof Graph) {
    if (target == null) {
      result=0.5F;
    }
 else     if (VARIANT_RDF_N3.isCompatible(target) || VARIANT_RDF_NTRIPLES.isCompatible(target) || VARIANT_RDF_TURTLE.isCompatible(target)|| VARIANT_RDF_XML.isCompatible(target)) {
      result=1.0F;
    }
 else {
      result=0.5F;
    }
  }
  return result;
}
