{
  Principal principal=(request.getChallengeResponse() == null) ? null : request.getChallengeResponse().getPrincipal();
  if (this.roleChecker == null)   this.roleChecker=RoleChecker.REJECT_WITH_ERROR;
  return roleChecker.isInRole(principal,role);
}
