{
  if (definition == null) {
synchronized (Swagger2SpecificationRestlet.class) {
      definition=ApplicationIntrospector.getDefinition(application,baseRef,null,introspectorPlugins);
      if (definition.getVersion() == null) {
        definition.setVersion("1.0");
      }
    }
  }
  return definition;
}
