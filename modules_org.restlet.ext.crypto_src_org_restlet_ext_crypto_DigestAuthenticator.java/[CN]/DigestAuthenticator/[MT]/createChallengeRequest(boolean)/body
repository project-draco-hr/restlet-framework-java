{
  ChallengeRequest result=super.createChallengeRequest(stale);
  if (ChallengeScheme.HTTP_DIGEST.equals(getScheme())) {
    HttpDigestHelper.addParameters(result,getDomainUris(),getServerKey(),stale);
  }
  return result;
}
