{
  super(context,request,response);
  provider=(OAuthProvider)context.getAttributes().get("oauth_provider");
  realm=(String)context.getAttributes().get("realm");
  getVariants().add(new Variant(MediaType.TEXT_PLAIN));
}
