def __init__(self, dict=None):
    self.data = {}

    def remove(k, selfref=ref(self)):
        self = selfref()
        if (self is not None):
            del self.data[k]
    self._remove = remove
    if (dict is not None):
        self.update(dict)
