def test_raise_catch(exc):
    try:
        raise exc, 'spam'
    except exc as err:
        buf = str(err)
    try:
        raise exc('spam')
    except exc as err:
        buf = str(err)
    print buf
