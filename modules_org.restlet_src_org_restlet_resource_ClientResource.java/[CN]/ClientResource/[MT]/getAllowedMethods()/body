{
  Set<Method> result=null;
  try {
    head();
    result=getResponse().getAllowedMethods();
  }
 catch (  ResourceException e) {
    getLogger().log(Level.INFO,"Unable to determine the allowed methods. The HEAD call failed.",e);
  }
  return result;
}
