{
  return new Route(this,uriPattern,target){
    @Override protected void beforeHandle(    Request request,    Response response){
      super.beforeHandle(request,response);
      request.setRootRef(request.getResourceRef().getBaseRef());
    }
  }
;
}
