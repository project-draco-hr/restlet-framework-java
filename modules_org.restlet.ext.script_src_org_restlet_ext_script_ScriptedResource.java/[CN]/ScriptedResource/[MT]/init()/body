{
  setAnnotated(false);
  ScriptedResourceContainer container=new ScriptedResourceContainer(this,getVariants());
  try {
    container.invoke(getInitializeResourceEntryPointName());
  }
 catch (  ResourceException e) {
    e.printStackTrace();
  }
}
