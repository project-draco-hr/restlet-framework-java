{
  ResponseInfo responseInfo=new ResponseInfo();
  for (  final Variant variant : getWadlVariants()) {
    responseInfo.getRepresentations().add(getRepresentationInfo(variant));
  }
  methodInfo.setResponse(responseInfo);
}
