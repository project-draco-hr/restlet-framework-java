def seek(self, pos, whence=0):
    if (whence == 0):
        self.pos = (self.start + pos)
    elif (whence == 1):
        self.pos = (self.pos + pos)
    elif (whence == 2):
        self.pos = (self.stop + pos)
