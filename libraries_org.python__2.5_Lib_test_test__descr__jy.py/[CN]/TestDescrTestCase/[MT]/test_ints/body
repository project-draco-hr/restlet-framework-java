def test_ints(self):


    class C(int):
        pass
    try:
        foo = int(None)
    except TypeError:
        pass
    else:
        self.assert_(False, 'should have raised TypeError')
    try:
        foo = C(None)
    except TypeError:
        pass
    else:
        self.assert_(False, 'should have raised TypeError')
