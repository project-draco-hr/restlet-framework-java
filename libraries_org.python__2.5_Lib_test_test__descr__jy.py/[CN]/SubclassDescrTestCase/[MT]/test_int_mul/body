def test_int_mul(self):


    class Foo(tuple):

        def __rmul__(self, other):
            return 'foo'
    foo = Foo()
    self.assertEqual((3.0 * foo), 'foo')
    self.assertEqual((4 * foo), 'foo')
