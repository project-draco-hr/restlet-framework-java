{
  final Form input=new Form();
  input.add("password",password);
  final String uri="https://maps.example.com/user/" + Reference.encode(user);
  new Client(Protocol.HTTP).put(uri,input.getWebRepresentation());
}
