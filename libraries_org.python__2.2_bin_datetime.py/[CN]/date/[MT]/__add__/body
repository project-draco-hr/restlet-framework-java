def __add__(self, other):
    'Add a date to a timedelta.'
    if isinstance(other, timedelta):
        t = tmxxx(self.__year, self.__month, (self.__day + other.days))
        self._checkOverflow(t.year)
        result = self.__class__(t.year, t.month, t.day)
        return result
    raise TypeError
