def test_id_comparisons(self):
    L = []
    for i in range(10):
        L.insert((len(L) // 2), Empty())
    for a in L:
        for b in L:
            self.assertEqual(cmp(a, b), cmp(id(a), id(b)), ('a=%r, b=%r' % (a, b)))
