{
  tokens.remove(accessor);
  accessor.setProperty("user",userId);
  accessor.setProperty("authorized",Boolean.TRUE);
  logger.fine("Authorizing request token " + accessor + " for userId "+ userId);
  tokens.add(accessor);
}
