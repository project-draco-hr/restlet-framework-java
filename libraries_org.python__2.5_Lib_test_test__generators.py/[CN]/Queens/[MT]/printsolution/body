def printsolution(self, row2col):
    n = self.n
    assert (n == len(row2col))
    sep = ('+' + ('-+' * n))
    print sep
    for i in range(n):
        squares = [' ' for j in range(n)]
        squares[row2col[i]] = 'Q'
        print (('|' + '|'.join(squares)) + '|')
        print sep
