def test_read_boundary3(self):
    size = (READAHEAD_SIZE - 1)
    read(self.fp, ('x' * size), size)
    read(self.fp, '\n')
    self.fp.seek(0)
    read(self.fp, (('x' * size) + '\n'), READAHEAD_SIZE)
