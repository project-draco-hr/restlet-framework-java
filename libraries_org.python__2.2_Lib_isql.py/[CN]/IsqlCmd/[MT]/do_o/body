def do_o(self, arg):
    '\nSet the output.\n'
    if (not arg):
        fp = self.db.out
        try:
            if fp:
                fp.close()
        finally:
            self.db.out = None
    else:
        fp = open(arg, 'w')
        self.db.out = fp
