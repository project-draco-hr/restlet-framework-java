def close(self):
    self._stream.close()
    returncode = self._proc.wait()
    if (returncode == 0):
        return None
    if (_name == 'nt'):
        return returncode
    else:
        return returncode
