def test_insecure_strings(self):
    insecure = ['abc', '2 + 2', "'abc' + 'def'", "'abc", '\'abc"', "'abc'   ?", '\'abc"\'\'', "'\\\\a'''\\'\\\\''"]
    for s in insecure:
        buf = (('S' + s) + '\np0\n.')
        self.assertRaises(ValueError, self.loads, buf)
