{
  if (preferences.isEmpty())   return new SortedMetadata<MediaType>(Collections.singletonList((Collection<MediaType>)Collections.singletonList(MediaType.ALL)));
 else   return new SortedMetadata<MediaType>(preferences);
}
