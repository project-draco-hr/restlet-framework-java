@bigmemtest(minsize=(_2G + 2), memuse=16)
def test_compare(self, size):
    l1 = ([u''] * size)
    l2 = ([u''] * size)
    self.failUnless((l1 == l2))
    del l2
    l2 = ([u''] * (size + 1))
    self.failIf((l1 == l2))
    del l2
    l2 = ([2] * size)
    self.failIf((l1 == l2))
