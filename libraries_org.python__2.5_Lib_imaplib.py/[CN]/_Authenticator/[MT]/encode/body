def encode(self, inp):
    oup = ''
    while inp:
        if (len(inp) > 48):
            t = inp[:48]
            inp = inp[48:]
        else:
            t = inp
            inp = ''
        e = binascii.b2a_base64(t)
        if e:
            oup = (oup + e[:(-1)])
    return oup
