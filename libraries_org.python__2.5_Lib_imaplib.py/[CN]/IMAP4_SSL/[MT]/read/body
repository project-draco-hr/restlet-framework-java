def read(self, size):
    "Read 'size' bytes from remote."
    chunks = []
    read = 0
    while (read < size):
        data = self.sslobj.read(min((size - read), 16384))
        read += len(data)
        chunks.append(data)
    return ''.join(chunks)
