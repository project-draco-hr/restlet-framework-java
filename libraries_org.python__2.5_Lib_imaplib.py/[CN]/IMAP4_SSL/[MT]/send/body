def send(self, data):
    'Send data to remote.'
    bytes = len(data)
    while (bytes > 0):
        sent = self.sslobj.write(data)
        if (sent == bytes):
            break
        data = data[sent:]
        bytes = (bytes - sent)
