def Int2AP(num):
    'Convert integer to A-P string representation.'
    val = ''
    AP = 'ABCDEFGHIJKLMNOP'
    num = int(abs(num))
    while num:
        (num, mod) = divmod(num, 16)
        val = (AP[mod] + val)
    return val
