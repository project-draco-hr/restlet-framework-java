def test():
    buf = StringIO.StringIO()
    _stdout = sys.stdout
    try:
        sys.stdout = buf
        _test()
    finally:
        sys.stdout = _stdout
    s = buf.getvalue()
    for line in s.split('\n'):
        print line.strip()
