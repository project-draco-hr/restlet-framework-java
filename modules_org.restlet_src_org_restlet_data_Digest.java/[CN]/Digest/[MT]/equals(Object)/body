{
  boolean result=(obj instanceof Digest) && obj != null;
  if (result) {
    Digest objDigest=(Digest)obj;
    result=(objDigest.getAlgorithm().equals(this.getAlgorithm()) && (objDigest.getValue().equals(this.getValue())));
  }
  return result;
}
