{
  if (isStarted()) {
    try {
      transport.stop();
      this.started=false;
    }
 catch (    TransportException ex) {
      logger.log(Level.WARNING,"Failed to stop transport",ex);
    }
  }
}
