{
  boolean result=false;
  if (object instanceof Link) {
    Link link=(Link)object;
    result=((getIdentifier() == null) || (getIdentifier().equals(link.getIdentifier()))) && ((getSourceRef() == null) || (getSourceRef().equals(link.getSourceRef()))) && ((getTarget() == null) || (getTarget().equals(link.getTarget())))&& ((getTypeRef() == null) || (getTypeRef().equals(link.getTypeRef())));
  }
  return result;
}
