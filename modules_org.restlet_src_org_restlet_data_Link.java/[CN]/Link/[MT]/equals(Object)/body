{
  boolean result=false;
  if (object instanceof Link) {
    Link link=(Link)object;
    result=((getIdentifier() == null) || (getIdentifier().equals(link.getIdentifier()))) && ((getSourceAsReference() == null) || (getSourceAsReference().equals(link.getSourceAsReference()))) && ((getTarget() == null) || (getTarget().equals(link.getTarget())))&& ((getTypeRef() == null) || (getTypeRef().equals(link.getTypeRef())));
  }
  return result;
}
