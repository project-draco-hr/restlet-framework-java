{
  update(challengeResponse,request,response);
  AuthenticatorHelper helper=Engine.getInstance().findHelper(challengeResponse.getScheme(),false,true);
  challengeResponse.setSecret(helper.formatSecret(challengeResponse,request,response,identifier,baseSecret,baseSecretAlgorithm));
}
