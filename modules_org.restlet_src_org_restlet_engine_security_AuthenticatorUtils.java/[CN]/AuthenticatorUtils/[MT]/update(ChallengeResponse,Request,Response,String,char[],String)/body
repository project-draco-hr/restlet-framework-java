{
  update(challengeResponse,previousRequest,previousResponse);
  final AuthenticatorHelper helper=Engine.getInstance().findHelper(challengeResponse.getScheme(),false,true);
  challengeResponse.setSecret(helper.formatSecret(challengeResponse,previousRequest,previousResponse,identifier,baseSecret,baseSecretAlgorithm));
}
