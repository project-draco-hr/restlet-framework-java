{
  update(challengeResponse,request,response);
  final AuthenticatorHelper helper=Engine.getInstance().findHelper(passwordScheme,false,true);
  challengeResponse.setSecret(helper.formatSecret(challengeResponse,password));
}
