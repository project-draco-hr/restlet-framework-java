{
  CacheDirective directive;
  for (int i=0; i < directives.size(); i++) {
    if (i > 0) {
      append(", ");
    }
    directive=directives.get(i);
    try {
      append(directive);
    }
 catch (    IOException e) {
    }
  }
  return this;
}
