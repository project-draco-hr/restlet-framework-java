{
  if (response.isEntityAvailable() && response.getEntity().getEncodings().contains(Encoding.FREEMARKER)) {
    response.setEntity(new TemplateRepresentation(response.getEntity(),config,dataModel,response.getEntity().getMediaType()));
  }
}
