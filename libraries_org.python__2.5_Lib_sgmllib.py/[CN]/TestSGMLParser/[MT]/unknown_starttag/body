def unknown_starttag(self, tag, attrs):
    self.flush()
    if (not attrs):
        print (('start tag: <' + tag) + '>')
    else:
        print ('start tag: <' + tag),
        for (name, value) in attrs:
            print ((((name + '=') + '"') + value) + '"'),
        print '>'
