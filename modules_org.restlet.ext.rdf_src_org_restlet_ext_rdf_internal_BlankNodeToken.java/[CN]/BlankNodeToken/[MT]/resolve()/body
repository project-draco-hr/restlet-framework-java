{
  if (!isResolved()) {
    setResolved(true);
    if (getContentHandler() != null) {
      getContentHandler().generateLinks(lexicalUnits);
    }
  }
  if (this.getValue() != null) {
    return getValue();
  }
 else {
  }
  return null;
}
