def join(a, *p):
    'Join two (or more) paths.'
    path = a
    for b in p:
        if isabs(b):
            path = b
        elif ((path == '') or (path[(-1):] in '/\\:')):
            path = (path + b)
        else:
            path = ((path + '\\') + b)
    return path
