def findtestdirs(all=ALL):
    testdirs = []
    j = os.path.join
    for p in sys.path:
        p = j(p, 'test')
        if os.path.isfile(j(p, 'regrtest.py')):
            testdirs.append(p)
            if (not all):
                break
    return testdirs
