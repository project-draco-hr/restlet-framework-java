{
  HttpClientCall result=null;
  try {
    result=new HttpUrlConnectionCall(this,call.getMethod().toString(),call.getResourceRef().toString(),call.isInputAvailable());
  }
 catch (  IOException ioe) {
    logger.log(Level.WARNING,"Unable to create the HTTP client call",ioe);
  }
  return result;
}
