{
  HttpClientCall result=null;
  try {
    result=new HttpUrlConnectionCall(this,request.getMethod().toString(),request.getResourceRef().toString(),request.isInputAvailable());
  }
 catch (  IOException ioe) {
    logger.log(Level.WARNING,"Unable to create the HTTP client call",ioe);
  }
  return result;
}
