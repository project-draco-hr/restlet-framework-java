def _testElementReprAndStrUnicode():
    dom = Document()
    el = dom.appendChild(dom.createElement(u'abc'))
    string1 = repr(el)
    string2 = str(el)
    confirm((string1 == string2))
    dom.unlink()
