def testAppendChildFragment():
    (dom, orig, c1, c2, c3, frag) = _create_fragment_test_nodes()
    dom.documentElement.appendChild(frag)
    confirm((tuple(dom.documentElement.childNodes) == (orig, c1, c2, c3)), 'appendChild(<fragment>)')
    frag.unlink()
    dom.unlink()
