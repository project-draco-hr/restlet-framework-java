def _isinfinity(self):
    'Returns whether the number is infinite\n\n        0 if finite or not a number\n        1 if +INF\n        -1 if -INF\n        '
    if (self._exp == 'F'):
        if self._sign:
            return (-1)
        return 1
    return 0
