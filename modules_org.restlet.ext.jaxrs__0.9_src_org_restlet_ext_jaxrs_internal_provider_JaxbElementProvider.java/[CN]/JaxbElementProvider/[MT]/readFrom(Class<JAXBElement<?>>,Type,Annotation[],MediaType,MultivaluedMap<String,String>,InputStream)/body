{
  Class<?> clazz=Util.getGenericClass(genericType);
  if (clazz == null)   throw new ImplementationException("The JaxbElement provider has gotten a type it could not unmarshal. Perhaps is the JaxbElementProvider not consistent to itself.");
  QName qName=new QName("testQName");
  Class<?> declaredType=clazz;
  Object value=unmarshal(clazz,entityStream);
  return new JAXBElement(qName,declaredType,value);
}
