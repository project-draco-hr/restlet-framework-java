def handlerB(self, signum, frame):
    self.b_called = True
    if test_support.verbose:
        print ('handlerB invoked from signal %s at:\n%s' % (signum, self.format_frame(frame, limit=1)))
    raise HandlerBCalled(signum, self.format_frame(frame))
