{
  if (uriPart == null)   throw throwIllegalArgExc(indexForErrMessage,errMessName,uriPart," must not be null");
  if (encode)   return encodeNotBraces(uriPart,encodeSlash);
 else   checkForInvalidUriChars(uriPart,indexForErrMessage,errMessName);
  return uriPart;
}
