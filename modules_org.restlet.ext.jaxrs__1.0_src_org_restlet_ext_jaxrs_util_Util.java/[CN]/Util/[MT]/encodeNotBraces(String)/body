{
  StringBuilder stb=new StringBuilder();
  int l=string.length();
  for (int i=0; i < l; i++) {
    char c=string.charAt(i);
    if (c == '{' || c == '}')     stb.append(c);
 else     stb.append(Reference.encode(string.substring(i,i + 1)));
  }
  return stb.toString();
}
