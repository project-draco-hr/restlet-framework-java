{
  if (uriPart == null)   throw throwIllegalArgExc(index,errMessName,uriPart," must not be null");
  if (encode)   return encodeNotBraces(uriPart,encodeSlash);
 else   checkForInvalidUriChars(uriPart,index,errMessName);
  return uriPart;
}
