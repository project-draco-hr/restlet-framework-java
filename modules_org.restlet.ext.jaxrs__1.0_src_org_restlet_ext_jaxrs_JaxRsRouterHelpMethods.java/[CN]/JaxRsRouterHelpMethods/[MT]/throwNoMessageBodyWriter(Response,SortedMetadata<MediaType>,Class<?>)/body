{
  response.setStatus(Status.SERVER_ERROR_INTERNAL);
  response.setEntity(new StringRepresentation("No MessageBodyReader found to convert from java type " + paramType + " to one of the media types "+ accMediaTypes));
  throw new RequestHandledException();
}
