def test_func_closure():
    a = 12

    def f():
        print a
    c = f.func_closure
    verify(isinstance(c, tuple))
    verify((len(c) == 1))
    verify((c[0].__class__.__name__ == 'cell'))
    cantset(f, 'func_closure', c)
