def close(self):
    try:
        if (not self.sock_impl):
            return
        sock = self.sock_impl
        self.sock_impl = None
        sock.close()
    except java.lang.Exception as jlx:
        raise _map_exception(jlx)
