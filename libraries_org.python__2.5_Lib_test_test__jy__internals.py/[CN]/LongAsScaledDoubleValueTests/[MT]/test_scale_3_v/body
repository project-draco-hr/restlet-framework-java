def test_scale_3_v(self):
    e = self.e
    v = self.v8
    sdv = self.sdv
    assert (long(sdv(v, e)) == v)
    assert (e[0] == 0)
    assert ((long(sdv((v + 1), e)) - v) == 0)
    assert (e[0] == 0)
