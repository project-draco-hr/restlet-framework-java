def test_unique_ids(self):
    d = {}
    cnt = 0
    for i in xrange(100000):
        s = ('test' + repr(i))
        j = id(s)
        if d.has_key(j):
            cnt = (cnt + 1)
        d[j] = s
    self.assertEquals(cnt, 0)
