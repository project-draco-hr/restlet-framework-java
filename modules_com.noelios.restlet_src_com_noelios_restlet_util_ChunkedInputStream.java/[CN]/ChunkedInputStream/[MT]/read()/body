{
  int result=-1;
  initialize();
  if (!done) {
    if (chunkAvailable()) {
      result=source.read();
      position++;
    }
 else {
      initializeChunk();
      return read();
    }
  }
  return result;
}
