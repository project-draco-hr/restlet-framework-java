{
  if (directoryRedirection) {
    getResponse().setStatus(Status.REDIRECTION_FOUND);
    getResponse().setEntity(null);
    getResponse().setRedirectRef(getRequest().getResourceRef().getIdentifier() + "/");
  }
 else {
    super.handleGet();
  }
}
