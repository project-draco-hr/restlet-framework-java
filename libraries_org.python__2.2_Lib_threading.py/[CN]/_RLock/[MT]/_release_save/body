def _release_save(self):
    if __debug__:
        self._note('%s._release_save()', self)
    count = self.__count
    self.__count = 0
    owner = self.__owner
    self.__owner = None
    self.__block.release()
    return (count, owner)
