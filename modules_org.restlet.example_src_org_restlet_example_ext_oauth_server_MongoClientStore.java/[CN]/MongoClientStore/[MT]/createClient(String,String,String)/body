{
  DBObject client=new BasicDBObject().append("_id",clientId).append(MongoClient.CLIENT_SECRET,clientSecret).append(MongoClient.REDIRECT_URI,redirectUri);
  clients.insert(client);
  return new MongoClient(client);
}
