def test_tuple(self):
    eq = self.assertEquals
    eq(r((1,)), '(1,)')
    t3 = (1, 2, 3)
    eq(r(t3), '(1, 2, 3)')
    r2 = Repr()
    r2.maxtuple = 2
    expected = (repr(t3)[:(-2)] + '...)')
    eq(r2.repr(t3), expected)
