{
  int value=defaultValue;
  if (node != null) {
    try {
      value=Integer.parseInt(node.getNodeValue());
    }
 catch (    final Exception e) {
      value=defaultValue;
    }
  }
  return value;
}
