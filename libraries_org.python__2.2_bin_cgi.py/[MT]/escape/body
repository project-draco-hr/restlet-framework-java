def escape(s, quote=None):
    "Replace special characters '&', '<' and '>' by SGML entities."
    s = s.replace('&', '&amp;')
    s = s.replace('<', '&lt;')
    s = s.replace('>', '&gt;')
    if quote:
        s = s.replace('"', '&quot;')
    return s
