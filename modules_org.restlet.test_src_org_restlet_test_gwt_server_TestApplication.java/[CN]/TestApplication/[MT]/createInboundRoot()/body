{
  return new Restlet(){
    @Override public void handle(    Request request,    Response response){
      response.setEntity("The Restlet server side is alive. Method called: " + request.getMethod(),MediaType.TEXT_PLAIN);
    }
  }
;
}
