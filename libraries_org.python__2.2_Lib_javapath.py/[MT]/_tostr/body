def _tostr(s, method):
    if isinstance(s, basestring):
        return s
    import org
    raise TypeError, ('%s() argument must be a str or unicode object, not %s' % (method, org.python.core.Py.safeRepr(s)))
