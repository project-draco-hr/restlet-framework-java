def setUp(self):
    self.case = 'unit set (tuple)'
    self.values = [(0, 'zero')]
    self.set = Set(self.values)
    self.dup = Set(self.values)
    self.length = 1
    self.repr = "Set([(0, 'zero')])"
