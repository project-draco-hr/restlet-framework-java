{
  Router router=new Router(getContext());
  router.attach("/dom",DomResource.class);
  Directory dir=new Directory(getContext(),LocalReference.createFileReference("D:\\workspace\\restlet-1.1\\BouquinApress\\src\\chapter7"));
  router.attach("/xml",dir);
  return router;
}
