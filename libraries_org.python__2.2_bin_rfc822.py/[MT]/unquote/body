def unquote(str):
    'Remove quotes from a string.'
    if (len(str) > 1):
        if ((str[0] == '"') and (str[(-1):] == '"')):
            return str[1:(-1)]
        if ((str[0] == '<') and (str[(-1):] == '>')):
            return str[1:(-1)]
    return str
