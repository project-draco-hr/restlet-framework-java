def parseaddr(address):
    'Parse an address into a (realname, mailaddr) tuple.'
    a = AddressList(address)
    list = a.addresslist
    if (not list):
        return (None, None)
    else:
        return list[0]
