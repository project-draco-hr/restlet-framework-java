def test_deepcopy_slots(self):


    class C(object):
        __slots__ = ['foo']
    x = C()
    x.foo = [42]
    y = copy.deepcopy(x)
    self.assertEqual(x.foo, y.foo)
    self.assert_((x.foo is not y.foo))
