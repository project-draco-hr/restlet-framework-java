def test_deepcopy_atomic(self):


    class Classic:
        pass


    class NewStyle(object):
        pass

    def f():
        pass
    tests = [None, 42, (2L ** 100), 3.14, True, False, 1j, 'hello', u'hello\u1234', f.func_code, NewStyle, xrange(10), Classic, max]
    for x in tests:
        self.assert_((copy.deepcopy(x) is x), repr(x))
