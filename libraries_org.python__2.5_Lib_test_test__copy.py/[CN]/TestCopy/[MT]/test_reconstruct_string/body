def test_reconstruct_string(self):


    class C(object):

        def __reduce__(self):
            return ''
    x = C()
    y = copy.copy(x)
    self.assert_((y is x))
    y = copy.deepcopy(x)
    self.assert_((y is x))
