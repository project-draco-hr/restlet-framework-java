import curses, sys, tempfile
import test_support
test_support.requires('curses')
if (__name__ == '__main__'):
    curses.wrapper(main)
else:
    try:
        stdscr = curses.initscr()
        main(stdscr)
    finally:
        curses.endwin()
