def test_main():
    import test_doctest2
    EXPECTED = 19
    (f, t) = test_support.run_doctest(test_doctest2)
    if (t != EXPECTED):
        raise test_support.TestFailed(('expected %d tests to run, not %d' % (EXPECTED, t)))
