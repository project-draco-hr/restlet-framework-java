{
  final StringBuilder path=getPath();
  if ((this.host != null) && ((newPathSegment.length() == 0) || (newPathSegment.charAt(0) != '/'))) {
    path.append('/');
  }
  path.append(newPathSegment);
}
