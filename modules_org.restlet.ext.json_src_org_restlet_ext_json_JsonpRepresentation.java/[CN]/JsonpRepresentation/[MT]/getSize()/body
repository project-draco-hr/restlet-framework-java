{
  long result=super.getSize();
  if (result > 0 && MediaType.APPLICATION_JSON.equals(super.getMediaType())) {
    return result + getCallback().length() + "({status:,body:});".length()+ Integer.toString(getStatus().getCode()).length();
  }
  return UNKNOWN_SIZE;
}
