{
  Collection<Constructor> constructors=Arrays.asList(resourceClass.getConstructors());
  for (  Constructor constr : constructors) {
    if (constr.getParameterTypes().length == 0)     return constr;
  }
  return constructors.iterator().next();
}
