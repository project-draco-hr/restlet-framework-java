def startElement(self, name, atts):
    self.stack.append(name)
    if hasattr(self.handler, ('start_' + name)):
        self.handler.handle_starttag(name, getattr(self.handler, ('start_' + name)), atts)
    else:
        self.handler.unknown_starttag(name, atts)
