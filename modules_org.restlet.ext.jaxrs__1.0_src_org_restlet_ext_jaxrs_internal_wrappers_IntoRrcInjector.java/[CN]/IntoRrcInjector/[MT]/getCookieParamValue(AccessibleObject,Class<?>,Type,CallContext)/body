{
  CookieParam headerParam=fieldOrBeanSetter.getAnnotation(CookieParam.class);
  DefaultValue defaultValue=fieldOrBeanSetter.getAnnotation(DefaultValue.class);
  Object value=WrapperUtil.getCookieParamValue(convTo,paramGenericType,headerParam,defaultValue,callContext);
  return value;
}
