{
  FormReader fr=null;
  if (decode) {
    fr=new FormReader(logger,parametersString,characterSet,separator);
  }
 else {
    fr=new FormReader(logger,parametersString,separator);
  }
  fr.addParameters(form);
}
