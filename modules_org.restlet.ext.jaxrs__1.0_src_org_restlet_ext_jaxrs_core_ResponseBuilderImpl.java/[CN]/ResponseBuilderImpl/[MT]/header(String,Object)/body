{
  if (name == null)   throw new IllegalArgumentException("You must give a name of the header");
  if (name.equals(HttpHeaders.SET_COOKIE))   this.cookie((NewCookie)value);
 else   getMetadata().add(name,value);
  return this;
}
