{
  boolean result=true;
  if (call.getMethod().equals(Methods.GET) || call.getMethod().equals(Methods.HEAD) || call.getMethod().equals(Methods.DELETE)) {
    result=false;
  }
 else {
    result=(call.getInput() != null) && call.getInput().isContentAvailable();
  }
  return result;
}
