def __init__(self, f):
    if (type(f) == type('')):
        filename = f
        f = __builtin__.open(f, 'wb')
    else:
        filename = '???'
    self.initfp(f)
    if (filename[(-5):] == '.aiff'):
        self._aifc = 0
    else:
        self._aifc = 1
