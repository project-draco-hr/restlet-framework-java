{
  Reference ref=getDefaultReference();
  int port=8080;
  ref.setHostPort(port);
  assertEquals(port,ref.getHostPort().intValue());
  port=9090;
  ref.setHostPort(port);
  assertEquals(port,ref.getHostPort().intValue());
}
