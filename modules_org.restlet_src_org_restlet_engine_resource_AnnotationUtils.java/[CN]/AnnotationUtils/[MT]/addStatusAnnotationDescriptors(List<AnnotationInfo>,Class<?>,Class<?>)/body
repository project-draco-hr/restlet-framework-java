{
  List<AnnotationInfo> result=descriptors;
  Annotation annotation=clazz.getAnnotation(org.restlet.resource.Status.class);
  if (annotation != null) {
    result.add(new StatusAnnotationInfo(initialClass,((org.restlet.resource.Status)annotation).value()));
  }
  return result;
}
