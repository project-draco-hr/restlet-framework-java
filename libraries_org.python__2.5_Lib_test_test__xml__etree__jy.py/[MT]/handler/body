def handler(name, header='XML>', returns=None):

    def _handler(*args):
        if (len(args) == 1):
            args = ('(%r)' % args[0])
        else:
            args = str(args)
        print header, (name + ('%s' % args))
        return returns
    return _handler
