def test_radd_specials(self):
    u = UserList('eggs')
    u2 = ('spam' + u)
    self.assertEqual(u2, list('spameggs'))
    u2 = u.__radd__(UserList('spam'))
    self.assertEqual(u2, list('spameggs'))
