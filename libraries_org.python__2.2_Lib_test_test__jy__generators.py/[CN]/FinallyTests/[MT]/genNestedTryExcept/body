def genNestedTryExcept(self):
    for j in range(3):
        try:
            try:
                (1 // 0)
            finally:
                for k in range(3):
                    try:
                        (1 // 0)
                    finally:
                        yield (j, k)
        except:
            pass
