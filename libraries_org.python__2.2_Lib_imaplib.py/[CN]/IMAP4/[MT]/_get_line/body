def _get_line(self):
    line = self.readline()
    if (not line):
        raise self.abort('socket error: EOF')
    line = line[:(-2)]
    if __debug__:
        if (self.debug >= 4):
            _mesg(('< %s' % line))
        else:
            _log(('< %s' % line))
    return line
