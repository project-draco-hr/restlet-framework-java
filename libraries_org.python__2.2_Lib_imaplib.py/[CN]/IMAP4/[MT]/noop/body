def noop(self):
    'Send NOOP command.\n\n        (typ, data) = <instance>.noop()\n        '
    if __debug__:
        if (self.debug >= 3):
            _dump_ur(self.untagged_responses)
    return self._simple_command('NOOP')
