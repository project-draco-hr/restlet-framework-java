def _mk_bitmap(bits):
    data = []
    m = 1
    v = 0
    for c in bits:
        if c:
            v = (v + m)
        m = (m << 1)
        if (m > MAXCODE):
            data.append(v)
            m = 1
            v = 0
    return data
