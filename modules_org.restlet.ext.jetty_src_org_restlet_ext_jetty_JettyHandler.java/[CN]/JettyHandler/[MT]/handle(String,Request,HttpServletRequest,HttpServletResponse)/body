{
  final Request baseRequest=(servletRequest instanceof Request) ? (Request)servletRequest : HttpConnection.getCurrentConnection().getRequest();
  this.helper.handle(new JettyCall(this.helper.getHelped(),HttpConnection.getCurrentConnection()));
  baseRequest.setHandled(true);
}
