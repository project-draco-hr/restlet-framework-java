def quote_plus(s, safe=''):
    "Quote the query fragment of a URL; replacing ' ' with '+'"
    if (' ' in s):
        s = quote(s, (safe + ' '))
        return s.replace(' ', '+')
    return quote(s, safe)
