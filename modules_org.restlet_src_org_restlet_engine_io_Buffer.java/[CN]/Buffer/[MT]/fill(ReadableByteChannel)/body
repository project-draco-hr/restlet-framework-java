{
  int result=0;
  if (canFill()) {
    if (sourceChannel.isOpen()) {
      result=sourceChannel.read(getBytes());
      if (result > 0) {
        flip();
        if (Context.getCurrentLogger().isLoggable(Level.FINE)) {
          Context.getCurrentLogger().fine("Refilled buffer with " + result + " byte(s)");
        }
      }
 else {
        if (Context.getCurrentLogger().isLoggable(Level.FINE)) {
          Context.getCurrentLogger().fine("Coudn't refill buffer : " + toString());
        }
      }
    }
  }
  return result;
}
