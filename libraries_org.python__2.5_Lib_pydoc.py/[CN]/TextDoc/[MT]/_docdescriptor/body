def _docdescriptor(self, name, value, mod):
    results = []
    push = results.append
    if name:
        push(self.bold(name))
        push('\n')
    doc = (getdoc(value) or '')
    if doc:
        push(self.indent(doc))
        push('\n')
    return ''.join(results)
