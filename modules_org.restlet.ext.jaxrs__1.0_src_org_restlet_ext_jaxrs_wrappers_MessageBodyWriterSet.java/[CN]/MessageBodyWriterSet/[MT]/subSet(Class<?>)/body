{
  List<MessageBodyWriter> mbws=new ArrayList<MessageBodyWriter>();
  for (  MessageBodyWriter mbw : this) {
    if (mbw.isWriteable(entityClass))     mbws.add(mbw);
  }
  return new MessageBodyWriterSet(mbws,true);
}
