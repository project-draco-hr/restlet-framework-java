{
  if (!(o instanceof Role))   return false;
  Role role=(Role)o;
  return SystemUtils.equals(role.getApplication(),getApplication()) && SystemUtils.equals(role.getName(),getName()) && SystemUtils.equals(role.getChildRoles(),getChildRoles());
}
