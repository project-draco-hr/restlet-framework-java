{
  if (attribute == AttributeExchange.EMAIL)   u.setEmail(value);
 else   if (attribute == AttributeExchange.FIRST_NAME)   u.setFirstName(value);
 else   if (attribute == AttributeExchange.LAST_NAME)   u.setLastName(value);
}
