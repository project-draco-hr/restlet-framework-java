{
  if (attribute == AX.FULL_NAME)   return this.getName();
 else   if (attribute == AX.EMAIL)   return this.getEmail();
 else   if (attribute == AX.FIRST_NAME)   return this.getFirstName();
 else   if (attribute == AX.LAST_NAME)   return this.getLastName();
  return null;
}
