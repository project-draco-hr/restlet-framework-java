def _search_start(self):
    while 1:
        pos = self.fp.tell()
        line = self.fp.readline()
        if (not line):
            raise EOFError
        if ((line[:5] == 'From ') and self._isrealfromline(line)):
            self.fp.seek(pos)
            return
