def _search_end(self):
    while 1:
        pos = self.fp.tell()
        line = self.fp.readline()
        if (not line):
            return
        if (line == '\x1f\x0c\n'):
            self.fp.seek(pos)
            return
