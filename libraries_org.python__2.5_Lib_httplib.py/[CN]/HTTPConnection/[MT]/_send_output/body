def _send_output(self):
    'Send the currently buffered request and clear the buffer.\n\n        Appends an extra \\r\\n to the buffer.\n        '
    self._buffer.extend(('', ''))
    msg = '\r\n'.join(self._buffer)
    del self._buffer[:]
    self.send(msg)
