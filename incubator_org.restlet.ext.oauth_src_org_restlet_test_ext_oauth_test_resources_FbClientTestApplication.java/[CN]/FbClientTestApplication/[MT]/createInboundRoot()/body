{
  Context ctx=getContext();
  Router router=new Router(ctx);
  local=new FacebookProxy("118328624855019","26e1ccb99f6135ebbf901b04508cba09","offline_access,publish_stream",getContext().createChildContext());
  local.setNext(FacebookFeedMe.class);
  router.attach("/me",local);
  router.attach("/unprotected",DummyResource.class);
  return router;
}
