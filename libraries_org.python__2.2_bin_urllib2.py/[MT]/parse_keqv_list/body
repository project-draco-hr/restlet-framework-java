def parse_keqv_list(l):
    'Parse list of key=value strings where keys are not duplicated.'
    parsed = {}
    for elt in l:
        (k, v) = elt.split('=', 1)
        if ((v[0] == '"') and (v[(-1)] == '"')):
            v = v[1:(-1)]
        parsed[k] = v
    return parsed
