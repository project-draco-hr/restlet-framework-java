def encode_digest(digest):
    hexrep = []
    for c in digest:
        n = ((ord(c) >> 4) & 15)
        hexrep.append(hex(n)[(-1)])
        n = (ord(c) & 15)
        hexrep.append(hex(n)[(-1)])
    return ''.join(hexrep)
