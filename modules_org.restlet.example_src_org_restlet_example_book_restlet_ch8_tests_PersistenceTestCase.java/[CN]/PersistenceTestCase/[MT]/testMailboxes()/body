{
  assertEquals(this.mailRoot.getMailboxes().size(),3);
  assertEquals(this.mailRoot.getMailboxes().get(0).getOwner(),this.mailRoot.getUsers().get(0));
  assertTrue(this.mailRoot.getMailboxes().get(2).getOwner().isAdministrator());
}
