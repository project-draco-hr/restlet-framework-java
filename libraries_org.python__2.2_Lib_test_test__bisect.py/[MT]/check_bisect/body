def check_bisect(func, list, elt, expected):
    global nerrors
    got = func(list, elt)
    if (got != expected):
        print  >> sys.stderr, ('expected %s(%s, %s) -> %s, but got %s' % (func.__name__, list, elt, expected, got))
        nerrors += 1
