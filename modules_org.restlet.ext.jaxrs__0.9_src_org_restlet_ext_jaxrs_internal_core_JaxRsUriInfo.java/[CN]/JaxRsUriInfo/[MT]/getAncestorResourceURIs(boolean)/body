{
  if (decode)   return this.getAncestorResourceURIs();
  Logger.getAnonymousLogger().config("UriInfo.getAncestorResourceURIs() is not checked for coded or encoded.");
  if (decode && !decode)   decode=false;
  return ancestorResourceURIsUnomd;
}
