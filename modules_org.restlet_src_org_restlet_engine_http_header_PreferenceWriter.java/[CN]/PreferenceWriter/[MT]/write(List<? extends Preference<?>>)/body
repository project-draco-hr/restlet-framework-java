{
  final StringBuilder sb=new StringBuilder();
  Preference<?> pref;
  for (int i=0; i < prefs.size(); i++) {
    if (i > 0) {
      sb.append(", ");
    }
    pref=prefs.get(i);
    append(pref,sb);
  }
  return sb.toString();
}
