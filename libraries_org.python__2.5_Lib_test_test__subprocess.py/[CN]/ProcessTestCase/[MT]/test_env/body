def test_env(self):
    newenv = os.environ.copy()
    newenv['FRUIT'] = 'orange'
    p = subprocess.Popen([sys.executable, '-c', 'import sys,os;sys.stdout.write(os.getenv("FRUIT"))'], stdout=subprocess.PIPE, env=newenv)
    self.assertEqual(p.stdout.read(), 'orange')
