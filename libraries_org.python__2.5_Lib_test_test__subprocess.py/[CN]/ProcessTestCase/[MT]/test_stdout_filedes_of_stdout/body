def test_stdout_filedes_of_stdout(self):
    cmd = "import sys, os; sys.exit(os.write(sys.stdout.fileno(), '.\\n'))"
    rc = subprocess.call([sys.executable, '-c', cmd], stdout=sys.__stdout__.fileno())
    self.assertEquals(rc, 2)
