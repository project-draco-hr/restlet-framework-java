def test_stdout_none(self):
    p = subprocess.Popen([sys.executable, '-c', 'print "    this bit of output is from a test of stdout in a different process ..."'], stdin=subprocess.PIPE, stderr=subprocess.PIPE)
    p.wait()
    self.assertEqual(p.stdout, None)
