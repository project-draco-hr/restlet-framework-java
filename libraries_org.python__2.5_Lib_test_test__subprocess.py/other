import unittest
from test import test_support
import subprocess
import sys
import os
import tempfile
import time
import re
mswindows = (sys.platform == 'win32')
jython = sys.platform.startswith('java')
if mswindows:
    SETBINARY = 'import msvcrt; msvcrt.setmode(sys.stdout.fileno(), os.O_BINARY);'
elif jython:
    SETBINARY = 'import os,sys;sys.stdout = os.fdopen(sys.stdout.fileno(), "wb");'
else:
    SETBINARY = ''
if (not jython):
    import signal
if (__name__ == '__main__'):
    test_main()
