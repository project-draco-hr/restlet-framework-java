def dirname(path):
    'Return the directory component of a pathname'
    path = _tostr(path, 'dirname')
    result = File(path).getParent()
    if (not result):
        if isabs(path):
            result = path
        else:
            result = ''
    return result
