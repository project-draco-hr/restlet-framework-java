{
  ClientResource resource=new ClientResource("http://www.manning.com");
  resource.get();
  Status status=resource.getStatus();
  if (status.isSuccess()) {
    System.out.println("Request successful.");
  }
 else   if (status.isError()) {
    System.out.println("Request failed.");
  }
}
