def getran2(ndigits):
    answer = 0L
    for i in xrange(ndigits):
        answer = ((answer << SHIFT) | random.randint(0, MASK))
    if (random.random() < 0.5):
        answer = (- answer)
    return answer
