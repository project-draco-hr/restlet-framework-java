def test_format(self):
    for x in special:
        self.check_format_1(x)
    for i in xrange(10):
        for lenx in xrange(1, (MAXDIGITS + 1)):
            x = self.getran(lenx)
            self.check_format_1(x)
