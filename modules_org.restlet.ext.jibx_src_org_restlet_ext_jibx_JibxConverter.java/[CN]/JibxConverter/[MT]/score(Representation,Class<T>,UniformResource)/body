{
  float result=-1.0F;
  if ((source != null) && (isJibxBoundClass(target) || JibxRepresentation.class.isAssignableFrom(source.getClass()))) {
    result=1.0F;
  }
 else   if (JibxRepresentation.class.isAssignableFrom(target)) {
    result=1.0F;
  }
  return result;
}
