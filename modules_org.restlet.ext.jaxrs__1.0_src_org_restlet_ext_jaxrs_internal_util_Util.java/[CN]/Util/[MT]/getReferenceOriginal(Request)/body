{
  Reference reference=(Reference)request.getAttributes().get(TunnelService.ATTRIBUTE_ORIGINAL_REF);
  if (reference == null)   return getReferenceCut(request);
  reference.setBaseRef(getReferenceCut(request).getBaseRef());
  return reference;
}
