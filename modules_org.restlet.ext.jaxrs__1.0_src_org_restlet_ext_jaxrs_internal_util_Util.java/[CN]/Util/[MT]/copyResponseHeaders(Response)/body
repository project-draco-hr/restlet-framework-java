{
  final Series<Parameter> headers=new Form();
  HttpServerConverter.addResponseHeaders(restletResponse,headers);
  HttpServerConverter.addEntityHeaders(restletResponse.getEntity(),headers);
  return headers;
}
