{
  String annoName=annotation.getName();
  String suffix=annoName.substring(annoName.lastIndexOf('.'));
  for (  Annotation anno : field.getDeclaredAnnotations()) {
    if (anno.annotationType().getName().endsWith(suffix))     return true;
  }
  return false;
}
