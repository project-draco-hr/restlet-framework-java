{
  super(context);
  this.setAuthenticator(authenticator);
  this.loadDefaultProviders();
  if (loadAllRootResourceClasses || loadAllProviders)   JaxRsClassesLoader.loadFromClasspath(this,loadAllRootResourceClasses,loadAllProviders);
}
