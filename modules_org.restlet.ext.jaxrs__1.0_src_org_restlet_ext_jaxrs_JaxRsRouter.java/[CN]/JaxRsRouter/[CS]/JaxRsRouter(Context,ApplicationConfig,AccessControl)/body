{
  super(context);
  this.wrapperFactory=new WrapperFactory(getContext().getLogger());
  this.loadDefaultProviders();
  if (appConfig != null)   this.attach(appConfig);
  if (accessControl != null)   this.setAccessControl(accessControl);
 else   this.setAccessControl(AccessControl.REJECT_WITH_ERROR);
}
