{
  for (  MediaType mediaType : m)   if (Util.isConcrete(mediaType))   return mediaType;
  if (m.contains(MediaType.ALL) || m.contains(MediaType.APPLICATION_ALL))   return MediaType.APPLICATION_OCTET_STREAM;
  throw throwNotAcceptable(restletResponse);
}
