{
  Router router=new Router(context);
  JaxRsGuard guard=new JaxRsGuard(context,challangeScheme,realmName,authorizator);
  router.attach(guard);
  guard.setNext(new JaxRsRouter(context,authorizator));
  return router;
}
