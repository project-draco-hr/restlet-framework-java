{
  if (authorizator == null)   throw new IllegalArgumentException(" You can use the " + AllowAllAuthorizator.class.getName() + " or the "+ ForbidAllAuthorizator.class.getName());
  this.authorizator=authorizator;
}
