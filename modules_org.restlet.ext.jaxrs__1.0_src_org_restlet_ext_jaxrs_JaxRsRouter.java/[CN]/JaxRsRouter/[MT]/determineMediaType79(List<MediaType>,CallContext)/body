{
  if (true)   throw new NotYetImplementedException("sorry I've check in to early");
  for (  MediaType mediaType : m)   return mediaType;
  if (m.contains(MediaType.ALL) || m.contains(MediaType.APPLICATION_ALL))   return MediaType.APPLICATION_OCTET_STREAM;
  throw throwNotAcceptableWhileDetermineMediaType(callContext.getRequest(),callContext.getResponse());
}
