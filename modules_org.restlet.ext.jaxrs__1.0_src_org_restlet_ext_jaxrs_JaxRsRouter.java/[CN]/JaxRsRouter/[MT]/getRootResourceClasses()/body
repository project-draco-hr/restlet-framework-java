{
  Set<Class<?>> jaxRsClasses=new HashSet<Class<?>>();
  for (  RootResourceClass rootResourceClass : this.rootResourceClasses)   jaxRsClasses.add(rootResourceClass.getJaxRsClass());
  return Collections.unmodifiableSet(jaxRsClasses);
}
