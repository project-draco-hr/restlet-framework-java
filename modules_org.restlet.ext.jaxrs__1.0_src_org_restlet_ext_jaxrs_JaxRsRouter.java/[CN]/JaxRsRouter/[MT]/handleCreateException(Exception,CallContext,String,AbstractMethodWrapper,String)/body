{
  if (exception instanceof WebApplicationException)   throw handleWebAppExc((WebApplicationException)exception,callContext,resourceMethod);
  callContext.getResponse().setStatus(Status.SERVER_ERROR_INTERNAL);
  getLogger().logp(Level.WARNING,this.getClass().getName(),methodName,logMessage,exception.getCause());
  exception.printStackTrace();
  throw new RequestHandledException();
}
