{
  if (authenticator == null)   throw new IllegalArgumentException("The authenticator must nit be null. You can use the " + AllowAllAuthenticator.class.getName() + " or the "+ ForbidAllAuthenticator.class.getName());
  this.authenticator=authenticator;
}
