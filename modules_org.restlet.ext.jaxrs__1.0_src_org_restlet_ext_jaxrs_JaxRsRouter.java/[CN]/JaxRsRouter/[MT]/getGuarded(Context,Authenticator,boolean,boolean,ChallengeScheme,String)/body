{
  JaxRsGuard guard=new JaxRsGuard(context,challangeScheme,realmName,authenticator);
  guard.setNext(new JaxRsRouter(context,authenticator,loadAllRootResourceClasses,loadAllProviders));
  return guard;
}
