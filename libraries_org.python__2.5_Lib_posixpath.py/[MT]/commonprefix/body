def commonprefix(m):
    'Given a list of pathnames, returns the longest common leading component'
    if (not m):
        return ''
    s1 = min(m)
    s2 = max(m)
    n = min(len(s1), len(s2))
    for i in xrange(n):
        if (s1[i] != s2[i]):
            return s1[:i]
    return s1[:n]
