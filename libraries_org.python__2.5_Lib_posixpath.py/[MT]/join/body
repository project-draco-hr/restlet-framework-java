def join(a, *p):
    "Join two or more pathname components, inserting '/' as needed"
    path = a
    for b in p:
        if b.startswith('/'):
            path = b
        elif ((path == '') or path.endswith('/')):
            path += b
        else:
            path += ('/' + b)
    return path
