{
  try {
    this.connection.completeResponse();
    this.connection.commitResponse(true);
  }
 catch (  IOException ex) {
    getLogger().log(Level.WARNING,"Unable to complete or commit the response",ex);
  }
}
