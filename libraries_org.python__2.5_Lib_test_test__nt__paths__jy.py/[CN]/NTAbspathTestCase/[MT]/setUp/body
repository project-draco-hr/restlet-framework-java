def setUp(self):
    with open(test_support.TESTFN, 'w') as fp:
        fp.write('foo')
    (drive, self.path) = os.path.splitdrive(os.path.abspath(test_support.TESTFN))
    self.orig_cwd = os.getcwd()
    os.chdir(os.path.join(drive, os.sep))
