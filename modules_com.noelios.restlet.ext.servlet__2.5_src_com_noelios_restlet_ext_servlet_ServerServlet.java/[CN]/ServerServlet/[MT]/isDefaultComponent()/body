{
  String configPath=getServletContext().getRealPath("/WEB-INF/restlet.xml");
  File configFile=new File(configPath);
  String componentAttributeName=getInitParameter(COMPONENT_KEY,null);
  if ((configPath != null && configFile.exists()) || componentAttributeName != null) {
    return false;
  }
  return true;
}
