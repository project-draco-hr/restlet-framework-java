{
  AssociationEnd result=null;
  for (  NavigationProperty association : type.getAssociations()) {
    AssociationEnd to=association.getToRole();
    if (to.getRole().equals(propertyName) || to.getNormalizedRole().equals(propertyName)) {
      result=to;
      break;
    }
  }
  return result;
}
