def __init__(self, a, b, ignore=None, hide=None):
    self.left = a
    self.right = b
    if (hide is None):
        self.hide = [os.curdir, os.pardir]
    else:
        self.hide = hide
    if (ignore is None):
        self.ignore = ['RCS', 'CVS', 'tags']
    else:
        self.ignore = ignore
