{
  if ((values != null) && !values.isEmpty()) {
    boolean first=true;
    for (    T value : values) {
      if (first) {
        first=false;
      }
 else {
        appendValueSeparator();
      }
      append(value);
    }
  }
  return this;
}
