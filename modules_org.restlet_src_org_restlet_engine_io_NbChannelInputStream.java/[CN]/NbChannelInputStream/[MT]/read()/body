{
  int result=-1;
  if (!this.endReached) {
    if (!this.byteBuffer.hasRemaining()) {
      refill();
    }
    if (!this.endReached) {
      result=this.byteBuffer.get() & 0xff;
    }
  }
  return result;
}
