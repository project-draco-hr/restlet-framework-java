def test_boom2():
    a = Boom2()
    b = Boom2()
    a.attr = b
    b.attr = a
    gc.collect()
    garbagelen = len(gc.garbage)
    del a, b
    expect(gc.collect(), 4, 'boom2')
    expect(len(gc.garbage), garbagelen, 'boom2')
