def get_error(self, exc, section, option):
    try:
        self.cf.get(section, option)
    except exc as e:
        return e
    else:
        self.fail(('expected exception type %s.%s' % (exc.__module__, exc.__name__)))
