def check_items_config(self, expected):
    cf = self.fromstring('[section]\nname = value\nkey: |%(name)s| \ngetdefault: |%(default)s|\ngetname: |%(__name__)s|', defaults={'default': '<default>', })
    L = list(cf.items('section'))
    L.sort()
    self.assertEqual(L, expected)
