def tostring(self):
    s = ''
    for (lo, hi) in self.pairs:
        if (lo == hi):
            t = repr(lo)
        else:
            t = ((repr(lo) + self.rng) + repr(hi))
        if s:
            s = (s + (self.sep + t))
        else:
            s = t
    return s
