def release(self):
    self.__cond.acquire()
    self.__value = (self.__value + 1)
    if __debug__:
        self._note('%s.release: success, value=%s', self, self.__value)
    self.__cond.notify()
    self.__cond.release()
