def join(self, timeout=None):
    if timeout:
        millis = (timeout * 1000.0)
        millis_int = int(millis)
        nanos = int(((millis - millis_int) * 1000000.0))
        self._thread.join(millis_int, nanos)
    else:
        self._thread.join()
