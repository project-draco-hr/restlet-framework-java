'\n   Tests for the mhlib module\n   Nick Mathewson\n'
import unittest
from test.test_support import run_unittest, TESTFN, TestSkipped, underlying_system
import os, StringIO
import sys
import mhlib
if (underlying_system.startswith('win') or (underlying_system == 'riscos') or underlying_system.startswith('atheos')):
    raise TestSkipped((('skipped on %s -- ' % underlying_system) + 'too many Unix assumptions'))
_mhroot = (TESTFN + '_MH')
_mhpath = os.path.join(_mhroot, 'MH')
_mhprofile = os.path.join(_mhroot, '.mh_profile')
if (__name__ == '__main__'):
    test_main()
