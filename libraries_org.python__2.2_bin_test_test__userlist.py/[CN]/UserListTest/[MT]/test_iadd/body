def test_iadd(self):
    u = UserList((0, 1))
    u += [0, 1]
    self.assertEqual(u, [0, 1, 0, 1])
    u += UserList([0, 1])
    self.assertEqual(u, [0, 1, 0, 1, 0, 1])
    u = UserList('spam')
    u += 'eggs'
    self.assertEqual(u, list('spameggs'))
