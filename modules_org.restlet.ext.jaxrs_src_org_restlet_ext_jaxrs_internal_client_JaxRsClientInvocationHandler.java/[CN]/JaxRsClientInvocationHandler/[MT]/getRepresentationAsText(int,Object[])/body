{
  String representationAsText=null;
  Representation representation=clientResource.getApplication().getConverterService().toRepresentation(args[argIndex]);
  try {
    representationAsText=representation.getText();
  }
 catch (  IOException exception) {
    throw new RuntimeException(exception);
  }
  return representationAsText;
}
