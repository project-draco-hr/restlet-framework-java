def test_tuple(self):
    t = tuple(self.d.keys())
    new = marshal.loads(marshal.dumps(t))
    self.assertEqual(t, new)
    new = roundtrip(t)
    self.assertEqual(t, new)
    os.unlink(test_support.TESTFN)
