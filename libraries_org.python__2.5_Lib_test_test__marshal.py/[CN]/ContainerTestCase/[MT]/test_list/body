def test_list(self):
    lst = self.d.items()
    new = marshal.loads(marshal.dumps(lst))
    self.assertEqual(lst, new)
    new = roundtrip(lst)
    self.assertEqual(lst, new)
    os.unlink(test_support.TESTFN)
