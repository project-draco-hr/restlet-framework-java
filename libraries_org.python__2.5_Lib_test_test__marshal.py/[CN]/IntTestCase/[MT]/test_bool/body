def test_bool(self):
    for b in (True, False):
        new = marshal.loads(marshal.dumps(b))
        self.assertEqual(b, new)
        self.assertEqual(type(b), type(new))
        new = roundtrip(b)
        self.assertEqual(b, new)
        self.assertEqual(type(b), type(new))
