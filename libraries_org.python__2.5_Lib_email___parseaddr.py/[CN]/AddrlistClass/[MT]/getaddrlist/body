def getaddrlist(self):
    'Parse all addresses.\n\n        Returns a list containing all of the addresses.\n        '
    result = []
    while (self.pos < len(self.field)):
        ad = self.getaddress()
        if ad:
            result += ad
        else:
            result.append(('', ''))
    return result
