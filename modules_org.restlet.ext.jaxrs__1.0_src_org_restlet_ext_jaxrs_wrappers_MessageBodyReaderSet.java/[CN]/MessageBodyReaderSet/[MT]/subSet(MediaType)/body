{
  List<MessageBodyReader> mbrs=new ArrayList<MessageBodyReader>();
  for (  MessageBodyReader mbr : this) {
    if (mbr.supports(mediaType))     mbrs.add(mbr);
  }
  return new MessageBodyReaderSet(mbrs,true);
}
