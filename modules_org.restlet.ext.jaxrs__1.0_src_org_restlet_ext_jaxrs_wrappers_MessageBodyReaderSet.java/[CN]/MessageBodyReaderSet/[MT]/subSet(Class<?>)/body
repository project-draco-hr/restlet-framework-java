{
  List<MessageBodyReader> mbws=new ArrayList<MessageBodyReader>();
  for (  MessageBodyReader mbw : this) {
    if (mbw.isReadable(entityClass))     mbws.add(mbw);
  }
  return new MessageBodyReaderSet(mbws,true);
}
