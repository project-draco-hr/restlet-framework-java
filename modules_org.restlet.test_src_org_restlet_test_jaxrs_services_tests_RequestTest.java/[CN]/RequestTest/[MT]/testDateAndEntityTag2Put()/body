{
  if (CONDTIONS_GETSTATUS_PATCH_NOT_APPLIED)   return;
  final Conditions conditions=createConditions(AFTER,getDatastoreETag());
  final Response response=put("date",null,conditions);
  assertEquals(PREC_FAILED,response.getStatus());
}
