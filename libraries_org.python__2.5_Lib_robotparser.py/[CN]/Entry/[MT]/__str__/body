def __str__(self):
    ret = ''
    for agent in self.useragents:
        ret = (((ret + 'User-agent: ') + agent) + '\n')
    for line in self.rulelines:
        ret = ((ret + str(line)) + '\n')
    return ret
