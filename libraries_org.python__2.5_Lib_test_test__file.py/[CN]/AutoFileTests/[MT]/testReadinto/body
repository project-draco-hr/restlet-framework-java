def testReadinto(self):
    self.f.write('12')
    self.f.close()
    a = array('c', ('x' * 10))
    self.f = open(TESTFN, 'rb')
    n = self.f.readinto(a)
    self.assertEquals('12', a.tostring()[:n])
