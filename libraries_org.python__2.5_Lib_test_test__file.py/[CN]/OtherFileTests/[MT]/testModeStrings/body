def testModeStrings(self):
    for mode in ('', 'aU', 'wU+'):
        try:
            f = open(TESTFN, mode)
        except ValueError:
            pass
        else:
            f.close()
            self.fail(('%r is an invalid file mode' % mode))
