def testStdin(self):
    if (sys.platform != 'osf1V5'):
        self.assertRaises(IOError, sys.stdin.seek, (-1))
    else:
        print  >> sys.__stdout__, '  Skipping sys.stdin.seek(-1), it may crash the interpreter. Test manually.'
    self.assertRaises(IOError, sys.stdin.truncate)
