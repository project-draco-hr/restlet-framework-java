{
  if (isStarted()) {
    for (    Service service : getServices()) {
      service.stop();
    }
    if (getHelper() != null) {
      getHelper().stop();
    }
    AnnotationUtils.clearCache();
    super.stop();
  }
}
