{
  Reference currentSubject=getCurrentSubject();
  if (currentSubject instanceof Reference) {
    if (this.currentObject instanceof Reference) {
      this.graphHandler.link((Reference)currentSubject,this.currentPredicate,(Reference)this.currentObject);
    }
 else     if (this.currentObject instanceof Literal) {
      this.graphHandler.link((Reference)currentSubject,this.currentPredicate,(Literal)this.currentObject);
    }
 else {
    }
  }
 else {
  }
}
