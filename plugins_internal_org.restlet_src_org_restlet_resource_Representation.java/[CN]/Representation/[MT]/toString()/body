{
  String result=null;
  try {
    result=getValue();
  }
 catch (  IOException e) {
    Logger.getAnonymousLogger().log(Level.WARNING,"Unexcepted exception while getting the representation content as a string",e);
  }
  return result;
}
