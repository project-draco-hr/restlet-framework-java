{
  ConvertParameterException cpe=new ConvertParameterException("Could not convert the String \"" + unparseableValue + "\" to a "+ paramType);
  if (cause != null)   cpe.setStackTrace(cause.getStackTrace());
  throw cpe;
}
