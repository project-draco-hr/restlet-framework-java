def readlines(self):
    'Read and return the list of all logical lines remaining in the\n           current file.'
    lines = []
    while 1:
        line = self.readline()
        if (line is None):
            return lines
        lines.append(line)
