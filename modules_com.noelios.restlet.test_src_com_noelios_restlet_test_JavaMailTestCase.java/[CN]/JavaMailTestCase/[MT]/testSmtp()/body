{
  Request request=new Request(Method.POST,"smtp://smtp.mail.yahoo.fr");
  request.setChallengeResponse(new ChallengeResponse(ChallengeScheme.SMTP_PLAIN,YAHOO_ID,YAHOO_PASSWORD));
  sendMail(Protocol.SMTP,request,false);
}
