{
  Request request=new Request(Method.POST,"smtp://alaska.noelios.com");
  request.setChallengeResponse(new ChallengeResponse(ChallengeScheme.SMTP_PLAIN,NOELIOS_LOGIN,NOELIOS_PASSWORD));
  sendMail(Protocol.SMTP,request,true);
}
