def _update(self):
    self._index = {}
    try:
        f = _open(self._dirfile)
    except IOError:
        pass
    else:
        for line in f:
            line = line.rstrip()
            (key, pos_and_siz_pair) = eval(line)
            self._index[key] = pos_and_siz_pair
        f.close()
