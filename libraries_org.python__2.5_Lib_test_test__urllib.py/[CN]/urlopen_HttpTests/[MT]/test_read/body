def test_read(self):
    self.fakehttp('Hello!')
    try:
        fp = urllib.urlopen('http://python.org/')
        self.assertEqual(fp.readline(), 'Hello!')
        self.assertEqual(fp.readline(), '')
    finally:
        self.unfakehttp()
