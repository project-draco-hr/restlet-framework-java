def test_bug828737(self):
    charmap = {ord('&'): u'&amp;', ord('<'): u'&lt;', ord('>'): u'&gt;', ord('"'): u'&quot;', }
    for n in (1, 10, 100, 1000):
        text = (u'abc<def>ghi' * n)
        text.translate(charmap)
