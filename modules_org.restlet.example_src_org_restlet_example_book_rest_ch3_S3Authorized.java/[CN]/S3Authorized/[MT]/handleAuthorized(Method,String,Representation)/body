{
  final Request request=new Request(method,uri,entity);
  request.setChallengeResponse(new ChallengeResponse(ChallengeScheme.HTTP_AWS_S3,PUBLIC_KEY,PRIVATE_KEY));
  return new Client(Protocol.HTTPS).handle(request);
}
