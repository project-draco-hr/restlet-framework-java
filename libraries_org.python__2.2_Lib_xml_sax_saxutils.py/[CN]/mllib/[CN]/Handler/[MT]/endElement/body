def endElement(self, name):
    if hasattr(self.handler, ('end_' + name)):
        self.handler.handle_endtag(name, getattr(self.handler, ('end_' + name)))
    else:
        self.handler.unknown_endtag(name)
    del self.stack[(-1)]
