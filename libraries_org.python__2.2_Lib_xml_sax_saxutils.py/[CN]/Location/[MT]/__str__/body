def __str__(self):
    if (self.__line is None):
        line = '?'
    else:
        line = self.__line
    if (self.__col is None):
        col = '?'
    else:
        col = self.__col
    return ('%s:%s:%s' % ((self.__sysid or self.__pubid or '<unknown>'), line, col))
