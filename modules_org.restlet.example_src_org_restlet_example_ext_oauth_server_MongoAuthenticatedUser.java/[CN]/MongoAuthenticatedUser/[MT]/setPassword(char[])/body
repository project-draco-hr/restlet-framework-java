{
  DBCollection users=OAuth2Sample.getDefaultDB().getCollection("users");
  users.update(new BasicDBObject("_id",getId()),new BasicDBObject("$set",new BasicDBObject("password",new String(password))));
}
