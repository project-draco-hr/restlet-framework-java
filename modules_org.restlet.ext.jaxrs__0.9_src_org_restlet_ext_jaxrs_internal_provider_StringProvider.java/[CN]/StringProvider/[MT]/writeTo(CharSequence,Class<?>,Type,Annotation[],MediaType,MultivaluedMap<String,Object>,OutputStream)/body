{
  final CharacterSet cs=Response.getCurrent().getEntity().getCharacterSet();
  final InputStream inputStream=getInputStream(charSequence,cs.toString());
  Util.copyStream(inputStream,entityStream);
}
