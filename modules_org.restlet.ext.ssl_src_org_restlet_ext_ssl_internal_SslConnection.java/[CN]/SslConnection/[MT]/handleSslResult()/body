{
switch (getSslEngineStatus()) {
case BUFFER_OVERFLOW:
    break;
case BUFFER_UNDERFLOW:
  break;
case CLOSED:
setSslState(SslState.CLOSED);
close(true);
break;
case OK:
handleSslHandshake();
break;
}
}
