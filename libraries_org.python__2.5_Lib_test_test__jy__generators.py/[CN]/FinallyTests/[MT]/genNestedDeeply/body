def genNestedDeeply(self):
    for i in range(4):
        try:
            continue
        finally:
            for j in range(i):
                try:
                    pass
                finally:
                    for k in range(j):
                        try:
                            try:
                                (1 // 0)
                            finally:
                                yield (i, j, k)
                        except:
                            pass
