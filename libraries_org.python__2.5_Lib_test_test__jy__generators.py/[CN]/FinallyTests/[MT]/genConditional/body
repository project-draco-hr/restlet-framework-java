def genConditional(self):
    for i in range(3):
        x = 0
        try:
            if (i == 2):
                continue
            x = 1
        finally:
            for j in range(x, (x + 2)):
                yield j
