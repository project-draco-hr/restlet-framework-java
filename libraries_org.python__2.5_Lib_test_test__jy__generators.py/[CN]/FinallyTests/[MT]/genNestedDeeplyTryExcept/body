def genNestedDeeplyTryExcept(self):
    for i in range(3):
        try:
            try:
                (1 // 0)
            finally:
                for j in range(3):
                    try:
                        (1 // 0)
                    finally:
                        for k in range(3):
                            try:
                                (1 // 0)
                            finally:
                                yield (i, j, k)
        except:
            pass
