def handle_read_event(self):
    if self.accepting:
        if (not self.connected):
            self.connected = True
        self.handle_accept()
    elif (not self.connected):
        self.handle_connect()
        self.connected = True
        self.handle_read()
    else:
        self.handle_read()
