def accept(self):
    try:
        (conn, addr) = self.socket.accept()
        return (conn, addr)
    except socket.error as why:
        if (why[0] == EWOULDBLOCK):
            pass
        else:
            raise
