def test_line_terminator(self):
    s = echo_server()
    s.start()
    time.sleep(1)
    c = echo_client('\n')
    c.push('hello ')
    c.push('world\n')
    asyncore.loop()
    s.join()
    self.assertEqual(c.contents, 'hello world')
