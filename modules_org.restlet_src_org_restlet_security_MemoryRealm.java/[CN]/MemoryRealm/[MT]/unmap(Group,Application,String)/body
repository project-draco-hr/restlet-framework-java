{
  if (application == null) {
    throw new IllegalArgumentException("The application argument can't be null");
  }
  Role role=(application == null) ? null : application.getRole(roleName);
  role=(role == null) ? new Role(application,roleName) : role;
  unmap(group,role);
}
