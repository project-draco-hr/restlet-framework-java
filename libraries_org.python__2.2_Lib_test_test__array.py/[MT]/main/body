def main():
    test_jarray()
    test_java_compat()
    test_java_object_arrays()
    testtype('c', 'c')
    for type in ['b', 'h', 'i', 'l', 'f', 'd']:
        testtype(type, 1)
    init_tests()
    extend_tests()
    fromlist_tests()
    unlink(TESTFN)
