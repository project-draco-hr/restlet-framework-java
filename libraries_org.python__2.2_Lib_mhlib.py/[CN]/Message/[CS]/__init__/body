def __init__(self, f, n, fp=None):
    'Constructor.'
    self.folder = f
    self.number = n
    if (not fp):
        path = f.getmessagefilename(n)
        fp = open(path, 'r')
    mimetools.Message.__init__(self, fp)
