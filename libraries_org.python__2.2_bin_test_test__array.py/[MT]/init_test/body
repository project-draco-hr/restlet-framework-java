def init_test(name, init, typecode):
    if verbose:
        print (40 * '*')
        print name, '- type:', typecode
        print 'initialiser:', init
    a = array(typecode, init)
    if verbose:
        print a
    return a
