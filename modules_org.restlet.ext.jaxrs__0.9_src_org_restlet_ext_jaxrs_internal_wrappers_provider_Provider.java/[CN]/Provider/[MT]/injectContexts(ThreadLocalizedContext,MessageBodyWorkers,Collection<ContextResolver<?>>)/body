{
  Class<? extends Object> providerClass=this.jaxRsProvider.getClass();
  ContextInjector iph=new ContextInjector(providerClass,mbWorkers,allResolvers);
  iph.inject(this.jaxRsProvider,tlContext);
}
