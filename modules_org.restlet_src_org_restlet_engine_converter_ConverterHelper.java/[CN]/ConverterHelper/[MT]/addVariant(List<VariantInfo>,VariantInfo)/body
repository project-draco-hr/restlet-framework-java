{
  if (userVariant != null) {
    if (variants == null) {
      variants=new ArrayList<VariantInfo>();
    }
    userVariant.setConverterHelper(this);
    variants.add(userVariant);
  }
  return variants;
}
