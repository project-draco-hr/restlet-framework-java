{
  if (variants == null) {
    variants=new ArrayList<Variant>();
  }
  if (userVariant != null) {
    variants.add(userVariant);
  }
 else {
    variants.add(defaultVariant);
  }
  return variants;
}
