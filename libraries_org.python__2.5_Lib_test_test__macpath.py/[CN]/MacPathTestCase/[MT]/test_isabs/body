def test_isabs(self):
    isabs = macpath.isabs
    self.assert_(isabs('xx:yy'))
    self.assert_(isabs('xx:yy:'))
    self.assert_(isabs('xx:'))
    self.failIf(isabs('foo'))
    self.failIf(isabs(':foo'))
    self.failIf(isabs(':foo:bar'))
    self.failIf(isabs(':foo:bar:'))
