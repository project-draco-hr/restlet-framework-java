{
  if (!type.equals(List.class))   return false;
  Class<?> genericClass=Util.getGenericClass(genericType);
  if (genericClass == null)   return false;
  return FileItem.class.isAssignableFrom(genericClass);
}
