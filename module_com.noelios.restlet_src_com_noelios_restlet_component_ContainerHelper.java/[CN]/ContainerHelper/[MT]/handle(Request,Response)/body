{
  if (getFirst() != null) {
    getFirst().handle(request,response);
  }
 else {
    response.setStatus(Status.SERVER_ERROR_INTERNAL);
    getComponent().getLogger().log(Level.SEVERE,"The container wasn't properly started, it can't handle calls.");
  }
}
