{
  if ((this.object == null) && (getText() != null)) {
    try {
      SerializationStreamReader streamReader=getSerializationStreamFactory().createStreamReader(getText());
      this.object=(T)streamReader.readObject();
    }
 catch (    Exception e) {
      this.object=null;
      e.printStackTrace();
    }
  }
  return object;
}
