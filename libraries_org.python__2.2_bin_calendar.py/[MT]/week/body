def week(theweek, width):
    'Returns a single week in a string (no newline).'
    days = []
    for day in theweek:
        if (day == 0):
            s = ''
        else:
            s = ('%2i' % day)
        days.append(_center(s, width))
    return ' '.join(days)
