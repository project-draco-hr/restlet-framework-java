def __mul__(self, other):
    if isinstance(other, (int, long)):
        return self.__class__((self.__days * other), (self.__seconds * other), (self.__microseconds * other))
    return NotImplemented
