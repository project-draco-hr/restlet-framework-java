def __div__(self, other):
    if isinstance(other, (int, long)):
        usec = ((((self.__days * (24 * 3600L)) + self.__seconds) * 1000000) + self.__microseconds)
        return self.__class__(0, 0, (usec // other))
    return NotImplemented
