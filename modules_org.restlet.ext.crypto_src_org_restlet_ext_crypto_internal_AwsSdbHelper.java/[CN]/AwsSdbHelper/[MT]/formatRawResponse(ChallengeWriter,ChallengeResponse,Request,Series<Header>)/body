{
  if (request.getChallengeResponse() != null) {
    String signature=AwsUtils.getSdbSignature(request,request.getChallengeResponse().getSecret());
    request.getResourceRef().addQueryParameter("Signature",signature);
  }
}
