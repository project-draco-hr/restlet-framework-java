{
  for (  Class<?> clazz : application.getClasses()) {
    scanClazz(collectInfo,clazz,introspectionHelper);
  }
  for (  Object singleton : application.getSingletons()) {
    if (singleton != null) {
      scanClazz(collectInfo,singleton.getClass(),introspectionHelper);
    }
  }
}
