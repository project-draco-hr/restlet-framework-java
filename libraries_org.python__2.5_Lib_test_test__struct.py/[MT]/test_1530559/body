def test_1530559():
    for endian in ('', '>', '<'):
        for fmt in ('B', 'H', 'I', 'L', 'b', 'h', 'i', 'l'):
            check_float_coerce((endian + fmt), 1.0)
            check_float_coerce((endian + fmt), 1.5)
