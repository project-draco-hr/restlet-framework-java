{
  final Request baseRequest=(servletRequest instanceof Request) ? (Request)servletRequest : AbstractHttpConnection.getCurrentConnection().getRequest();
  this.helper.handle(new JettyCall(this.helper.getHelped(),AbstractHttpConnection.getCurrentConnection()));
  baseRequest.setHandled(true);
}
