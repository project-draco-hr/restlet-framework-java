def __repr__(self):
    data = self.data
    if (len(data) > 10):
        dotdotdot = '...'
    else:
        dotdotdot = ''
    return ('<DOM %s node "%s%s">' % (self.__class__.__name__, data[0:10], dotdotdot))
