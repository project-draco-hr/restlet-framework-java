def _write_data(writer, data):
    'Writes datachars to writer.'
    data = data.replace('&', '&amp;').replace('<', '&lt;')
    data = data.replace('"', '&quot;').replace('>', '&gt;')
    writer.write(data)
