{
  final StringBuilder b=new StringBuilder();
  if (isAttached) {
    b.append("attachment; filename=\"");
  }
 else {
    b.append("inline; filename=\"");
  }
  if (fileName != null) {
    b.append(fileName);
  }
  b.append('"');
  return b.toString();
}
