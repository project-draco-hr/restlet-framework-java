{
  Parameter result=null;
  if (name != null) {
    if (value != null) {
      result=new Parameter(Reference.decode(name.toString()),Reference.decode(value.toString()));
    }
 else {
      result=new Parameter(Reference.decode(name.toString()),null);
    }
  }
  return result;
}
