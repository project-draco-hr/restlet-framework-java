def test_iter_callable(self):


    class C:

        def __init__(self):
            self.i = 0

        def __call__(self):
            i = self.i
            self.i = (i + 1)
            if (i > 100):
                raise IndexError
            return i
    self.check_iterator(iter(C(), 10), range(10))
