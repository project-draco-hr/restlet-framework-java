{
  int extensionsIndex=getLastSegment().indexOf('.');
  int matrixIndex=getLastSegment().indexOf(';');
  return (extensionsIndex != -1) && ((matrixIndex == -1) || (extensionsIndex < matrixIndex));
}
