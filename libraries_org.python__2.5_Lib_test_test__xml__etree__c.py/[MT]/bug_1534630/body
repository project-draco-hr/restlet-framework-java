def bug_1534630():
    '\n    >>> bob = ET.TreeBuilder()\n    >>> e = bob.data("data")\n    >>> e = bob.start("tag", {})\n    >>> e = bob.end("tag")\n    >>> e = bob.close()\n    >>> serialize(ET, e)\n    \'<tag />\'\n    '
