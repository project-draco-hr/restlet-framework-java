{
  float result=-1.0F;
  if (isJaxbRootElementClass(target) || JaxbRepresentation.class.isAssignableFrom(source.getClass())) {
    result=1.0F;
  }
 else   if (JaxbRepresentation.class.isAssignableFrom(target)) {
    result=1.0F;
  }
  return result;
}
