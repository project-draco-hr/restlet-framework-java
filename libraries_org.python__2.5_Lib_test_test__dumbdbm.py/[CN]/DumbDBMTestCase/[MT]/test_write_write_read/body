def test_write_write_read(self):
    f = dumbdbm.open(_fname)
    f['1'] = 'hello'
    f['1'] = 'hello2'
    f.close()
    f = dumbdbm.open(_fname)
    self.assertEqual(f['1'], 'hello2')
    f.close()
