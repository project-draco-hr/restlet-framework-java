{
  if (string == null)   throw throwIllegalArgExc(Integer.MIN_VALUE,nameForMessage,string," must not be null");
  if (encode)   return encodeNotBraces(string,true,true);
 else   EncodeOrCheck.checkForInvalidUriChars(string,Integer.MIN_VALUE,nameForMessage);
  return string.toString();
}
