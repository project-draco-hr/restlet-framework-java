{
  ThreadPoolExecutor result=new ThreadPoolExecutor(getMinThreads(),getMaxThreads(),(long)getThreadMaxIdleTimeMs(),TimeUnit.MILLISECONDS,new LinkedBlockingQueue<Runnable>(),new LoggingThreadFactory(getLogger()));
  return result;
}
