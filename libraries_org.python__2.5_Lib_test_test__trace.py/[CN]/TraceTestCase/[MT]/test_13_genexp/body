def test_13_genexp(self):
    self.run_test(generator_example)
    tracer = Tracer()
    sys.settrace(tracer.traceWithGenexp)
    generator_example()
    sys.settrace(None)
    self.compare_events(generator_example.func_code.co_firstlineno, tracer.events, generator_example.events)
