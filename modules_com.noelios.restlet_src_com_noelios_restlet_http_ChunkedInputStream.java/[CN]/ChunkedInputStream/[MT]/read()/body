{
  int result=-1;
  initialize();
  if (!this.done) {
    if (chunkAvailable()) {
      result=this.source.read();
      this.position++;
    }
 else {
      initializeChunk();
      return read();
    }
  }
  return result;
}
