{
  int result=-1;
  initialize();
  if (!this.endReached) {
    if (!chunkAvailable()) {
      initializeChunk();
    }
    if (!this.endReached) {
      result=this.source.read();
      this.position++;
    }
  }
  return result;
}
