def test_reraise(self):

    def raiser():
        raise Exception(), None, tb
    try:
        raiser()
    except Exception:
        self.assertEqual(tb_info(), [('test_reraise', 'raiser()'), ('<module>', "raise Exception('foo')")])
    else:
        self.fail('Expected Exception')
