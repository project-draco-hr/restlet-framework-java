{
  String result=null;
  try {
    result=URL.decode(toDecode);
  }
 catch (  final NullPointerException npe) {
    System.err.println("Unable to decode the string with the UTF-8 character set.");
  }
  return result;
}
