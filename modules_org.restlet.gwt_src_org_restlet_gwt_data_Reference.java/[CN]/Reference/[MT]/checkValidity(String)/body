{
  if (uriRef != null) {
    for (int i=0; i < uriRef.length(); i++) {
      if (!isValid(uriRef.charAt(i))) {
        throw new IllegalArgumentException("Invalid character detected in URI reference at index '" + i + "': \""+ uriRef.charAt(i)+ "\"");
      }
    }
  }
}
