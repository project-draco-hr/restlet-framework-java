{
  Source result=null;
  if (getIdentifier() != null) {
    result=new StreamSource(getStream(),getIdentifier().getTargetRef().toString());
  }
 else {
    result=new StreamSource(getStream());
  }
  return result;
}
