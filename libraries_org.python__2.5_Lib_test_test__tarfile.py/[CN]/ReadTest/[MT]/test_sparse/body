def test_sparse(self):
    'Test sparse member extraction.\n        '
    if (self.sep != '|'):
        f1 = self.tar.extractfile('S-SPARSE')
        f2 = self.tar.extractfile('S-SPARSE-WITH-NULLS')
        self.assert_((f1.read() == f2.read()), '_FileObject failed on sparse file member')
