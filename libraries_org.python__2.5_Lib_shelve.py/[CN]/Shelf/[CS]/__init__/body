def __init__(self, dict, protocol=None, writeback=False):
    self.dict = dict
    if (protocol is None):
        protocol = 0
    self._protocol = protocol
    self.writeback = writeback
    self.cache = {}
