{
  this.header.append('(');
  char c;
  for (int i=0; i < content.length(); i++) {
    c=content.charAt(i);
    if (HttpUtils.isCommentText(c)) {
      this.header.append(c);
    }
 else {
      appendQuotedPair(c);
    }
  }
  this.header.append(')');
  return this;
}
