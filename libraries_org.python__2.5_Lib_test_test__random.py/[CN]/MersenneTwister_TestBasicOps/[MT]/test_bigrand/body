def test_bigrand(self):
    span = (2 ** 500)
    cum = 0
    for i in xrange(100):
        r = self.gen.randrange(span)
        self.assert_((0 <= r < span))
        cum |= r
    self.assertEqual(cum, (span - 1))
