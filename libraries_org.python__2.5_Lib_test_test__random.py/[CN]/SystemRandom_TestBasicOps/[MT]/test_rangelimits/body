def test_rangelimits(self):
    for (start, stop) in [((-2), 0), (((- (2 ** 60)) - 2), (- (2 ** 60))), ((2 ** 60), ((2 ** 60) + 2))]:
        self.assertEqual(set(range(start, stop)), set([self.gen.randrange(start, stop) for i in xrange(100)]))
