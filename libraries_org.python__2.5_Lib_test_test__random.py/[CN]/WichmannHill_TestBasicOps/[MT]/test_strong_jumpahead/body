def test_strong_jumpahead(self):
    N = 1000
    s = self.gen.getstate()
    self.gen.jumpahead(N)
    r1 = self.gen.random()
    self.gen.setstate(s)
    for i in xrange(N):
        self.gen.random()
    r2 = self.gen.random()
    self.assertEqual(r1, r2)
