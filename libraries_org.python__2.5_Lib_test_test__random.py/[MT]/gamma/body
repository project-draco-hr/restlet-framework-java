def gamma(z, cof=_gammacoeff, g=7):
    z -= 1.0
    sum = cof[0]
    for i in xrange(1, len(cof)):
        sum += (cof[i] / (z + i))
    z += 0.5
    return (((((z + g) ** z) / exp((z + g))) * sqrt((2 * pi))) * sum)
