def tostring(self):
    s = ''
    for (lo, hi) in self.pairs:
        if (lo == hi):
            t = `lo`
        else:
            t = ((`lo` + self.rng) + `hi`)
        if s:
            s = (s + (self.sep + t))
        else:
            s = t
    return s
