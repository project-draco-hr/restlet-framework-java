def indexed_value(self, key, location):
    if (key in self.dict):
        if (len(self.dict[key]) > location):
            return self.dict[key][location]
        else:
            return None
    else:
        return None
