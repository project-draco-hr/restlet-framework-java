def read_lines_to_eof(self):
    'Internal: read lines until EOF.'
    while 1:
        line = self.fp.readline((1 << 16))
        if (not line):
            self.done = (-1)
            break
        self.__write(line)
