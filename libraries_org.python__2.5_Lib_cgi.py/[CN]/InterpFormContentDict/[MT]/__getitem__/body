def __getitem__(self, key):
    v = SvFormContentDict.__getitem__(self, key)
    if (v[0] in '0123456789+-.'):
        try:
            return int(v)
        except ValueError:
            try:
                return float(v)
            except ValueError:
                pass
    return v.strip()
