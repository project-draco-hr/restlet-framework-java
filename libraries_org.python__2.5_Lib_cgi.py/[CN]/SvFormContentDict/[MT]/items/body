def items(self):
    result = []
    for (key, value) in self.dict.items():
        if (len(value) == 1):
            result.append((key, value[0]))
        else:
            result.append((key, value))
    return result
