def noheaders():
    'Return an empty mimetools.Message object.'
    global _noheaders
    if (not _noheaders):
        import mimetools
        import StringIO
        _noheaders = mimetools.Message(StringIO.StringIO(), 0)
        _noheaders.fp.close()
    return _noheaders
