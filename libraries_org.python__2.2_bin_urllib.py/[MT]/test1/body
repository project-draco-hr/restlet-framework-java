def test1():
    import time
    s = ''
    for i in range(256):
        s = (s + chr(i))
    s = (s * 4)
    t0 = time.time()
    qs = quote(s)
    uqs = unquote(qs)
    t1 = time.time()
    if (uqs != s):
        print 'Wrong!'
    print `s`
    print `qs`
    print `uqs`
    print round((t1 - t0), 3), 'sec'
