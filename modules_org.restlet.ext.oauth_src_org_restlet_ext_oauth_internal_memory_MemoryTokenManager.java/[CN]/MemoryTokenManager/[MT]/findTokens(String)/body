{
  ArrayList<Token> list=new ArrayList<Token>();
  for (  MemoryToken token : tokens.values()) {
    if (token.getUsername() != null && token.getUsername().equals(username)) {
      list.add(token);
    }
  }
  return list.toArray(new Token[list.size()]);
}
