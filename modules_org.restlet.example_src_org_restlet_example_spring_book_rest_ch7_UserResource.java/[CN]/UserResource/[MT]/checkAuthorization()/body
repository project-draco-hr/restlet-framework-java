{
  int result=0;
  if (this.user != null) {
    if ((this.login != null) && (this.password != null)) {
      if (this.userName.equals(this.login) && this.password.equals(this.user.getPassword())) {
        result=1;
      }
 else {
        result=-1;
      }
    }
  }
  return result;
}
