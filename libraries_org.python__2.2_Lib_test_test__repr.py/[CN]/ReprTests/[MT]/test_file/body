def test_file(self):
    fp = open(unittest.__file__)
    self.failUnless(repr(fp).startswith(("<open file '%s', mode 'r'" % unittest.__file__)))
    fp.close()
    self.failUnless(repr(fp).startswith(("<closed file '%s', mode 'r'" % unittest.__file__)))
