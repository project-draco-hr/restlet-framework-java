def test_string(self):
    eq = self.assertEquals
    eq(r('abc'), "'abc'")
    eq(r('abcdefghijklmnop'), "'abcdefghijklmnop'")
    s = (('a' * 30) + ('b' * 30))
    expected = ((`s`[:13] + '...') + `s`[(-14):])
    eq(r(s), expected)
    eq(r('"\''), repr('"\''))
    s = (('"' * 30) + ("'" * 100))
    expected = ((`s`[:13] + '...') + `s`[(-14):])
    eq(r(s), expected)
