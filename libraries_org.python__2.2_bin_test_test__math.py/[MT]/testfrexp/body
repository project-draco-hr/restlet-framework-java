def testfrexp(name, (mant, exp), (emant, eexp)):
    if ((abs((mant - emant)) > eps) or (exp != eexp)):
        raise TestFailed, ('%s returned %s, expected %s' % (name, `(mant, exp)`, `(emant, eexp)`))
