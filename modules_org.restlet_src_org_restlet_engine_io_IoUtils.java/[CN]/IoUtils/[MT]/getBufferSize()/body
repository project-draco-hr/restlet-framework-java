{
  int result=BUFFER_SIZE;
  try {
    result=Integer.parseInt(System.getProperties().getProperty("org.restlet.engine.io.buffer.size"));
  }
 catch (  NumberFormatException nfe) {
    result=BUFFER_SIZE;
  }
  return result;
}
