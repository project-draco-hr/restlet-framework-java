{
  InputStream result=null;
  if (readableChannel != null) {
    result=isBlocking(readableChannel) ? Channels.newInputStream(readableChannel) : new NbChannelInputStream(readableChannel);
  }
  return result;
}
