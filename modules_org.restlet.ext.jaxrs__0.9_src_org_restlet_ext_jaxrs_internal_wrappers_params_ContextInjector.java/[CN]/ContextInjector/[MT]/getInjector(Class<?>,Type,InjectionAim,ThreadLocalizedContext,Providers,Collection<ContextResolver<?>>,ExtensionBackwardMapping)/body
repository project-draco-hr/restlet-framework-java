{
  if (declaringClass.equals(UriInfo.class)) {
    return new UriInfoInjector(aim,tlContext);
  }
 else {
    return new EverSameInjector(aim,getInjectObject(declaringClass,genericType,tlContext,mbWorkers,allResolvers,extensionBackwardMapping));
  }
}
