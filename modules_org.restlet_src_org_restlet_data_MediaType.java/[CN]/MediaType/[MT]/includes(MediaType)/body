{
  boolean result=false;
  if (included != null) {
    result=equals(ALL) || equals(included);
    if (!result) {
      result=getMainType().equals(included.getMainType()) && (getSubType().equals(included.getSubType()) || getSubType().equals("*"));
    }
  }
  return result;
}
