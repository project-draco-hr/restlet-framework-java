{
  return new Application(){
    @Override @SuppressWarnings("unchecked") public Set<Class<?>> getClasses(){
      return (Set)Collections.singleton(ThreadLocalContextTestResource.class);
    }
    @Override @SuppressWarnings("unchecked") public Set<Object> getSingletons(){
      return (Set)TestUtils.createSet(new ThreadLocalContextTestExcMapper());
    }
  }
;
}
