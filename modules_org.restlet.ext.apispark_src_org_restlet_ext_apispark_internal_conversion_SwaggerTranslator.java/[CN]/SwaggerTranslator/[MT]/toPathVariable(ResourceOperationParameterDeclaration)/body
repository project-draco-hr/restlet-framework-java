{
  PathVariable result=new PathVariable();
  result.setName(parameter.getName());
  result.setDescription(parameter.getDescription());
  result.setType(SwaggerUtils.toJavaType(new SwaggerUtils.SwaggerTypeFormat(parameter.getType(),parameter.getFormat())));
  return result;
}
