{
  if (!replyToAdded) {
    String header=getHeaders().getValues(SipConstants.HEADER_REPLY_TO);
    try {
      setReplyTo(new AddressReader(header).readValue());
    }
 catch (    Exception e) {
      Context.getCurrentLogger().info(e.getMessage());
    }
  }
  return replyTo;
}
