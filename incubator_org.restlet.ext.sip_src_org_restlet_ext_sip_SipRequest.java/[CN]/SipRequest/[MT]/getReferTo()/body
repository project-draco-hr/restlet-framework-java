{
  if (!referToAdded) {
    String header=getHeaders().getValues(SipConstants.HEADER_REFER_TO);
    try {
      setReferTo(new AddressReader(header).readValue());
    }
 catch (    Exception e) {
      Context.getCurrentLogger().info(e.getMessage());
    }
  }
  return referTo;
}
