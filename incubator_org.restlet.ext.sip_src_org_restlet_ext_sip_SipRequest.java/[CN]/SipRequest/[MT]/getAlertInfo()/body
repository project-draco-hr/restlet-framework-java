{
  if (!alertInfoAdded) {
    String header=getHeaders().getValues(SipConstants.HEADER_ALERT_INFO);
    try {
      setAlertInfo(new AddressReader(header).readValue());
    }
 catch (    Exception e) {
      Context.getCurrentLogger().info(e.getMessage());
    }
  }
  return alertInfo;
}
