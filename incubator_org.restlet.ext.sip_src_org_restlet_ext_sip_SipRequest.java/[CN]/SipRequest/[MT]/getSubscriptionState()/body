{
  if (!subscriptionStateAdded) {
    String header=(getHeaders() == null) ? null : getHeaders().getValues(SipConstants.HEADER_SUBSCRIPTION_STATE);
    if (header != null) {
      try {
        setSubscriptionState(new SubscriptionStateReader(header).readValue());
      }
 catch (      Exception e) {
        Context.getCurrentLogger().info(e.getMessage());
      }
    }
    subscriptionStateAdded=true;
  }
  return subscriptionState;
}
