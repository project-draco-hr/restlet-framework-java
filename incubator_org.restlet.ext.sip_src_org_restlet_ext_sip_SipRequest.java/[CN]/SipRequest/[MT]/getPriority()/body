{
  if (!priorityAdded) {
    String header=getHeaders().getValues(SipConstants.HEADER_PRIORITY);
    try {
      setPriority(Priority.valueOf(header));
    }
 catch (    Exception e) {
      Context.getCurrentLogger().info(e.getMessage());
    }
  }
  return priority;
}
