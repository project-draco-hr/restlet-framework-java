{
  if (!sipIfMatchAdded) {
    String header=getHeaders().getValues(SipConstants.HEADER_SIP_IF_MATCH);
    try {
      setSipIfMatch(Tag.parse(header));
    }
 catch (    Exception e) {
      Context.getCurrentLogger().info(e.getMessage());
    }
  }
  return sipIfMatch;
}
