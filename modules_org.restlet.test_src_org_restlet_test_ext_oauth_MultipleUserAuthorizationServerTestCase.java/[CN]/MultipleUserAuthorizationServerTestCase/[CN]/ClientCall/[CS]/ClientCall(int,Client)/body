{
  this.numTimes=numTimes;
  if (client != null)   myClient=client;
 else   myClient=createClient();
  r=new Random(System.nanoTime());
  params=new OAuthParameters("client1234","secret1234",getProt() + "://localhost:" + oauthServerPort+ "/oauth/",Scopes.toRoles("foo bar"));
}
