{
  if (subTags == null) {
    String[] tags=getName().split("-");
    subTags=new ArrayList<String>();
    if (tags.length > 0) {
      for (int i=1; i < tags.length; i++) {
        subTags.add(tags[i]);
      }
    }
  }
  return subTags;
}
