def test_reflexsive_relations(self):
    (a, zero) = (self.a, Set())
    self.assertEqual((a ^ a), zero)
    self.assertEqual((a - a), zero)
    self.assertEqual((a | a), a)
    self.assertEqual((a & a), a)
    self.assert_((a <= a))
    self.assert_((a >= a))
    self.assert_((a == a))
