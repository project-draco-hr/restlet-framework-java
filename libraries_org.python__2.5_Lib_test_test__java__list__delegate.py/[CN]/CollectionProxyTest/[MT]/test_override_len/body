def test_override_len(self):


    class MyList(ArrayList):

        def __len__(self):
            return (self.size() + 1)
    m = MyList()
    m.addAll(range(0, 10))
    self.assert_((len(m) == 11))
