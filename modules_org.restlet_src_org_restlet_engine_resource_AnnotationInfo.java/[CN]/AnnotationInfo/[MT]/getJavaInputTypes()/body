{
  int count=getJavaMethod().getParameterTypes().length;
  Class<?>[] classes=new Class[count];
  for (int i=0; i < count; i++) {
    classes[i]=GenericTypeResolver.resolveParameterType(getJavaMethod(),i,resourceInterface);
  }
  return classes;
}
