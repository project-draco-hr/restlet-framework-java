{
  super.start();
  server=new HttpServerHelper(null);
  server.setName(getName());
  server.setId(getId());
  server.setNetworkHandler(getNetworkHandler());
  try {
    server.start();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
