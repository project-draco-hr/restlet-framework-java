{
  if (rwadef == null) {
    Introspector i=new Introspector(application,false);
    rwadef=i.getDefinition();
  }
  return new JacksonRepresentation<ApiDeclaration>(SwaggerConverter.getApiDeclaration(category,rwadef));
}
