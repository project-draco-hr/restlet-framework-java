{
  if (rwadef == null) {
    Introspector i=new Introspector(application,false);
    rwadef=i.getDefinition();
  }
  return new JacksonRepresentation<ResourceListing>(SwaggerConverter.getResourcelisting(rwadef));
}
