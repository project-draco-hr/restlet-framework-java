{
  if (rwadef == null) {
    Introspector i=new Introspector(application,false);
    rwadef=i.getDefinition();
  }
  return new JacksonRepresentation<ResourceListing>(new RWADefToSwaggerConverter().getResourcelisting(rwadef));
}
