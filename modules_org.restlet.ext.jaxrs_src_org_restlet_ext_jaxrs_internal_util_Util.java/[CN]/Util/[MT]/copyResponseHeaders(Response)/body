{
  final Series<Parameter> headers=new Form();
  HttpServerAdapter.addResponseHeaders(restletResponse,headers);
  HttpServerAdapter.addEntityHeaders(restletResponse.getEntity(),headers);
  return headers;
}
