{
  final Series<Parameter> headers=new Form();
  ServerAdapter.addResponseHeaders(restletResponse,headers);
  ServerAdapter.addEntityHeaders(restletResponse.getEntity(),headers);
  return headers;
}
