def sanitize(self, s):
    if ((s[:5] == 'pass ') or (s[:5] == 'PASS ')):
        i = len(s)
        while ((i > 5) and (s[(i - 1)] in '\r\n')):
            i = (i - 1)
        s = ((s[:5] + ('*' * (i - 5))) + s[i:])
    return `s`
