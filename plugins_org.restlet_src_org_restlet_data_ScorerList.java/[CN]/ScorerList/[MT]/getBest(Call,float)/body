{
  Scorer result=null;
  float bestScore=0F;
  float score;
  for (  Scorer current : this) {
    score=current.score(call);
    if ((score > bestScore) && (score >= requiredScore)) {
      bestScore=score;
      result=current;
    }
  }
  return result;
}
