def test_header(self):
    (file_location, header) = urllib.urlretrieve('http://www.python.org/')
    os.unlink(file_location)
    self.assert_(isinstance(header, mimetools.Message), 'header is not an instance of mimetools.Message')
