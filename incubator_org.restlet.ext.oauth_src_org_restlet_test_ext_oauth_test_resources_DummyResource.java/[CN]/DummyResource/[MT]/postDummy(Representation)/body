{
  org.restlet.security.User u=getRequest().getClientInfo().getUser();
  if (u != null && u instanceof OAuthUser)   Oauth2ClientTestApplication.user=(OAuthUser)u;
  return new StringRepresentation("Dummy");
}
