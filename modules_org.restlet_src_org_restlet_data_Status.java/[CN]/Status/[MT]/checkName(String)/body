{
  if (name != null) {
    if (name.contains("\n") || name.contains("\r")) {
      throw new IllegalArgumentException("Name of the status must not contain CR or LF characters.");
    }
  }
  return name;
}
