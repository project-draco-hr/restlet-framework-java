def __init__(self, passwd=None):
    if (passwd is None):
        passwd = HTTPPasswordMgr()
    self.passwd = passwd
    self.add_password = self.passwd.add_password
    self.retried = 0
    self.nonce_count = 0
