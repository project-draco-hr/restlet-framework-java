{
  if (this.mediaType == null) {
    final org.restlet.data.MediaType rmt=this.request.getEntity().getMediaType();
    final CharacterSet rCharSet=this.request.getEntity().getCharacterSet();
    this.mediaType=Converter.toJaxRsMediaType(rmt,rCharSet);
  }
  return this.mediaType;
}
