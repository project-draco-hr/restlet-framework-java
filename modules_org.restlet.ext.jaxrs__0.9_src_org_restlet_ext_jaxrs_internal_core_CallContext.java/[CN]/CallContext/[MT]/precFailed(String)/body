{
  ResponseBuilder rb=Response.status(STATUS_PREC_FAILED);
  rb.entity(entityMessage);
  rb.language(Language.ENGLISH.getName());
  rb.type(Converter.toJaxRsMediaType(org.restlet.data.MediaType.TEXT_PLAIN,null));
  return rb;
}
