def next(self):
    m = self.mutex
    c = self.characters
    choose = self.rng.choice
    m.acquire()
    try:
        letters = [choose(c) for dummy in '123456']
    finally:
        m.release()
    return self.normcase(''.join(letters))
