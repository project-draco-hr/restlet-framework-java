{
  throw new CouldNotFindResMethodException(errorRestletUnsupportedMediaType,"there is no java method on class " + resourceClass.getName() + " supporting the http method "+ httpMethod+ " on an "+ (u.isEmptyOrSlash() ? "empty remaining path" : (" remaining path " + u))+ " and the given media type "+ givenMediaType);
}
