{
  int maxBuffer=Math.min(sourceBuffer.remaining(),targetBuffer.remaining());
  int result=(maxCopied == 0) ? maxBuffer : Math.min((int)maxCopied,maxBuffer);
  for (int i=0; i < result; i++) {
    targetBuffer.put(sourceBuffer.get());
  }
  return result;
}
