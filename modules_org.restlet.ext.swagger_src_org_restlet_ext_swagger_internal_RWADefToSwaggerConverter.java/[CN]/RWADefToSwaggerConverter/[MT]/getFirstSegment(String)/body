{
  String segment=null;
  if (path != null) {
    segment="/";
    for (    String part : path.split("/")) {
      if (part != null && !"".equals(part)) {
        segment+=part;
        break;
      }
    }
  }
  return segment;
}
