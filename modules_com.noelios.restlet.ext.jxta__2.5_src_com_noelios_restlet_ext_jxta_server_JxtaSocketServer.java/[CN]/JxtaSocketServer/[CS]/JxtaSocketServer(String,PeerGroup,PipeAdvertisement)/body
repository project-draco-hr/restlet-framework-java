{
  super(name,group,pipe);
  if (pipe == null || (!pipe.getType().equals(PipeService.UnicastType) && !pipe.getType().equals(PipeService.UnicastSecureType))) {
    throw new IllegalArgumentException("invalid pipe: " + pipe);
  }
}
