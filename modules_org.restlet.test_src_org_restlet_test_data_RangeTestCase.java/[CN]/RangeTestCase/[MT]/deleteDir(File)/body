{
  if (dir.exists()) {
    final File[] entries=dir.listFiles();
    for (    final File entry : entries) {
      if (entry.isDirectory()) {
        deleteDir(entry);
      }
      entry.delete();
    }
  }
  dir.delete();
}
