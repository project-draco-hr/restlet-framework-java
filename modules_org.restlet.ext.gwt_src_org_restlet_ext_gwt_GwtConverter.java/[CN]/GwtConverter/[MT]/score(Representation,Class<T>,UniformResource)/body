{
  float result=-1.0F;
  if (Serializable.class.isAssignableFrom(target)) {
    if ((target != null) && MediaType.APPLICATION_JAVA_OBJECT_GWT.isCompatible(source.getMediaType())) {
      result=0.7F;
    }
 else {
      result=0.5F;
    }
  }
  return result;
}
