{
  Collection<MediaType> accepted=null;
  if (mediaType != null) {
    accepted=new ArrayList<MediaType>();
    accepted.addAll(Arrays.asList(mediaType));
  }
  return accessServer(Method.GET,reference,accepted,null,null,null,null,null);
}
