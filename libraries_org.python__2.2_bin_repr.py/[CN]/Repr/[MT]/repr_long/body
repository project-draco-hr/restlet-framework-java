def repr_long(self, x, level):
    s = `x`
    if (len(s) > self.maxlong):
        i = max(0, ((self.maxlong - 3) // 2))
        j = max(0, ((self.maxlong - 3) - i))
        s = ((s[:i] + '...') + s[(len(s) - j):])
    return s
