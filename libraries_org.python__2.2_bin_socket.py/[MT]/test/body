def test():
    s = socket(AF_INET, SOCK_STREAM)
    s.connect(('', 80))
    s.send('GET / HTTP/1.0\r\n\r\n')
    while 1:
        data = s.recv(2000)
        print data
        if (not data):
            break
