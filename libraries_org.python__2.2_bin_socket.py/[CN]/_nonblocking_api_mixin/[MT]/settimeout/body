def settimeout(self, timeout):
    self.timeout = _calctimeoutvalue(timeout)
    if (self.timeout is None):
        self.mode = MODE_BLOCKING
    elif (self.timeout < 1e-06):
        self.mode = MODE_NONBLOCKING
    else:
        self.mode = MODE_TIMEOUT
    self._config()
