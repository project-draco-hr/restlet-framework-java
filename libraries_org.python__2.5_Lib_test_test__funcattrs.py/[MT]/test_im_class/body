def test_im_class():


    class C:

        def foo(self):
            pass
    verify((C.foo.im_class is C))
    verify((C().foo.im_class is C))
    cantset(C.foo, 'im_class', C)
    cantset(C().foo, 'im_class', C)
