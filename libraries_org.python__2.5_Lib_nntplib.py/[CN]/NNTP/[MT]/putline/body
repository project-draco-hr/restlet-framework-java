def putline(self, line):
    'Internal: send one line to the server, appending CRLF.'
    line = (line + CRLF)
    if (self.debugging > 1):
        print '*put*', repr(line)
    self.sock.sendall(line)
