def setUp(self):
    mode = 'w'
    if (sys.platform not in ['cygwin']):
        mode += 't'
    fp = open(temp_filename, mode)
    fp.write(TEST_NETRC)
    fp.close()
    self.netrc = netrc.netrc(temp_filename)
