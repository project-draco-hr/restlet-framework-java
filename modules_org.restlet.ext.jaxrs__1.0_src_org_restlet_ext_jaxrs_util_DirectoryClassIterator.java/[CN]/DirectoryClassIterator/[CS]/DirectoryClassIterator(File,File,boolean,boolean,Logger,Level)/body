{
  super(throwOnExc,logger,logLevel);
  if (!directory.getPath().startsWith(rootDirectory.getPath()))   throw new IllegalArgumentException("the directory must be a under the rootDirectory.");
  pushDir(directory);
  rootDirnameLength=rootDirectory.getPath().length() + 1;
  this.subDir=subDirs;
}
