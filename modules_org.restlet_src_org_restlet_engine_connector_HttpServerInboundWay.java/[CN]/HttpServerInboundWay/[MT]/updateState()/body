{
  Queue<Response> outboundMessages=((HttpServerOutboundWay)getConnection().getOutboundWay()).getMessages();
  if ((getIoState() == IoState.IDLE) && getMessages().isEmpty() && outboundMessages.isEmpty()) {
    setIoState(IoState.INTEREST);
  }
  super.updateState();
}
