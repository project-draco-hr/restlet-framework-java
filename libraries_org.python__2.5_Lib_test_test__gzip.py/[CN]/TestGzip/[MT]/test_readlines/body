def test_readlines(self):
    self.test_write()
    f = gzip.GzipFile(self.filename, 'rb')
    L = f.readlines()
    f.close()
    f = gzip.GzipFile(self.filename, 'rb')
    while 1:
        L = f.readlines(150)
        if (L == []):
            break
    f.close()
