def test_append(self):
    self.test_write()
    f = gzip.GzipFile(self.filename, 'ab')
    f.write((data2 * 15))
    f.close()
    f = gzip.GzipFile(self.filename, 'rb')
    d = f.read()
    f.close()
    self.assertEqual(d, ((data1 * 50) + (data2 * 15)))
