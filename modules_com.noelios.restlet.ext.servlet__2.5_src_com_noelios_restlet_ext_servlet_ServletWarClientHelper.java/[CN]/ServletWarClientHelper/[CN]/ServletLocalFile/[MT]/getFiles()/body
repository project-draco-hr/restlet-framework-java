{
  List<LocalFile> result=null;
  if (isDirectory()) {
    if (this.files == null) {
      setFiles(this.servletContext.getResourcePaths(this.path));
    }
    result=this.files;
  }
  return result;
}
