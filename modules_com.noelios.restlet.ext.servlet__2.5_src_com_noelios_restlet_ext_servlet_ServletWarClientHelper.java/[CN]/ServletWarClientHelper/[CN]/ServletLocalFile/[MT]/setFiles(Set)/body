{
  if (set != null && !set.isEmpty()) {
    this.files=new ArrayList<LocalFile>();
    for (    Object object : set) {
      this.files.add(new ServletLocalFile(this.servletContext,(String)object));
    }
  }
}
