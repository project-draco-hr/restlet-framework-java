def _node(default=''):
    ' Helper to determine the node name of this machine.\n    '
    try:
        import socket
    except ImportError:
        return default
    try:
        return socket.gethostname()
    except socket.error:
        return default
