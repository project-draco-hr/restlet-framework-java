{
  Connection<T> result=null;
  try {
    result=this.helper.createConnection(false,null,null,null);
  }
 catch (  IOException e) {
    helper.getLogger().log(Level.WARNING,"Unable to create a pool object",e);
  }
  return result;
}
