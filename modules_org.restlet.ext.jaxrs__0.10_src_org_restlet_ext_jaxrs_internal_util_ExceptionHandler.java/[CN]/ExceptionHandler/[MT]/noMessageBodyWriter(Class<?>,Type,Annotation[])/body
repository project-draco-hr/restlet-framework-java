{
  this.logger.warning("No message body writer found for class " + entityClass + ", genericType "+ genericType);
  annotations.toString();
  throw new WebApplicationException(Status.NOT_ACCEPTABLE);
}
