def test_list_folders(self):
    self._box.add_folder('one')
    self._box.add_folder('two')
    self._box.add_folder('three')
    self.assert_((len(self._box.list_folders()) == 3))
    self.assert_((set(self._box.list_folders()) == set(('one', 'two', 'three'))))
