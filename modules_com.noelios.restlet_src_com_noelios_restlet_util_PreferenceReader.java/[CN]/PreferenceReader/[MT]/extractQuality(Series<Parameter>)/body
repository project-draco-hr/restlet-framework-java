{
  float result=1F;
  boolean found=false;
  if (parameters != null) {
    Parameter param=null;
    for (Iterator<Parameter> iter=parameters.iterator(); !found && iter.hasNext(); ) {
      param=(Parameter)iter.next();
      if (param.getName().equals("q")) {
        result=PreferenceUtils.parseQuality(param.getValue());
        found=true;
        iter.remove();
      }
    }
  }
  return result;
}
