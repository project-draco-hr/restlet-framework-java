def __repr(self, object, context, level):
    (repr, readable, recursive) = _safe_repr(object, context, self.__depth, level)
    if (not readable):
        self.__readable = 0
    if recursive:
        self.__recursive = 1
    return repr
