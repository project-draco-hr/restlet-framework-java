{
  super(challengeRequest.getScheme());
  this.identifier=identifier;
  this.secret=(secret != null) ? secret.toCharArray() : null;
  org.restlet.engine.security.AuthenticatorUtils.update(this,request,response);
}
