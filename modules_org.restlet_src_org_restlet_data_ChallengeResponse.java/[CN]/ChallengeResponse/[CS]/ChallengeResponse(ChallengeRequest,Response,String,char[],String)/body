{
  super(challengeRequest.getScheme());
  this.identifier=identifier;
  org.restlet.engine.security.AuthenticatorUtils.update(this,response.getRequest(),response,identifier,baseSecret,baseSecretAlgorithm);
}
