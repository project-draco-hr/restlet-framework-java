{
  StringWriter writer=new StringWriter();
  OutputStream out=BioUtils.getOutputStream(writer,CharacterSet.ISO_8859_1);
  out.write("test?".getBytes());
  out.flush();
  out.close();
  assertEquals("test?",writer.toString());
}
