{
  if (Edition.CURRENT != Edition.GWT) {
    return new WrapperList<E>(getDelegate().subList(fromIndex,toIndex));
  }
 else {
    return org.restlet.engine.util.ListUtils.copySubList(this,fromIndex,toIndex);
  }
}
