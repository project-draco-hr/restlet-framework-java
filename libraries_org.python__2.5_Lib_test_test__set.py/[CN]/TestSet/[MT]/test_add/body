def test_add(self):
    self.s.add('Q')
    self.assert_(('Q' in self.s))
    dup = self.s.copy()
    self.s.add('Q')
    self.assertEqual(self.s, dup)
    self.assertRaises(TypeError, self.s.add, [])
