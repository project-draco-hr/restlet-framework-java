def test_constructor(self):
    inner = frozenset([1])
    outer = set([inner])
    element = outer.pop()
    self.assertEqual(type(element), frozenset)
    outer.add(inner)
    outer.remove(inner)
    self.assertEqual(outer, set())
    outer.discard(inner)
