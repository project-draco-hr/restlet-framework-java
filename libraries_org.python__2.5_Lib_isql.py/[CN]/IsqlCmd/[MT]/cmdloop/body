def cmdloop(self, intro=None):
    while 1:
        try:
            cmd.Cmd.cmdloop(self, intro)
        except IsqlExit as e:
            break
        except Exception as e:
            print 
            print e
            print 
        intro = None
