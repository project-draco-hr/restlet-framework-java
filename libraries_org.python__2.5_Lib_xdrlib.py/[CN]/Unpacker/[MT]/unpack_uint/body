def unpack_uint(self):
    i = self.__pos
    self.__pos = j = (i + 4)
    data = self.__buf[i:j]
    if (len(data) < 4):
        raise EOFError
    x = struct.unpack('>L', data)[0]
    try:
        return int(x)
    except OverflowError:
        return x
