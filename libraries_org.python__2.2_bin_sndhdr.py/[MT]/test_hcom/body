def test_hcom(h, f):
    if ((h[65:69] != 'FSSD') or (h[128:132] != 'HCOM')):
        return None
    divisor = get_long_be(h[(128 + 16):(128 + 20)])
    return ('hcom', (22050 / divisor), 1, (-1), 8)
