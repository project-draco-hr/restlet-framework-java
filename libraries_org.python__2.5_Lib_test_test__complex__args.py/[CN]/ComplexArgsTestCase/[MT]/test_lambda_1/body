def test_lambda_1(self):
    f = (lambda (x,): x)
    self.check(f, 3, (3,))
    f = (lambda (x,): x)
    self.check(f, 4, (4,))
    f = (lambda (x,): x)
    self.check(f, 5, (5,))
    f = (lambda (x,): x)
    self.check(f, 6, (6,))
