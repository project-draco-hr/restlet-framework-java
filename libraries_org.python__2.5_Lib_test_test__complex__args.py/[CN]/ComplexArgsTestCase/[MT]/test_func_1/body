def test_func_1(self):

    def f((x,)):
        return x
    self.check(f, 3, (3,))

    def f((x,)):
        return x
    self.check(f, 4, (4,))

    def f((x,)):
        return x
    self.check(f, 5, (5,))

    def f((x,)):
        return x
    self.check(f, 6, (6,))
