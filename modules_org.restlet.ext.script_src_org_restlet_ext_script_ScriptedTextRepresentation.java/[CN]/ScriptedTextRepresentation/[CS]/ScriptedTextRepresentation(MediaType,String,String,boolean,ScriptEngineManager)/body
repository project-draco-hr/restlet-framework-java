{
  super(mediaType);
  this.compositeScript=new CompositeScript(text,scriptEngineManager,defaultScriptEngineName,null,allowCompilation);
  this.compositeScriptContext=new CompositeScriptContext(scriptEngineManager);
}
