{
  try {
    ScriptedTextRepresentationContainer container=new ScriptedTextRepresentationContainer(this,writer,this.errorWriter);
    this.embeddedScript.run(writer,this.errorWriter,this.scriptEngines,new ScriptedTextRepresentationScriptContextController(this,container),false);
  }
 catch (  ScriptException e) {
    IOException ioe=new IOException("Script exception");
    ioe.initCause(e);
    throw ioe;
  }
}
