{
  try {
    this.compositeScript.run(false,writer,this.errorWriter,false,new CompositeScriptContext(this.compositeScript.getScriptEngineManager()),new ExposedScriptedTextRepresentationContainer(this),getScriptContextController());
  }
 catch (  ScriptException e) {
    IOException ioe=new IOException("Script exception");
    ioe.initCause(e);
    throw ioe;
  }
}
