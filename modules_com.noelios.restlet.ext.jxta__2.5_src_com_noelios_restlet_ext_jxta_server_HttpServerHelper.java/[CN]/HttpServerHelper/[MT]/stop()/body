{
  if (logger.isLoggable(Level.FINE)) {
    logger.log(Level.FINE,"stopping");
  }
  stopNetwork();
  stopServer();
  if (logger.isLoggable(Level.FINE)) {
    logger.log(Level.FINE,"stopped");
  }
}
