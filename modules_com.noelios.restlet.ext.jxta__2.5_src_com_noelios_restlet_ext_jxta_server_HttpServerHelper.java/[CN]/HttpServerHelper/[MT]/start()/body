{
  if (logger.isLoggable(Level.FINE)) {
    logger.log(Level.FINE,"starting");
  }
  startNetwork();
  startServer();
  if (logger.isLoggable(Level.FINE)) {
    logger.log(Level.FINE,"started");
  }
}
