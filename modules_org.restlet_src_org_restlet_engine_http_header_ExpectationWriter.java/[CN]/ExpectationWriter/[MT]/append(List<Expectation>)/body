{
  Expectation expectation;
  for (int i=0; i < expectations.size(); i++) {
    if (i > 0) {
      append(", ");
    }
    expectation=expectations.get(i);
    append(expectation);
  }
  return this;
}
