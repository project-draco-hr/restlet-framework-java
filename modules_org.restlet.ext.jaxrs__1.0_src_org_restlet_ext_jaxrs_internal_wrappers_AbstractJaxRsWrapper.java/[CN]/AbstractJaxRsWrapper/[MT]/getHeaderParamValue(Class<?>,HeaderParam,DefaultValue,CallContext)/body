{
  String headerParamValue=Util.getHttpHeaders(callContext.getRequest()).getFirstValue(annotation.value(),true);
  return convertParamValueFromParam(paramClass,headerParamValue,defaultValue,false);
}
