def getValue(self, i):
    try:
        if (type(i) == types.IntType):
            return self.map[self.getName(i)]
        else:
            return self.map[i]
    except KeyError as e:
        return None
