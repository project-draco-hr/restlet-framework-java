def __getpos(self, exception):
    if isinstance(exception, _exceptions.SAXParseException):
        return ('%s:%s:%s' % (exception.getSystemId(), exception.getLineNumber(), exception.getColumnNumber()))
    else:
        return '<unknown>'
