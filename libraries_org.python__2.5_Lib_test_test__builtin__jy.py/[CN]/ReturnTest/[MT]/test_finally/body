def test_finally(self):
    'return in finally causes java.lang.VerifyError at compile time'

    def timeit(f):
        t0 = time.clock()
        try:
            f()
        finally:
            t1 = time.clock()
            return (t1 - t0)
