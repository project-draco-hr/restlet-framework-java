def testmaxpp(data):
    if verbose:
        print 'maxpp'
    if ((audioop.maxpp(data[0], 1) != 0) or (audioop.maxpp(data[1], 2) != 0) or (audioop.maxpp(data[2], 4) != 0)):
        return 0
    return 1
