def testminmax(data):
    if verbose:
        print 'minmax'
    if ((audioop.minmax(data[0], 1) != (0, 2)) or (audioop.minmax(data[1], 2) != (0, 2)) or (audioop.minmax(data[2], 4) != (0, 2))):
        return 0
    return 1
