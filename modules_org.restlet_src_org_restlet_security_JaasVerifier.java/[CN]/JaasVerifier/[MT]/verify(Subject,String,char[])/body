{
  boolean result=true;
  try {
    LoginContext loginContext=new LoginContext(getName(),subject,createCallbackHandler(subject,identifier,secret),getConfiguration());
    loginContext.login();
  }
 catch (  LoginException le) {
    result=false;
  }
  return result;
}
