{
  Status result=getStatusService().getStatus(throwable,request,response);
  if (result == null) {
    getLogger().log(Level.SEVERE,"Unhandled exception or error intercepted",throwable);
    result=new Status(Status.SERVER_ERROR_INTERNAL,throwable);
  }
  return result;
}
