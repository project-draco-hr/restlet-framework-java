{
  if (status < 100 || status >= 600)   throw new IllegalArgumentException("The status must be between 100 (incl) and 600 (excl)");
  if (this.response == null) {
    this.response=new ResponseImpl(status);
  }
 else {
    this.response.setStatus(status);
  }
  return this;
}
