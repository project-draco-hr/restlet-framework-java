{
  Component component=new Component();
  component.getServers().add(Protocol.HTTP,8585);
  component.getClients().add(Protocol.FILE);
  component.getDefaultHost().attach("/rmep",new Application(component.getContext()));
  component.start();
}
