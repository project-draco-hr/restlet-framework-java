{
  Object result=null;
  JacksonRepresentation<?> jacksonSource=null;
  if (source instanceof JacksonRepresentation) {
    jacksonSource=(JacksonRepresentation<?>)source;
  }
 else   if (VARIANT_JSON.isCompatible(source) || VARIANT_JSON_SMILE.isCompatible(source) || VARIANT_APPLICATION_XML.isCompatible(source)|| VARIANT_TEXT_XML.isCompatible(source)|| VARIANT_APPLICATION_YAML.isCompatible(source)|| VARIANT_TEXT_YAML.isCompatible(source)|| VARIANT_TEXT_CSV.isCompatible(source)) {
    jacksonSource=create(source,target);
  }
  if (jacksonSource != null) {
    if ((target != null) && JacksonRepresentation.class.isAssignableFrom(target)) {
      result=jacksonSource;
    }
 else {
      result=jacksonSource.getObject();
    }
  }
  return (T)result;
}
