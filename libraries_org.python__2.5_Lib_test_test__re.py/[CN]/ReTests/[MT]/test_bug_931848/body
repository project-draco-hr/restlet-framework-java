def test_bug_931848(self):
    try:
        unicode
    except NameError:
        pass
    pattern = eval('u"[\\u002E\\u3002\\uFF0E\\uFF61]"')
    self.assertEqual(re.compile(pattern).split('a.b.c'), ['a', 'b', 'c'])
