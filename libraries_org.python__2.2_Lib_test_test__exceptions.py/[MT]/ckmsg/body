def ckmsg(src, msg):
    try:
        compile(src, '<fragment>', 'exec')
    except SyntaxError as e:
        print e.msg
        if (e.msg == msg):
            print 'ok'
        else:
            print 'expected:', msg
    else:
        print 'failed to get expected SyntaxError'
