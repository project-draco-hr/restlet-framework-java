{
  int result=-1;
  if (this.availableSize > 0) {
    result=this.source.read(b,off,Math.min(len,(int)this.availableSize));
    if (result > 0) {
      this.availableSize=this.availableSize - result;
    }
  }
  return result;
}
