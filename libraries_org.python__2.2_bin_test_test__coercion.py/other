import copy
import sys
import warnings
candidates = [2, 4.0, 2L, (2 + 0j), [1], (2,), None, MethodNumber(1), CoerceNumber(2)]
infix_binops = ['+', '-', '*', '/', '**', '%']
prefix_binops = ['divmod']
warnings.filterwarnings('ignore', 'complex divmod\\(\\), // and % are deprecated', DeprecationWarning, 'test_coercion$')
do_infix_binops()
do_prefix_binops()
