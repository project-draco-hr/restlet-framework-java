def test_defaults(self):
    defaults = (1900, 1, 1, 0, 0, 0, 0, 1, (-1))
    strp_output = _strptime.strptime('1', '%m')
    self.failUnless((strp_output == defaults), ('Default values for strptime() are incorrect; %s != %s' % (strp_output, defaults)))
