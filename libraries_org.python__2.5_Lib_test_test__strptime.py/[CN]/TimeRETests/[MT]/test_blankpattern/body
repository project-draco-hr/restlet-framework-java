def test_blankpattern(self):
    test_locale = _strptime.LocaleTime()
    test_locale.timezone = (frozenset(), frozenset())
    self.failUnless((_strptime.TimeRE(test_locale).pattern('%Z') == ''), "with timezone == ('',''), TimeRE().pattern('%Z') != ''")
