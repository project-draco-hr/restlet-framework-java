def test_pattern_escaping(self):
    pattern_string = self.time_re.pattern('\\d+')
    self.failUnless(('\\\\d\\+' in pattern_string), ('%s does not have re characters escaped properly' % pattern_string))
