{
  String result=null;
  if (canDecode()) {
    try {
      result=ByteUtils.toString(getStream());
    }
 catch (    IOException ioe) {
    }
  }
 else {
    result=getWrappedRepresentation().toString();
  }
  return result;
}
