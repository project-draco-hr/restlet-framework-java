{
  ReadableByteChannel result=null;
  if (getByteBuffer().hasRemaining()) {
    if (chunked) {
      getLogger().warning("Chunked encoding not supported (yet) in the NIO connector.");
    }
 else {
      result=new ReadableEntityChannel(getByteBuffer(),getConnection().getSocketChannel(),size);
    }
  }
 else {
    result=getConnection().getSocketChannel();
  }
  return result;
}
