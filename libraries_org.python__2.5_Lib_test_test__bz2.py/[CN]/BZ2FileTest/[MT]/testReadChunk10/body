def testReadChunk10(self):
    self.createTempFile()
    bz2f = BZ2File(self.filename)
    text = ''
    while 1:
        str = bz2f.read(10)
        if (not str):
            break
        text += str
    self.assertEqual(text, text)
    bz2f.close()
