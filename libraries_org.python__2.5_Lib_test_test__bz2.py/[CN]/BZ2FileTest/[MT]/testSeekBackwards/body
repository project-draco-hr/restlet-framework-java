def testSeekBackwards(self):
    self.createTempFile()
    bz2f = BZ2File(self.filename)
    bz2f.read(500)
    bz2f.seek((-150), 1)
    self.assertEqual(bz2f.read(), self.TEXT[(500 - 150):])
    bz2f.close()
