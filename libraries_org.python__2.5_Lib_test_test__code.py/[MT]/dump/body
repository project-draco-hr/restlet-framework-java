def dump(co):
    'Print out a text representation of a code object.'
    for attr in ['name', 'argcount', 'varnames', 'cellvars', 'freevars', 'nlocals']:
        print ('%s: %s' % (attr, getattr(co, ('co_' + attr))))
