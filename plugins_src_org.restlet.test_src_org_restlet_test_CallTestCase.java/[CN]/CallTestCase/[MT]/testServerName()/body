{
  Call call=getCall();
  String name="Restlet";
  call.setServerName(name);
  assertEquals(name,call.getServerName());
  name="Restlet Server";
  call.setServerName(name);
  assertEquals(name,call.getServerName());
}
