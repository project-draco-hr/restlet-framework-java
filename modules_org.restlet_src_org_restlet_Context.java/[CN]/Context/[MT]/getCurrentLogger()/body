{
  if (Edition.CURRENT != Edition.GWT) {
    return (Context.getCurrent() != null) ? Context.getCurrent().getLogger() : Logger.getLogger(Context.class.getCanonicalName());
  }
 else {
    return Logger.getLogger(Context.class.getCanonicalName());
  }
}
