{
  boolean result=equals(ALL);
  if (result) {
  }
 else {
    result=equals(included);
    if (result) {
    }
 else {
      result=getMainType().equals(included.getMainType()) && getSubType().equals("*");
      if (result) {
      }
 else {
      }
    }
  }
  return result;
}
