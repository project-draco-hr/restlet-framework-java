{
  if (mediaType == null)   return null;
  Series<Parameter> parameters=mediaType.getParameters();
  if (parameters == null || parameters.isEmpty())   return mediaType;
  return new org.restlet.data.MediaType(mediaType.getName());
}
