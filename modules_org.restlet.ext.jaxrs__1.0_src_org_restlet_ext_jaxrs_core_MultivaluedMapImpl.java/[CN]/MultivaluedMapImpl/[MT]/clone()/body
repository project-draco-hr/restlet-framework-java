{
  MultivaluedMapImpl<K,V> clone=new MultivaluedMapImpl<K,V>();
  for (  Map.Entry<K,List<V>> entry : this.entrySet())   clone.put(entry.getKey(),new ArrayList<V>(entry.getValue()));
  return clone;
}
