def values(self):
    result = []
    for value in self.dict.values():
        if (len(value) == 1):
            result.append(value[0])
        else:
            result.append(value)
    return result
