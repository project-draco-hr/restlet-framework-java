{
  try {
    if (this.collType != null)     return (Collection)collType.newInstance();
    return null;
  }
 catch (  Exception e) {
    throw new RuntimeException("Could not instantiate the collection type " + collType,e);
  }
}
