{
  Call call=getCall();
  String name="Restlet";
  call.setClientName(name);
  assertEquals(name,call.getClientName());
  name="Restlet Client";
  call.setClientName(name);
  assertEquals(name,call.getClientName());
}
