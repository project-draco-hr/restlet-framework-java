{
  ClientData client=getCall().getClient();
  String name="Restlet";
  client.setName(name);
  assertEquals(name,client.getName());
  name="Restlet Client";
  client.setName(name);
  assertEquals(name,client.getName());
}
