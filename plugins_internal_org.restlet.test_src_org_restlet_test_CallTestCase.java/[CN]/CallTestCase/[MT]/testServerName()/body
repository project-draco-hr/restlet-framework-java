{
  Call call=getCall();
  String name="Restlet";
  call.getServer().setName(name);
  assertEquals(name,call.getServer().getName());
  name="Restlet Server";
  call.getServer().setName(name);
  assertEquals(name,call.getServer().getName());
}
