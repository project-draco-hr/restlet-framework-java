{
  Call call=getCall();
  String name="Restlet";
  call.getServer().setAgent(name);
  assertEquals(name,call.getServer().getAgent());
  name="Restlet Server";
  call.getServer().setAgent(name);
  assertEquals(name,call.getServer().getAgent());
}
