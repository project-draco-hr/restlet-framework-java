{
  if (segments == null)   throw new IllegalArgumentException("The segments must not be null");
  for (int i=0; i < segments.length; i++)   segments[i]=encode(segments[i],i + ". segment of the path (index started with 0)");
  for (  String segment : segments)   reference.addSegment(segment);
  return this;
}
