def testavgpp(data):
    if verbose:
        print 'avgpp'
    if ((audioop.avgpp(data[0], 1) != 0) or (audioop.avgpp(data[1], 2) != 0) or (audioop.avgpp(data[2], 4) != 0)):
        return 0
    return 1
