def testtostereo(data):
    if verbose:
        print 'tostereo'
    data2 = ''
    for d in data[0]:
        data2 = ((data2 + d) + d)
    if (audioop.tostereo(data[0], 1, 1, 1) != data2):
        return 0
    return 1
