def testall():
    data = [gendata1(), gendata2(), gendata4()]
    names = dir(audioop)
    routines = []
    for n in names:
        if (type(eval(('audioop.' + n))) == type(audioop.add)):
            routines.append(n)
    for n in routines:
        testone(n, data)
