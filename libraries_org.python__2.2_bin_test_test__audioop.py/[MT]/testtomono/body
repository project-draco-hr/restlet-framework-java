def testtomono(data):
    if verbose:
        print 'tomono'
    data2 = ''
    for d in data[0]:
        data2 = ((data2 + d) + d)
    if (audioop.tomono(data2, 1, 0.5, 0.5) != data[0]):
        return 0
    return 1
