{
  int result=Guard.AUTHENTICATION_MISSING;
  final String identifier=cr.getIdentifier();
  final char[] secret=cr.getSecret();
  if ((identifier != null) && (secret != null)) {
    result=guard.checkSecret(request,identifier,secret) ? Guard.AUTHENTICATION_VALID : Guard.AUTHENTICATION_INVALID;
  }
  return result;
}
