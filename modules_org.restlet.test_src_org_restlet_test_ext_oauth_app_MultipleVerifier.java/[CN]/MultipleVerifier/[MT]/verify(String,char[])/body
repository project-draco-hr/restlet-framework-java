{
  if (users == null)   return RESULT_UNKNOWN;
  if (identifier == null || secret == null)   return RESULT_UNKNOWN;
  String pass=users.get(identifier);
  if (pass == null)   return RESULT_UNKNOWN;
  return compare(pass.toCharArray(),secret) ? RESULT_VALID : RESULT_INVALID;
}
