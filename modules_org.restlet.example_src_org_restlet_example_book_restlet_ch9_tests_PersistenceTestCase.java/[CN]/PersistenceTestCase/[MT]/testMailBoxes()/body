{
  assertEquals(mailRoot.getMailBoxes().size(),3);
  assertEquals(mailRoot.getMailBoxes().get(0).getOwner(),mailRoot.getUsers().get(0));
  assertTrue(mailRoot.getMailBoxes().get(2).getOwner().isAdministrator());
}
