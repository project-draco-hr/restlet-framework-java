def load_setitems(self):
    stack = self.stack
    mark = self.marker()
    dict = stack[(mark - 1)]
    for i in range((mark + 1), len(stack), 2):
        dict[stack[i]] = stack[(i + 1)]
    del stack[mark:]
