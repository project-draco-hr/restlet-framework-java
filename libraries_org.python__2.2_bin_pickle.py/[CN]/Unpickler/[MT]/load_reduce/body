def load_reduce(self):
    stack = self.stack
    callable = stack[(-2)]
    arg_tup = stack[(-1)]
    del stack[(-2):]
    if (arg_tup is None):
        value = callable.__basicnew__()
    else:
        value = apply(callable, arg_tup)
    self.append(value)
