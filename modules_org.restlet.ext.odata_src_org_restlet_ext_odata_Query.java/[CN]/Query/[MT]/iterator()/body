{
  Iterator<T> result=null;
  try {
    execute();
    result=new FeedParser<T>(getFeed(),this.entityClass,getSession().getMetadata()).parse();
  }
 catch (  Exception e) {
    getLogger().log(Level.WARNING,"Can't parse the content of " + createTargetUri(),e);
  }
  return result;
}
