{
  Reference targetUri=new Reference(getRootRef() + "/openid_login");
  targetUri.addQueryParameter("openid_identifier",AuthorizationServerTest.prot + "://localhost:" + AuthorizationServerTest.serverPort+ "/oauth/provider");
  redirectTemporary(targetUri);
  return new EmptyRepresentation();
}
