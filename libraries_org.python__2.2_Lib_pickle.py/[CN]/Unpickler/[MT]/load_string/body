def load_string(self):
    rep = self.readline()[:(-1)]
    if (not self._is_string_secure(rep)):
        raise ValueError, 'insecure string pickle'
    self.append(eval(rep, {'__builtins__': {}, }))
