{
  getLogger().info("Stopping the Restlet NIO " + getProtocols() + " server");
  super.stop();
  if (getServerSocketChannel() != null) {
    getServerSocketChannel().close();
  }
  getAttributes().put("ephemeralPort",-1);
}
