{
  if (isSelectable()) {
    return ((org.restlet.engine.io.SelectionChannel)getChannel()).register(java.nio.channels.SelectionKey.OP_READ,listener);
  }
 else {
    throw new IllegalStateException("The representation isn't selectable");
  }
}
