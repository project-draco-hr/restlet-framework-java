{
  if (isSelectable()) {
    return ((org.restlet.engine.io.SelectionChannel)getChannel()).register(SelectionKey.OP_READ,listener);
  }
 else {
    throw new IllegalStateException("The representation isn't selectable");
  }
}
