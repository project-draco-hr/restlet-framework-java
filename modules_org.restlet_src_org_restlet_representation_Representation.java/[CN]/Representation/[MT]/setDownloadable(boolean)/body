{
  if (getDisposition() == null) {
    this.disposition=new Disposition((downloadable ? Disposition.DISPOSITION_TYPE_ATTACHMENT : Disposition.DISPOSITION_TYPE_INLINE));
  }
 else {
    this.disposition.setType((downloadable ? Disposition.DISPOSITION_TYPE_ATTACHMENT : Disposition.DISPOSITION_TYPE_INLINE));
  }
}
