{
  if (getDisposition() == null) {
    this.disposition=new Disposition((downloadable ? Disposition.TYPE_ATTACHMENT : Disposition.TYPE_INLINE));
  }
 else {
    this.disposition.setType((downloadable ? Disposition.TYPE_ATTACHMENT : Disposition.TYPE_INLINE));
  }
}
