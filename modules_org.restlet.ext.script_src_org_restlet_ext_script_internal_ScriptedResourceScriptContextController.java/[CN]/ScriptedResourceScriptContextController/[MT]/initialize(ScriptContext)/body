{
  scriptContext.setAttribute(this.resource.getContainerVariableName(),this.container,ScriptContext.ENGINE_SCOPE);
  ScriptContextController scriptContextController=this.resource.getScriptContextController();
  if (scriptContextController != null) {
    scriptContextController.initialize(scriptContext);
  }
  this.resource.getScriptSource().initialize(scriptContext);
}
