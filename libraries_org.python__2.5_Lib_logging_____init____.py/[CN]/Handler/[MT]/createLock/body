def createLock(self):
    '\n        Acquire a thread lock for serializing access to the underlying I/O.\n        '
    if thread:
        self.lock = threading.RLock()
    else:
        self.lock = None
