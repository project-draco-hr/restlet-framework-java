{
  int result=RESULT_VALID;
  try {
    LoginContext loginContext=new LoginContext(getName(),request.getClientInfo().getSubject(),createCallbackHandler(request,response),getConfiguration());
    loginContext.login();
  }
 catch (  LoginException le) {
    result=RESULT_INVALID;
  }
  return result;
}
