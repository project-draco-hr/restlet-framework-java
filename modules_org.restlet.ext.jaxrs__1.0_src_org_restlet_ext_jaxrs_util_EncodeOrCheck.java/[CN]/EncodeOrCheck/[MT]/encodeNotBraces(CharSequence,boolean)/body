{
  StringBuilder stb=new StringBuilder();
  int l=uriPart.length();
  for (int i=0; i < l; i++) {
    char c=uriPart.charAt(i);
    if (c == '{' || c == '}' || (!encodeSlash && c == '/'))     stb.append(c);
 else     encode(c,stb);
  }
  return stb;
}
