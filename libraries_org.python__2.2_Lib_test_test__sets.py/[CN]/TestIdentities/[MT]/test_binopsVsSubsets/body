def test_binopsVsSubsets(self):
    (a, b) = (self.a, self.b)
    self.assert_(((a - b) < a))
    self.assert_(((b - a) < b))
    self.assert_(((a & b) < a))
    self.assert_(((a & b) < b))
    self.assert_(((a | b) > a))
    self.assert_(((a | b) > b))
    self.assert_(((a ^ b) < (a | b)))
