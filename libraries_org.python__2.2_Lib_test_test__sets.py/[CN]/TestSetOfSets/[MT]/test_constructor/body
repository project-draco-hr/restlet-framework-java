def test_constructor(self):
    inner = Set([1])
    outer = Set([inner])
    element = outer.pop()
    self.assertEqual(type(element), ImmutableSet)
    outer.add(inner)
    outer.remove(inner)
    self.assertEqual(outer, Set())
    outer.discard(inner)
