def test_delete_deref(self):
    source = re.sub('(?m)^ *:', '', '            :def foo(x):\n            :  def bar():\n            :    print x\n            :  del x\n            :')
    self._check_error(source, 'nested scope')
