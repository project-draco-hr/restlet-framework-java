{
  String result=null;
  if (schemeIndex != -1) {
    if (fragmentIndex != -1) {
      result=this.uri.substring(schemeIndex + 1,fragmentIndex);
    }
 else {
      result=this.uri.substring(schemeIndex + 1);
    }
  }
  return result;
}
