def test_utime(self):
    new_utime = (os.stat(self.basename1).st_mtime + 100)
    os.utime(self.basename1, (new_utime, new_utime))
    self.assertEqual(os.stat(self.basename1).st_mtime, os.stat(self.filename1).st_mtime)
