def test_windows_getcwd_ensures_drive_letter(self):
    drive = os.path.splitdrive(self.subdir)[0]
    os.chdir('\\')
    self.assertEqual(os.path.normcase(os.getcwd()), os.path.normcase(os.path.join(drive, '\\')))
