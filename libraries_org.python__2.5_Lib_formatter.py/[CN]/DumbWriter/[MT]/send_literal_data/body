def send_literal_data(self, data):
    self.file.write(data)
    i = data.rfind('\n')
    if (i >= 0):
        self.col = 0
        data = data[(i + 1):]
    data = data.expandtabs()
    self.col = (self.col + len(data))
    self.atbreak = 0
