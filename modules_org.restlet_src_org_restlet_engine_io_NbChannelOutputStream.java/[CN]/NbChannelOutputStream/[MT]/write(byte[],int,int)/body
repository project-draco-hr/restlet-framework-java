{
  for (int index=0; index < len; index=index + NioUtils.BUFFER_SIZE) {
    int size=len - index > NioUtils.BUFFER_SIZE ? NioUtils.BUFFER_SIZE : len - index;
    this.bb.clear();
    this.bb.put(b,index,size);
    this.bb.flip();
    doWrite();
  }
}
