def read_uint1(f):
    "\n    >>> import StringIO\n    >>> read_uint1(StringIO.StringIO('\\xff'))\n    255\n    "
    data = f.read(1)
    if data:
        return ord(data)
    raise ValueError('not enough data in stream to read uint1')
