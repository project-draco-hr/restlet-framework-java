{
  if (getLogger().isLoggable(Level.FINE)) {
    getLogger().fine("New base URI: " + request.getResourceRef().getBaseRef());
    getLogger().fine("New relative part: " + request.getResourceRef().getRemainingPart());
  }
}
