def ftperrors():
    'Return the set of errors raised by the FTP class.'
    global _ftperrors
    if (not _ftperrors):
        import ftplib
        _ftperrors = ftplib.all_errors
    return _ftperrors
