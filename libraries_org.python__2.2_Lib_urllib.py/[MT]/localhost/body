def localhost():
    "Return the IP address of the magic hostname 'localhost'."
    global _localhost
    if (not _localhost):
        _localhost = socket.gethostbyname('localhost')
    return _localhost
