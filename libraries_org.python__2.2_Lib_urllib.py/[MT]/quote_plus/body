def quote_plus(s, safe=''):
    "Quote the query fragment of a URL; replacing ' ' with '+'"
    if (' ' in s):
        l = s.split(' ')
        for i in range(len(l)):
            l[i] = quote(l[i], safe)
        return '+'.join(l)
    else:
        return quote(s, safe)
