def unwrap(url):
    "unwrap('<URL:type://host/path>') --> 'type://host/path'."
    url = url.strip()
    if ((url[:1] == '<') and (url[(-1):] == '>')):
        url = url[1:(-1)].strip()
    if (url[:4] == 'URL:'):
        url = url[4:].strip()
    return url
