{
  try {
    serverWrapper.startServer(rootResourceClasses,protocol,port,challengeScheme,contextParameter);
  }
 catch (  Exception e) {
    try {
      serverWrapper.stopServer();
    }
 catch (    Exception e1) {
    }
    throw e;
  }
}
