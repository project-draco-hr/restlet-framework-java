{
  if (serverWrapperFactory == null) {
    if (USE_TCP)     serverWrapperFactory=new RestletServerWrapperFactory();
 else     serverWrapperFactory=new DirectServerWrapperFactory();
  }
  return serverWrapperFactory;
}
