{
  Reference reference=createReference(klasse,subPath);
  Client client=createClient();
  Request request=new Request(httpMethod,reference);
  if (conditions != null)   request.setConditions(conditions);
  if (clientInfo != null)   request.setClientInfo(clientInfo);
  Response response=client.handle(request);
  return response;
}
