{
  try {
    this.jaxRsApplication=createApplication(appConfig,challengeScheme,roleChecker);
    serverWrapper.startServer(jaxRsApplication,challengeScheme,protocol);
  }
 catch (  Exception e) {
    try {
      stopServer();
    }
 catch (    Exception e1) {
    }
    throw e;
  }
}
