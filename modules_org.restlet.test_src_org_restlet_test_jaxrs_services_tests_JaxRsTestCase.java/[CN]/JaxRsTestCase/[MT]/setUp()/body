{
  super.setUp();
  if (shouldStartServerInSetUp()) {
    startServer();
    try {
      Thread.sleep(50);
    }
 catch (    InterruptedException e) {
      Thread.currentThread().interrupt();
    }
  }
}
