{
  super.setUp();
  if (shouldStartServerInSetUp()) {
    startServer();
    try {
      Thread.sleep(200);
    }
 catch (    InterruptedException e) {
      Thread.currentThread().interrupt();
    }
  }
}
