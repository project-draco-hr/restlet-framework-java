{
  super.setUp();
  if (shouldStartServerInSetUp()) {
    startServer();
    try {
      Thread.sleep(100);
    }
 catch (    InterruptedException e) {
      Thread.currentThread().interrupt();
    }
  }
}
