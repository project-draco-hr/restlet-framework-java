{
  try {
    return config.getTemplate(templateName);
  }
 catch (  final IOException e) {
    logger.warning("Unable to get the template " + templateName + ". Error message: "+ e.getMessage());
    return null;
  }
}
