'Test script for the bsddb C module\n   Roger E. Masse\n'
import os
import bsddb
import dbhash
import tempfile
from test_support import verbose, verify
types = [(bsddb.btopen, 'BTree'), (bsddb.hashopen, 'Hash Table')]
for type in types:
    test(type[0], type[1])
